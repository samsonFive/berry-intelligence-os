fastapi==0.116.1
uvicorn[standard]==0.35.0
jinja2==3.1.6
pydantic==2.11.7
jsonschema==4.25.0
python-multipart==0.0.20
feedparser==6.0.11
httpx==0.28.1
# trafilatura -- readable-article-text extraction for
# app/services/article_acquisition.py (Article Ingestion vertical slice).
# Pulls in lxml/courlan/htmldate/justext as its own transitive
# dependencies; not pinned individually here, consistent with how this
# file already treats fastapi/uvicorn's own sub-dependencies. Verified
# working (2.2.0) against real trade-press article pages during this
# feature's own pilot run.
trafilatura==2.2.0
openpyxl==3.1.5
playwright==1.62.0
faster-whisper==1.1.1
# faster-whisper 1.1.1's own model-download helper (faster_whisper/utils.py)
# imports `requests` directly but does not declare it as an install
# dependency (verified: `pip show faster-whisper` lists only av,
# ctranslate2, huggingface-hub, onnxruntime, tokenizers, tqdm) -- pinned
# explicitly here so `pip install -r requirements.txt` alone is sufficient.
requests==2.32.3
# yt-dlp -- YouTube caption/audio metadata resolution for
# app/services/youtube_media_acquisition.py. No system ffmpeg dependency:
# used only to inspect a public video's caption tracks and to resolve/
# download an existing audio-only stream as-is (no --extract-audio/
# post-processing), which the already-installed faster-whisper decodes
# itself via its own bundled PyAV/ffmpeg libraries. Pinned to the version
# verified working against real YouTube videos on 2026-08-16; YouTube
# changes its player/extraction requirements often enough that yt-dlp
# ships frequent releases and an old pin can start failing outright (a
# 2025.8.11 pin, current as of this project's other dependency pins,
# already failed against real videos during this verification and had to
# be upgraded) -- operators should expect to bump this pin periodically,
# independent of this project's own release cadence.
yt-dlp==2026.7.4
