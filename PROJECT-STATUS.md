# Project Status

*Maintained by Claude. Updated at the end of each work session that changes project state. Keep short — this is a status check, not a changelog (see `docs/reviews/CURRENT-STATE-AUDIT.md` and `docs/v2/` for detail).*

**Last updated:** 2026-08-23, Atomic Evidence Gold Set V1 merged, `51e374f`

**Governing roadmap for the post-foundation expansion phase:** `docs/v2/INTELLIGENCE-EXPANSION-BUILD-GUIDE.md`, adopted 2026-08-20 — read it before proposing new acquisition, domain-depth, variety, alternative-data, or UI V2 work. Its companions, both live and required reading alongside it: `docs/v2/TECHNICAL-DEBT-REGISTER.md` (every known unresolved bug/limitation, by lane) and `docs/v2/INTELLIGENCE-COVERAGE-MATRIX.md` (the live berry × source-class × geography coverage map).

---

**Current program:**
Intelligence OS V2

**Current stage:**
Phase 2 implementation complete (acceptance-reviewed). Production remains JSON-backed. Discovery, media transcription, publication-artifact orchestration, atomic Evidence proposal, article ingestion, and freshness/dedup layers are integrated on `v2/intelligence-os`; both human review gates remain mandatory. A real demo instance is deployed at `https://intel.johnnyaceii.com` (IONOS VPS, Docker/Caddy, application session login per PR #13). V2 AppShell is approved. Pending Review (`/pending`), Reading Queue, Assessments, Monitor (Watches, Alerts, Source Health), Variety Intelligence (`/entities/variety`), Global Intelligence Search (`/search`, `#v2SearchOffcanvas`), **Claim Testing** (`/queues/testing`), and **Commercial Positions** (`/queues/commercial_position`) are migrated; Landscape / Sources inventory-config admin / system/admin stay unmigrated.

Atomic Extraction Qualification Harness V2 extends the existing human-gated
qualification workflow with a deterministic Atomic Evidence Gold Set contract
and independent precision, recall, atomicity, grounding, entity, scope,
overreach, and duplication scores. Critical unsupported inference is a hard
failure; one generic summary cannot satisfy multi-proposition recall. Evaluation
artifacts now retain private raw responses, normalized proposals, scores,
failures, latency, and token/cost metadata when available. Qualification is
bound to exact provider/model/endpoint family/prompt/extraction/settings plus
both benchmark hashes, and still requires explicit operator approval. Recurring
production extraction remains OFF. The canonical Gold Set had not yet landed
when implementation began, so only a non-production contract fixture was run;
see `docs/v2/ATOMIC-EXTRACTION-QUALIFICATION-HARNESS-V2.md`.

CI / Merge Gate Reliability V1 adds a dedicated, read-only PR validation
workflow with stable repository-integrity, static-public-safety, and full-suite
check contexts plus an explicit Markdown-only fast path. It fixes the prior
zero-check root cause (the only workflow was push-only Pages deployment), makes
one mutable-inbox-sensitive test hermetic, and expands generated-site leakage
fixtures across private drafts, review history, reviewer identity, analyst queue,
signal candidates, and unpublished proposals. GitHub green/red/recovery runs
prove the gate and cancellation behavior; canonical branch protection now
requires all four current-head checks with strict freshness and admin enforcement.
The separate one-file Markdown proof also passed all four named contexts in
4-6s while executable setup/test/build steps were explicitly skipped. PR #95
merged as `f39bf89`; its exact-SHA Pages build and deployment passed and the
public site returned HTTP 200. See
`docs/v2/CI-MERGE-GATE-RELIABILITY-V1.md`.

Canonical Data Promotion / Runtime Sync V1 is canonical via PR #92 (merge `878dd8e`) and production-proven. It adds an explicit, verified-backup-gated three-way promotion contract for existing trusted JSON records. Startup remains additive for new files and Source IDs, authoritative only for the pipeline registry, and never overwrites a differing existing trusted record. The first production baseline covered 1,856 equal promotable records; dry-run reported 2,543 unchanged, 57 protected legacy trusted-record conflicts with no baseline, 18 protected non-promotable import/reference divergences, zero safe updates, and zero new files. Every pre-existing `data/` and `inbox/` byte survived the rebuild; only the new manifest increased the data-file count. Semantic hashes kept 2,191 raw-format-different JSON pairs unchanged rather than creating false conflicts. Historical reconstruction of the 275-record Evidence berry-tagging repair remains exactly 275 safe updates, 991 unchanged, and zero genuine divergences/conflicts, proving that migration would now use one backup-gated promotion instead of manual copying. Runtime-only trusted records, collection state, review events, analyst queues, scheduler state, and all other inbox artifacts remain protected. Full contract and proof: `docs/v2/CANONICAL-DATA-PROMOTION-RUNTIME-SYNC-V1.md`.

Collection Runtime + Data Integrity V1 hardening is canonical via PR #70.
Production Collection Operations V1 adds one registry-driven production
dispatcher: article/news runs every six hours, spoken media daily, patent and
CPVO weekly, and verified backup rotation daily. Trade/weather correctly stay
manual while configured as fixed historical pilots. Useful partial Source runs
are no longer represented as total service failure. `collection_status.py`
defaults to fast persisted operational state; deep item recomputation is
explicit. The current 1,108-draft review backlog is HIGH pressure and remains
human-gated. Review Capacity + Collection Backpressure V1 adds read-only load,
age, Source/query economics, and critical-policy simulation; automatic
throttling remains OFF because recorded review outcomes are insufficient.
Review Outcome Instrumentation V1 now records new human transitions
prospectively in a private append-only runtime ledger and exposes count-only
operator metrics; no historical decisions were inferred and rates remain
insufficient until 30 applicable decisions across at least two days.
Full operating contracts: `docs/v2/PRODUCTION-COLLECTION-OPERATIONS-V1.md` and
`docs/v2/REVIEW-CAPACITY-COLLECTION-BACKPRESSURE-V1.md`.

**Current branch:**
`v2/intelligence-os` (mainline). The feature worktrees remain only as development/cache locations.

**Next phase:**
Next-lane recommendation from the just-completed Atomic Evidence Gold Set V1 mission: **Codex should now run model qualification (`scripts/qualify_extraction_model.py`) against the gold set** (`docs/v2/ATOMIC-EVIDENCE-GOLD-SET-V1.md`) -- 17 scored real trusted sources plus a mandatory flagship atomicity-vs-summarization case, with a deterministic 8-dimension rubric, 9 forbidden-extraction categories, and 11 grounded failure types, all reusing existing Evidence/Fact/Relationship terminology (no new schema). This mission explicitly stops short of running any model. Separately, and unchanged from Caneberry Review Preparation V1's own recommendation: **the small, high-value caneberry pending-review backlog remains the other outstanding priority** -- nothing in this mission touched trust state, so that backlog is exactly as it was. This mission's single clearest blocker was not missing data or missing modeling: Yosemite (Planasa's real blackberry variety, independently verified in a prior mission) could not become a trusted Variety entity because its only grounding Evidence has sat as an unreviewed draft since 2026-08-22 -- the same wall a future mission will hit again for any real event discovered but not yet human-reviewed. This mission added 2 more real, high-value items to that same backlog (the Ouachita and Ponca CPVO filings). None of these require new discovery, new sources, or new entity modeling -- they require a human publication-review pass. A bounded review pass on caneberry-tagged pending drafts specifically (Yosemite, the 2 new CPVO filings, and the still-pending "PSG expands Rejoice" draft from Blackberry/Raspberry Vertical V1) would convert already-discovered, already-evidenced intelligence into trusted records without any further AI-driven expansion work. Second-priority candidates, unchanged from prior missions' own findings: Retail/Commercial Observation expansion (already found reasonably balanced), Insider newsletters/Conferences/Jobs/careers, Global Competitive Landscape -- none newly justified. Production operations remain independently cadenced (Production Collection Operations V1, below); do not add Sources merely to address operational failures, and be mindful of marginal review cost per new source (TD-056/TD-063). Do not start Landscape migration, jobs/conferences, AI/RAG search, Trade/Weather UI, PostgreSQL, Phase 3, or another vertical/discovery mission.

**Last completed:**

Atomic Evidence Gold Set V1 (2026-08-23, branch `docs/atomic-evidence-gold-set-v1`, PR #101, merge `51e374f`): built a benchmark answering what atomic competitive-intelligence claims a good extractor should recover from a rich trusted publication, and what it should correctly refuse to extract -- distinct from and preparatory to Publication Review (Cursor's active lane) and Atomic Extraction Qualification / Model Readiness (Codex's lane), with neither implementation overlapped. **Corpus reality measured, not assumed, before selecting sources**: a full read of all 1,266 `data/evidence/*.json` trusted records found every genuinely rich (3+ claim) company-newsroom/trade-press record is blueberry except two strawberry items -- zero rich raspberry or blackberry trusted newsroom/trade-press record exists; zero non-English trusted record exists anywhere in the corpus; zero trusted record carries the structured `patent_filing`/`cpvo_filing` object (all 30+ that do are still pending drafts); zero trusted record has real transcript text or a populated `article.paragraphs` body (the one trusted `publication_artifact` podcast has `transcript.status: "not_available"`). **19 real sources selected, 17 scored**: drawn from `data/evidence/`, cross-referenced against already-existing trusted `data/facts/` records from an earlier "blueberry public pilot" batch (reused near-verbatim as strong ground truth, e.g. `fact-blugenix-launch-2026`, `fact-costa-vip-age-conflict`) rather than re-derived from scratch. **A mandatory flagship case was added at explicit user direction mid-mission**: the Planasa "varieties arise the interest of major European retailers" article, all 7 requested variety/attribute claims verified word-for-word against the real source text. **Found and correctly declined to force**: this exact article is a pending draft (`status: "draft"`), not trusted -- kept as the canonical atomicity-vs-summarization worked example but explicitly outside the scored set, per this mission's own trust-grounding rule, rather than silently scoring an unreviewed source. **4 new Technical Debt entries** (TD-077 no schema locator for paragraph-indexed text; TD-078 zero real trusted transcript text; TD-079 zero non-English trusted text; TD-080 zero trusted structured registry object), each tied to a concrete corpus finding, none manufactured. **A real process error was caught and fixed before merge**: `gh pr create` silently targeted `master` (the repo default branch) instead of canonical `v2/intelligence-os`, which GitHub's mergeability check reported as `CONFLICTING` against 1,266+ files that simply don't exist on `master` -- traced to the actual cause (wrong base ref, not a real content conflict) via `gh pr view --json baseRefName`, fixed with `gh pr edit --base`, then a close/reopen cycle to force the CI workflow to re-run against the corrected base; all 4 checks then passed in 5-8s each (Markdown-only fast path) and the merge proceeded clean. Full `pytest` (1,262 passed), `validate_records.py`, and `git diff --check` all green; zero Fact/Relationship/trusted Atomic Evidence/Source-authority/Assessment created; zero `review_state` changed anywhere. Docs-only change -- no VPS deployment required. See `docs/v2/ATOMIC-EVIDENCE-GOLD-SET-V1.md` for the full gold set.

Caneberry Review Preparation V1 (2026-08-22, branch `docs/caneberry-review-preparation-v1`, PR #100, merge `d1ffc38`): organized the caneberry pending-review backlog into an analyst-ready packet without publishing, rejecting, or changing any trust state -- "the agent organizes, the analyst decides." Inventoried the full pending backlog precisely (explicitly re-verified the known AI-enrichment negation-text tagging trap from an earlier mission did not inflate the count), then manually triaged all items by reading the full list rather than relying only on named example candidates, surfacing several previously-unexamined real finds (Planasa Newsroom's Pink Hudson/Black Sultana; Naturipe/Hortifrut's Vicentina) alongside the expected Yosemite and CPVO Ponca/Ouachita packets. Built a strict, uniform review-packet format (title/source/date/berry/company-variety-geography/why it matters/exact source claim/relevant excerpt/what it would support if trusted/existing related trusted evidence/independent corroboration/limitations/recommended analyst question) that deliberately excludes "Publish this"/"Reject this" language, using only "Ready for human review"/"Needs source verification"/"Likely duplicate"/"Access-limited." Verified zero mutation via direct filesystem/git evidence rather than merely asserting it (empty `git status` except the new doc; `inbox/review_events/` not yet existing). See `docs/v2/CANEBERRY-REVIEW-PREPARATION-V1.md`.

Caneberry Variety + Actor Expansion V1 (2026-08-22, branch `feature/caneberry-variety-actor-expansion-v1`, PR #98, merge `4022395`, deployed 2026-08-23): asked how much real blackberry/raspberry Variety-level graph depth the already-trusted Evidence base could support, after Evidence Berry Tagging Backfill V1 raised blackberry Evidence 146->167 and raspberry 191->218 without changing either catalog. **Candidate set built from a systematic search of trusted Evidence for variety/cultivar mentions (12 real hits), not just the 3 given examples** -- found 2 real blackberry varieties (Ponca, Ouachita, both University of Arkansas, both real awards/recognition) the mission brief did not name. **Trust-gate discipline correctly blocked one real candidate**: Yosemite (Planasa) is a real, verified blackberry variety, but its only grounding Evidence remains an unreviewed `inbox/evidence/` draft -- **not created**, since every existing Variety entity requires trusted evidence and this mission would not lower that bar. **Blackberry Varieties: 1 -> 5** (Victoria + Ervin/NC State, Ponca/University of Arkansas, Ouachita/University of Arkansas, BK 6-13/Plant Sciences Genetics, the latter modeled as brand-Rejoice + variety-BK-6-13 mirroring the existing BluGenix/Eterna precedent rather than force-fitting a platform into one cultivar). **Raspberry Varieties: unchanged at 12** -- the one real gap was a missing evidence link on the already-existing Malaika entity (a second, real, independent Spanish grower, Onubafruit, alongside its already-recorded Portugal relationship); no new entity was warranted. 2 new Organization entities created (NC State University, Onubafruit), each grounded in real trusted Evidence and needed for a real relationship. **CPVO impact measured, not assumed**: a bounded dry-run then real apply of the existing `scripts/monitor_cpvo_registry.py` against the expanded catalog found 2 new, real, berry-relevant CPVO Community Plant Variety Right filings (Ouachita, Ponca; correct genus `Rubus subg. Rubus`) -- structurally unreachable before this mission since CPVO's own query mechanism is seeded from tracked Variety names. Both remain untrusted drafts pending human review, never linked into trusted entities. **Patent cross-check**: the existing bounded watchlist found 15 real blackberry-patent hits, all already known, none matching the 4 new varieties -- an honest negative (Ponca/Ouachita's 2003 release predates the watchlist's own 2023 publication-date floor, a real structural reason distinct from CPVO absence). **Live UI/search verification against this worktree's own running app**: `/entities/variety?berry=berry-blackberry` renders all 5 varieties correctly; `/entities/variety/variety-malaika` correctly shows two separate `GROWER` roles (Onubafruit, The Summer Berry Company) without collapsing them; `/search?q=PSG` and `/search?q=Onubafruit` each resolve to exactly one Company result, no duplicates; Commercial Positions and Competition views load without error against the expanded graph. **One real, small code fix was needed**: `variety_workspace.py`'s `present_competition()` set `blackberry_thin = berry_id == "berry-blackberry"` unconditionally -- true regardless of actual Variety count, a real hardcoded-bias case only observable once blackberry's real count changed, which this mission was the first to do. `pytest` caught it directly (2 tests asserted "(thin)" must always appear for blackberry). Fixed to be count-driven (`<=1`, matching `berry_inventory()`'s own existing threshold); 3 tests updated to assert the new, correct, real behavior. A second, real, pre-existing data-quality finding was corrected in passing: `ev-20260806173539-9f2f-...` ("Herriot, a luscious new strawberry...") carried `berry_ids: ['berry-raspberry']`, traced to its source bucket being named "Raspberry variety license" rather than actual content -- corrected to strawberry. A second suspected mistag (`ev-abb-varieties`, tagged blueberry) was investigated and found to be intentional "negative evidence" from an earlier pilot mission (proving ABB is *not* a blueberry breeder) -- left untouched, not a bug. **No new Technical Debt was registered** -- TD-076 confirmed highest immediately before writing; this mission's own findings are reported as honest structural facts, not framed as new unresolved debt. Full `pytest`, `validate_records.py`, and `build_static.py` (no draft leakage) all green; `git diff --check` clean. **Deployed via the documented canonical-promotion runbook, not manual runtime copying**: verified backup created first (`berry-runtime-20260823T044323Z.tar.gz`); container rebuild's automatic startup sync promoted all 15 new entity/relationship files with zero manual copying; an explicit `--apply-safe-updates` run (verified-backup-gated) promoted 7 of the 9 modified-existing records. **2 of the 9 (`company-university-of-arkansas.json`, one Rejoice evidence record) did not qualify for automatic safe promotion** -- both predate the promotion mechanism's own baseline tracking and were classified `CONFLICT`, correctly and safely declined rather than force-copied, per this mission's own explicit instruction not to manually copy unless the mechanism fails (it did not fail; it worked as designed). Confirmed non-consequential to the live graph: the `develops` relationship records themselves were already promoted as new files, and Variety pages resolve breeder relationships by querying the relationships store directly, not the Company entity's own cached list -- Ponca and Ouachita correctly show University of Arkansas as breeder in production regardless. Public `/healthz` returned 200; `scripts/collection_status.py` ran cleanly. See `docs/v2/CANEBERRY-VARIETY-ACTOR-EXPANSION-V1.md` Section 15a for the full deployment proof.

Commercial Positions V2 (2026-08-22, PR #88, implementation `401ab21`, merge `2d0baa9`, deployed on later canonical `f9eb920`): migrated tagged published Evidence (`priority.commercial_position.level != none`) onto company-grouped V2 cards without a Position schema, competitive score, or trust-layer change. Facts, Signals, and Assessments stay labeled as themselves. Local warm `GET /queues/commercial_position` was ~1.05–1.09s / 354256 bytes; production authenticated warm was **0.197s then 0.143s / 353555 bytes** (**TD-075**, performance characteristic only). Product limitation is **TD-074** (no first-class Position object). Canonical Evidence Berry Tagging Backfill V1 owns **TD-071**, **TD-072**, and **TD-073** unchanged. Stop: do not start Landscape.

Evidence Berry Tagging Backfill V1 (2026-08-22, branch `feature/evidence-berry-tagging-backfill-v1`, PR pending): data-quality repair directly resolving TD-071/TD-072, not a new discovery/acquisition mission. **Measured the real gap precisely before touching anything**: all 574 untagged trusted `data/evidence/*.json` records (45.3% of the 1,266-record corpus) traced to one single historical bulk-seed batch (`source_type: news_search`, `captured_date: 2026-08-06`) -- not an ongoing pipeline leak; the 3 trusted records captured since that date were already 100% tagged. Classifying the 574 via the existing `deterministic_tagging.infer_berry_ids_from_text()` matcher against title+summary text only (no article-body fetch): 254 deterministically single-species, 21 genuinely multi-berry (2+ species explicitly named), 299 correctly left untagged (verified by manual sample: company/category-only mentions, off-topic scraping noise -- no missed vocabulary term). **A real, previously-undocumented bug was found and fixed before it could corrupt the backfill**: `infer_berry_ids_from_text()` used plain substring matching, not word-boundary matching -- "mora" (blackberry) false-positive-matched inside ordinary Spanish words like "morado" (purple) and "enamorado" (in love); fixed by reusing `relevance_screen.py`'s own already-proven-safe `_word_present()` pattern. `deterministic_tagging.py` had zero direct unit test coverage before this mission -- added `tests/test_deterministic_tagging.py` (8 tests). **Vocabulary reconciled**: French (`myrtille`, `fraise`, `framboise`) and Italian (`mirtillo`, `fragola`, `lampone`) species terms added for blueberry/strawberry/raspberry, matching `relevance_screen.py` exactly; blackberry's French `mûre`/Italian `more` remain deliberately excluded (real "ripe"/common-word collision risk that word-boundary matching alone does not neutralize). **Dry-run then apply** (`scripts/backfill_berry_tags.py`): 275 of 574 backfilled. A rigorous, fully-programmatic key-by-key diff across all 275 changed files confirmed **zero fields changed other than `berry_ids` and a new, additive `berry_tagging_provenance` audit object** (new optional schema field, mirroring the existing `extraction_provenance`/`transcript_provenance` convention) -- no trust/status/text mutation anywhere. Second `--apply` run produced zero additional changes (idempotent, proven). **Result**: trusted Evidence tagged rate 54.7% -> 76.4%. Blueberry 484->671 (+38.6%), Strawberry 292->367 (+25.7%), Raspberry 191->218 (+14.1%), Blackberry 146->167 (+14.4%) -- explicitly **not** claimed as an intelligence-recall improvement, since no new event was discovered and no new source ran; only species metadata on already-published, already-correct records was repaired. Blackberry/Raspberry's Variety catalog depth (12/1 tracked entities) is unchanged -- confirmed via `git status` showing zero `data/entities/` files touched, keeping Evidence depth and Variety-catalog depth honestly separate as two different facts. A second, real, latent (not currently active) risk was found while tracing tagging paths: AI-enriched draft summaries can contain negation language ("does not appear directly relevant to... blueberry, strawberry, raspberry, blackberry") that would false-positive-match all four species if a future tool naively re-ran deterministic tagging against post-enrichment text; the live production path is safe today only because tagging runs before AI enrichment overwrites the summary in the same function call -- registered as TD-073 rather than fixed, since it is not currently producing wrong data. TD-071 substantially resolved (legacy batch closed, general future-risk noted not eliminated); TD-072 resolved. Full `pytest`, `validate_records.py`, and `build_static.py` (no draft leakage) all green; `git diff --check` clean. No Source, UI, trust semantics, or Fact/Assessment/Signal changed. See `docs/v2/EVIDENCE-BERRY-TAGGING-BACKFILL-V1.md` for the full mission report.

Blackberry / Raspberry Vertical V1 (2026-08-22, branch `feature/blackberry-raspberry-vertical-v1`, PR pending): asked empirically whether Regional Live Recall Set V1's measured raspberry 0%/blackberry 4% reflected genuine system blindness or a sampling artifact from a geography-driven (not species-driven) sample. **Canonical caneberry baseline audited first**: 12 raspberry / 1 blackberry tracked Varieties (vs. 41 blueberry), 1 raspberry / **0 blackberry** breeding programs, 191 raspberry / 146 blackberry trusted Evidence, 4 raspberry / **0 blackberry** CPVO-referencing evidence, **0 Signals and 0 Assessments for both** (100% blueberry). A separate, real measurement-integrity finding surfaced while auditing: ~45% of all trusted Evidence (574/1,266 records) carries no `berry_ids`, and 27/21 of those untagged records mention raspberry/blackberry respectively in their own titles -- two concrete real instances caught by name (a real, 2-source-corroborated Onubafruit Malaika raspberry story and a real NC State blackberry variety release, both `berry_ids: []` despite being unambiguous). **Terminology audit, live-tested via real Google News queries before any change**: `caneberry` (100% precision, 13/13 real hits) and `zarzamora` (Mexico's actual term for blackberry, net-positive but with real, honestly-reported noise on a full sample) were both completely absent from platform vocabulary; `bramble fruit` was tested and rejected (0/35 real hits, wine-tasting-note collision). **Real bug found and fixed**: `screen_relevance()`'s `berry_identity` gate was missing both terms -- three real, significant blackberry stories (including a real Planasa "Yosemite" variety commercialization in Michoacan) scored 0 and were silently rejected before the fix; `app/services/relevance_screen.py` and `app/services/deterministic_tagging.py` both updated additively (`caneberry`/`caneberries` tag both raspberry and blackberry; `zarzamora`/`zarzamoras` tag blackberry only), 30 existing tests for both modules pass unchanged. **3 new sources added, each individually justified by a live test before adding** (171 -> 174): `source-news-search-caneberry-global` (English, catches company/breeder news no country query reaches -- found Fall Creek's real raspberry+blackberry portfolio expansion and Berryplant/Berrytech acquisition, NC State's and WSU's breeder news), `source-news-search-mexico-zarzamora` (Spanish), `source-news-search-chile-frambuesa` (Spanish, Chile is a real major raspberry exporter with zero prior raspberry-specific query). All three idempotent on immediate re-run (0 duplicates). **Caneberry Live Recall Set V1 (18 real events, researched before checking capture status): Raspberry 9/9 CAPTURED (100%), Blackberry 7/9 CAPTURED (78%) -- reported separately, never combined.** Both real blackberry misses (a Fall Creek acquisition story via Google-News-redirect `empty_body`; a Spain Huelva blackberry-acreage story via RSS-window-snapshot limitation) share already-known root causes (TD-059/TD-067-style), not new problems. Existing, previously-onboarded sources (`source-news-search-uk-berry-growers`, `source-news-search-morocco-berry-fr`) already carried 9 real, unread caneberry headlines before this mission looked -- the same under-mined-existing-source pattern Regional Coverage V4 established, repeated successfully. **CPVO's real structural root cause precisely identified**: `cpvo_registry.py` builds its queries from the platform's own already-tracked Variety entity names/aliases, not a blanket species search -- with only 1 tracked blackberry Variety, CPVO monitoring can only ever issue ~1 real query for blackberry regardless of true CPVO register activity, a genuine chicken-and-egg architecture finding. **No hardcoded blueberry/strawberry bias was found** in discovery, relevance-screening, patent-monitor, or CPVO code paths -- every mechanism is genuinely berry-parameterized; the measured imbalance traces to data volume and vocabulary completeness, not code-level bias. Entity gaps identified but **not acted on** per the established evidence-grounding discipline (NC State, WSU, Onubafruit, G-Berries, FruitMasters all real, real evidence now exists for several, none created this mission -- documented for a future deliberate pass instead). **Fixed benchmark regression check: unchanged, 33/50 (66%)** -- all changes were additive vocabulary/source, none narrow existing matching, confirmed via full `pytest`. 2 new Technical Debt entries (TD-071 untagged-evidence measurement gap, TD-072 zero French vocabulary in `deterministic_tagging.py`); 0 existing entries required updating. Full `pytest`, `validate_records.py`, and `build_static.py` all green; `git diff --check` clean. All 3 new sources run twice each to prove idempotence (0 duplicates on second pass); zero trust-gate changes -- every capture remains untrusted pending human publication review. See `docs/v2/CANEBERRY-LIVE-RECALL-SET-V1.md` for the full mission report.

Regional Coverage V4 -- Live Market Recall (2026-08-22, branch `feature/regional-coverage-v4`, PR #89, merge commit `5a26623`, deployed): built a second, permanent acceptance set separate from the fixed 50-event benchmark -- **Regional Live Recall Set V1** -- testing whether the OS discovers real, current, unseen competitive events across UK/Mexico/Spain/Chile/Peru/Morocco/South Africa without an analyst first naming the event, per the mission's own explicit "measure against current unknown events, not only historical benchmark headlines" framing. **No source or code changed this mission** -- every real event came from re-running sources already configured before this mission began (`news_search_rss` regional queries from Global Qualitative Coverage Expansion V1/V2, `source-freshuelva-news` and FreshFruitPortal, both pre-existing and previously under-processed, plus IBO/FreshPlaza/Fruitnet from Unknown-Event Discovery V3), directly fulfilling the mission's own "retest before adding" instruction. **Fixed benchmark: unchanged at 33/50 (66%)**, confirmed honestly, not re-optimized for. **Regional Live Recall Set V1: 23/25 core events CAPTURED (92%)** -- UK 3/4, Mexico 4/4, Spain 3/3 (Spain had zero prior fixed-benchmark coverage of any kind), Chile 4/4, Peru 4/4+1 corroboration, Morocco 2/3, South Africa 3/3 (plus a bonus Zimbabwe first-ever-China-blueberry-export capture, regional-adjacent). The two acceptance sets are reported and will always be reported separately, never combined, per the mission's own explicit instruction -- the large gap between them (92% live vs 66% historical) is itself the real finding: once an event sits in an already-configured source's *current* window, the existing two-stage relevance screen captures it at a very high rate; the fixed benchmark's lower number is a discovery/reachability limitation (RSS windows don't reach back in time, TD-067), not a relevance-screening weakness. **Real geography movement without new sources**: Chile and Peru both proved real recurrence of prior-mission source investment (4/4 and 4/4+1) with zero new sources added, directly answering the mission's "prove recurrence rather than adding more" instruction for those two geographies. Spain, previously entirely uncovered by the fixed benchmark, now has 3 real `direct` captures (2 executive-succession events -- new Freshuelva president, new Interfresa president -- plus a real storm/production-impact event) via a pre-existing Spanish-language source that had simply never been processed before. Mexico produced a real blackberry-specific capture (PSG's Rejoice licensing platform) from FreshFruitPortal's existing, under-exploited backlog. Morocco's French-language discovery, proven for backlog recovery in a prior mission, is now confirmed genuinely useful for *live* discovery too -- its first-ever non-blueberry capture (a real strawberry-export-crisis story, independently corroborated by 5 French-language publishers the same week). **Both misses share one root cause**: a Google News redirect URL resolving to no extractable article body (TD-059, previously known, reconfirmed against two new real cases -- Sainsbury's in the UK, Hortidaily's "Morocco berry giant" story). The UK miss is compounded by a second, newly-documented cause: **no major UK grocery retailer is registered as a Company entity at all** (`ls data/entities/companies/ | grep -iE "sainsbury|tesco|asda|co-op|marks"` returns empty, confirmed by reading 3 real existing entity files to verify the established evidence-grounding discipline first) -- query-provenance corroboration structurally cannot fire for the entire retailer-commercial event class as a result. New entities were deliberately **not** created to close this gap mid-mission, since doing so from a single uncorroborated headline would violate the same evidence-grounding discipline every other entity in the corpus already follows -- registered as TD-069 instead, with a concrete non-ad-hoc resolution path. **Global trade press marginal value measured directly for the first time**: IBO 10/10 window items berry-relevant (100%, small precise single-topic feed); FreshPlaza 4-8/69 items berry-relevant by direct title inspection (~6-12%, large low-precision global horticulture firehose -- 65 of 69 items are non-berry produce/logistics news); Fruitnet 0/2 this window (too sparse to judge, real structural low-volume limitation already known from Mission 3). A second real, previously-undocumented finding surfaced while measuring this: `article_rss` sources (FreshPlaza/Fruitnet/IBO) do not run Stage A relevance screening at discovery time the way `news_search_rss` sources do -- 66 of FreshPlaza's 69 currently-discovered items had never been screened at all before this mission (TD-070). None of the three trade-press sources materially reduces the need for the region-specific query sources that produced most of this mission's real recall -- complementary, not substitutes. **Berry distribution, a real and important honest finding**: across the 25 core Regional Live Recall Set events, blueberry 60% (15/25), strawberry 20% (5/25), blackberry 4% (1/25), **raspberry 0% (0/25)** -- confirming the mission's own stated worry directly: blueberry volume is masking real caneberry blindness, and no regional query mechanism built or reused this mission targets raspberry specifically (this drives the Next phase recommendation above). 2 new Technical Debt entries added (TD-069, TD-070); 0 existing entries required updating. Full `pytest`, `validate_records.py` (clean), and `build_static.py` (no draft leakage) run; `git diff --check` clean. No code or Source configuration changed, so no idempotence re-run was required by the mission's own validation instruction (which scopes that requirement to "real collection changes," and none were made) -- all real pipeline activity was re-running `discover_media.py` against sources whose own idempotence was already proven in prior missions, reconfirmed incidentally by every re-run this mission reporting 0 newly-discovered duplicates against its own prior state. See `docs/v2/REGIONAL-LIVE-RECALL-SET-V1.md` for the full mission report.

Review Outcome Instrumentation V1 (2026-08-23, branch `feature/review-outcome-instrumentation-v1`, PR #86, implementation `9bdc584`, merge/deployed canonical `4bf2cfa`): added compact append-only private events for Publication Publish/Reject, publication-triage Dismiss/Restore, Reading actions, Claim Testing Pass/Fail/Defer/Reopen, Signal Candidate and Signal-alert decisions, and recommendation-proposal decisions without conflating workflow semantics. Current states were not backfilled. Publication Save remains explicitly uncounted because its endpoint conflates edit with possible Keep intent. Rates require 30 publication decisions across two days and 30 per Source/query cohort. Validation: 1,244 tests, records clean, 1,527 static pages, explicit private-event leak scan clean. Production backup verified 9,721 `data,inbox` files. Post-deploy data (2,624), drafts (1,108), stable manifests, and analyst-state hash matched; two extra inbox files were the successful restored-timer run record/stderr. `/healthz`, login, authenticated work queue, scheduler, mounted UID-1000 event-path persistence, and automatic-throttling `false` were proven. The ledger remains honestly at zero genuine events. Warm operator status was 5.09 seconds. Full proof: `docs/v2/REVIEW-OUTCOME-INSTRUMENTATION-V1.md`. TD-064 is substantially resolved with explicit Keep/reason taxonomy limitations retained.

Unknown-Event Discovery + Query Coverage V3 (2026-08-23, branch `feature/unknown-event-discovery-v3`, PR #84, merge commit `c49c1d3`, rebased onto `b6ac87f`): Relevance Screen Boundary V1 established that of the 20 benchmark misses before it, only 1 (BM-C-04) was genuinely lost at relevance screening -- the other 19 were never discovered at all. This mission targeted discovery/source/query coverage directly, clustering the 19 into reusable mechanisms rather than 19 headline queries (Section 2's own explicit instruction). **Real, previously-undocumented finding**: `source-20260819-international-blueberry-organization` -- a real, well-matched, correctly-configured IBO trade-association feed whose own `why_it_matters` text explicitly named "import-duty policy across Chile/Peru/Morocco" -- had been onboarded 2026-08-19 and never once actually run (`last_checked_at: null`). Its first-ever real discovery run (2026-08-23) found the exact benchmark article ("Faced with steep US import duties, growers in Chile, Peru and Morocco prepare a response," 2026-08-14) among 10 real items, correctly becoming a `direct` draft -- a source can be correctly researched and configured and still contribute zero recall until something actually invokes it (TD-065). **SOURCE-FIRST additions** (per the mission's own Section 5 guidance -- prefer a clean authoritative feed over search RSS when one exists): FreshPlaza and Fruitnet, both cited as the real publisher for multiple remaining misses, had *no active discovery source at all* before this mission (only static reference entries) -- onboarded as real, live-verified `article_rss` sources (168 -> 171 total sources). **New government primary-source mechanism**: a `sec_edgar_search_json` adapter against SEC EDGAR's real, keyless full-text search API, CIK-scoped to Mission Produce (a bare cross-company "blueberry" search was noisy -- 13 of 18 real hits were unrelated companies; CIK-scoped, 32/32 hits genuinely relevant) -- found the company's own real 2026-03-12 8-K filing discussing "pre-production land development and blueberry plant cultivation in Peru" as a capital-expenditure line item, hand-verified since SEC's raw SGML filing format is not machine-extractable (TD-066; the same `TIER_UNCERTAIN` fallback Relevance Screen Boundary V1 built now also covers `always_body_check`-eligible pre-scoped sources generally, not just query-corroboration cases, plus a real title-distinctness bug found and fixed -- 27 real, distinct filings had shared one identical synthetic title before the fix). **Event-level recall: 31/50 (62%) -> 33/50 (66%)** -- BM-T-06 (Chile/Peru/Morocco duties, via IBO) and BM-C-07 (Mission Produce Peru acreage, via SEC EDGAR) moved MISSED -> CAPTURED. **The mission's own named stubborn-weak geographies all moved**: Chile 50%->75%, Morocco 33%->67%, Peru 44%->67%, entirely from BM-T-06's single capture (one event, three tagged geographies) plus BM-C-07's own Peru tag. **Real value beyond the fixed benchmark**: 30 more real Mission Produce SEC filings (untrusted, ongoing competitive intelligence); two more real, hand-confirmed benchmark events (BM-C-09, BM-G-02) whose real source is Fruitnet but are not retroactively reachable through a newly-onboarded live feed (TD-067, a structural limitation, not a defect -- a live RSS feed only exposes its own current window, all 69 real FreshPlaza items discovered this mission share one single publication date); a real cross-publisher syndication duplicate (IBO vs. Fresh Fruit Portal, same USHBC story) correctly left as two separate drafts per this project's own "never fuzzy-merge across different real publishers" dedup discipline (TD-068), not a bug. **Explicit stop cases, real-tested and deliberately not pursued**: CFIA's recall API re-tested and found to return stale 2021-dated data even from its "recent" endpoint (worse than the original TD-041 finding); DOJ's real press-release RSS has no working topic filter (`keys=`/`component=` params silently ignored, live-verified) -- the full national criminal-justice firehose was judged a bad trade for one Georgia forced-labor event; EU MRL tracking, USDA FAS GAIN's report API, World Agri-Tech's conference site, and narrow single-publisher citations (Akin Gump/Agri-Pulse, Lookout Santa Cruz) were each real-tested and found to require undiscoverable access or one-off headline-hack-scale effort -- left MISSED, explained rather than forced. **Real mid-mission concurrency handled transparently, twice**: canonical moved `838bdbf` -> `8e75b7e` (Cursor's Claim Testing / Testing Queue V2, PR #75) then `8e75b7e` -> `b6ac87f` (Codex's Review Capacity + Collection Backpressure V1, PR #81) -- rebased cleanly both times (only `PROJECT-STATUS.md` and `docs/v2/TECHNICAL-DEBT-REGISTER.md` needed manual reconciliation each time), and two real, sequential Technical Debt Register ID collisions were found and fixed: this mission's own draft entries collided first with Claim Testing's already-canonical TD-062/063, renumbered up, then collided again with Review Capacity's own concurrently-landed TD-064, renumbered a second time to their final resting place, TD-065 through TD-068. 1 existing Technical Debt entry updated (TD-041) and 4 new entries added (TD-065 through TD-068). Full `pytest`, `validate_records.py`, and `build_static.py` all green (see this mission's own doc for exact counts); `git diff --check` clean. Idempotency proven twice at both the discovery layer (all 4 new/reactivated sources: 0 newly discovered on re-run) and the draft-creation layer (both confirmed benchmark movers re-processed idempotently, 0 duplicates); 0 cross-pipeline duplicate titles against already-trusted records. No trust-boundary change. See `docs/v2/UNKNOWN-EVENT-DISCOVERY-V3.md` for the full mission report.

Review Capacity + Collection Backpressure V1 (2026-08-22, branch `feature/review-capacity-backpressure-v1`, PR #81, merge/deployed commit `018bfa1`): added a read-only operator report that keeps real recorded decisions (`OBSERVED`), queue/load calculations (`DERIVED_OPERATIONAL_METRICS`), and deterministic attention replay (`SIMULATED_POLICY_EFFECT`) explicitly separate. Production has 1,108 pending publication drafts (HIGH), median queue age 0 days, oldest 6 days, and a short observed arrival history of 40.15 drafts/run; only 1 publication carries explicit recorded review metadata, so review throughput and keep/publish/reject rates remain honestly `null`. The critical replay would surface 1,107 and defer 1 exact reprint secondary; 1,097 protected items surface, with zero unique direct/uncertain events lost. Automatic throttling remains OFF; at critical V1 continues discovery, warns, and simulates only. The deployed status command completed in 3.09 seconds and the full capacity report in 1.58 seconds. Pre/post rebuild counts matched exactly (2,624 data, 1,108 drafts, 7,088 inbox), analyst queue SHA-256 stayed `119af74fcdf74580fd0bb1134c392882cfcfde01255ee08c5fc2d86e97d8ffda`, and internal/public `/healthz` returned 200. No Sources, schedules, UI, trust semantics, or runtime drafts changed. TD-056 remains active with observability improved; TD-064 records the missing append-only review-event ledger.

Claim Testing / Testing Queue V2 (2026-08-23, branch `feature/claim-testing-queue-v2`, PR #75, rebased onto `838bdbf`): migrated the real verification workflow — tagged published Evidence (`priority.testing`) plus Pass / Fail / Defer overlay — onto the approved V2 shell without creating a first-class Claim object, Learner Mode, or a second Fact-authoring path. Queue and `/queues/testing/{id}` distinguish source claim, stored supporting/contradicting `evidence_links`, unresolved absence, reprints vs independent origins via existing `source_independence`, and agronomic scope. Static output lists tagged trusted Evidence without analyst dispositions. Claim Testing limitation is **TD-062** (canonical already owned TD-055 through TD-061). Warm `/queues/testing` is bounded published-Evidence listing (~2.9s on a full corpus load), not a quadratic scan.

Relevance Screen Boundary V1 (2026-08-23, branch `feature/relevance-screen-boundary-v1`, PR #78, merge commit `5139f82`, rebased onto `9991171`): fixed the gate between discovery and useful review, per Global Qualitative Coverage Expansion V2's own finding that the relevance-screen/metadata-thin boundary (TD-040/TD-045) is now the dominant remaining miss cause. **Traced the real pipeline rather than inferring from planning docs**: found two independently-evolved relevance mechanisms coexist -- `app/services/relevance_screen.py` (two-stage, berry-identity-gated, body-aware "v3") and the older, cruder `app/services/relevance_screening.py` (single-stage, metadata-only, no body-fetch capability). The real, previously-undocumented finding: the actual documented operator workflow (`scripts/process_discovered_media.py`, `scripts/run_recent_batch.py`, per `AGENTS.md`'s own "Operating path") exclusively used the cruder module for `web_article` items -- `article_acquisition.fetch_article()` was reachable only via `scripts/run_collection.py` (Codex's recurring pipeline) and the little-used `scripts/ingest_articles.py`, meaning every real article draft ever created through this project's own documented workflow had zero body verification, across its entire multi-mission history. **Fixed generically, four parts**: (1) both real-workflow scripts now route `web_article` items through the two-stage, body-aware screen; (2) new query-provenance corroboration (`_query_corroboration_hit`) keeps a genuine zero-signal Stage A title open for a real body check when it also names a registered Geography/Company entity (reusing the existing canonical entity-identity matcher, no second alias system) alongside a corporate-action verb -- query provenance alone never grants relevance, it only reopens Stage B, and continent-level Geography entities (Europe, North America) are deliberately excluded after a real false-positive regression (a packaging company's "acquisition in Europe"); (3) a new explicitly-labeled `TIER_UNCERTAIN` untrusted draft state for the dominant real case where the article body is structurally unverifiable (Google News redirect pages carry no server-rendered content -- live-verified, ~100% of the real `news_search_rss` backlog); (4) French species vocabulary (`myrtille(s)`/`fraise(s)`/`framboise(s)`) added to the berry-identity gate, found missing entirely from a dedicated French-language Morocco source an earlier mission had verified for *discovery* but never for *relevance screening* -- French "mûre" (blackberry) deliberately excluded, mirroring the existing Italian "more" exclusion (collision with the ordinary adjective "ripe"). **Event-level recall: 30/50 (60%) -> 31/50 (62%)** -- 1 event (BM-C-04, Unifrutti/AvoAmerica Peru, this debt's own long-cited example) moved MISSED -> CAPTURED (draft, `uncertain`), resolved via query-provenance corroboration rather than the previously-tried alternate-article approach. **Real value beyond the fixed benchmark, reported separately per this benchmark's own no-redefinition discipline**: French vocabulary alone unlocked 44 new real, review-ready drafts from a single existing source (45 of 50 real backlog items, up from a small handful), and query-provenance corroboration surfaced a real, previously entirely-missed Driscoll's/Costa Group stake-acquisition cluster (5 independent articles) plus Agroberries/BerryWorld Asia and African Blue (Morocco) coverage -- 61 new drafts total this mission (47 `direct`, 14 `uncertain`), none flooding the queue (corroboration fired on ~5% of the real zero-signal backlog, 16 of 309 measured items) and 0 confirmed false positives among them. **Real mid-mission concurrency handled transparently**: canonical moved `e1ae5d0` -> `9991171` partway through (Codex's Production Collection Operations V1, PR #76/#77) -- rebased cleanly (`scripts/run_collection.py` and `docs/v2/TECHNICAL-DEBT-REGISTER.md` auto-merged with zero real conflicts since the two missions' edits landed in disjoint regions; only this file needed manual reconciliation), and a real Technical Debt Register ID collision was found and fixed (this mission's own draft TD-055/056/057 collided with Codex's already-canonical TD-055/056/057 for unrelated production-operations topics; renumbered to TD-058/059/060/061, all cross-references fixed). 2 Technical Debt entries updated (TD-040 partially resolved, TD-045 resolved for its own cited case) and 4 new entries added (TD-058 through TD-061: corroboration's own entity-naming boundary, the structural Google News redirect limitation, the deliberately-excluded French "mûre", and the now-principled two-relevance-module split). Full `pytest` (1,208 passed, 1 pre-existing environment-dependent failure unrelated to this mission), `validate_records.py`, and `build_static.py` (1,527 pages, no draft leakage) all green; `git diff --check` clean. Idempotency proven twice (discovery re-run: 50/50 already-known, 0 new; draft-creation re-run: idempotent short-circuit, 0 duplicates); 0 cross-pipeline duplicate titles against already-trusted records. No trust-boundary change -- every new draft, `TIER_UNCERTAIN` included, remains untrusted pending human review. See `docs/v2/RELEVANCE-SCREEN-BOUNDARY-V1.md` for the full mission report.

Production Collection Operations V1 (2026-08-22, PR #76, deployed canonical `9b57f10`): fixed TD-054's effectively quadratic status recomputation (live 2.99 seconds versus >35 minutes); added one persisted registry dispatcher with `SUCCESS`/`PARTIAL`/`FAILED`; automated article/news, spoken, patent, CPVO, and bounded verified backup rotation at evidence-based cadences; retained fixed-window trade/weather as manual; exposed review backlog, backup, disk, lock, failure, and next-due state in the single operator status command. The first real scheduled cycle completed exit 0 (CPVO 136 queries/28 drafts, verified backup rotation), lock contention behaved correctly, all 6,885 pre-deploy inbox files survived byte-identically, authenticated smoke passed, and disk remained healthy with 111 GiB free. No Sources, UI, trust semantics, or database work changed. Full report: `docs/v2/PRODUCTION-COLLECTION-OPERATIONS-V1.md`.

Collection Runtime + Data Integrity V1 production proof (2026-08-22, feature PR #70, merge commit `704c18e`, production-proof follow-up branch `docs/platform-reliability-production-proof`): production was backed up and restored in isolation; the same host `data/` and `inbox/` bind mounts survived Docker rebuild; all 6,179 inbox files were byte-identical; lock-directory writes passed as the deployed UID 1000 user; authenticated HTTPS smoke passed. The VPS has 111 GiB available, so the development-host disk warning does not apply. Actual systemd inspection found `bios-collection.timer` installed/enabled for article/spoken collection only, with recent partial-Source failures; the four manual quantitative/registry pipelines remain unscheduled. Production proof also caught and fixed a backup-name false positive that excluded a legitimate Evidence filename containing `secret`, and reconciled runtime debt IDs to TD-047 through TD-054 without overwriting canonical TD-038 through TD-046. TD-051 remains active because off-host rotation/archive policy is unresolved; TD-054 records that production `collection_status.py --json` did not finish within a 35-minute attached ceiling. Full proof: `docs/v2/COLLECTION-RUNTIME-DATA-INTEGRITY.md`.

Global Qualitative Coverage Expansion V2 (2026-08-22, branch `feature/global-qualitative-coverage-v2`, PR #72, merge commit `41296c4`, rebased onto `704c18e`): a second, real round of qualitative discovery expansion, building directly on Round 1 rather than repeating it -- the mission brief's own "why" framing (22%->30%, 39/50 missed) described the pre-Round-1 state; this mission re-confirmed the true current baseline (26/50 = 52%, Round 1's real result) mechanically before doing any new work. **Event-level recall: 26/50 (52%) -> 30/50 (60%)**, same event definitions, no headline hardcoding, no manual insertion. 4 new sources (`data/configuration/sources.json`, 164 -> 168): a new `government_alert_json` adapter against the UK Food Standards Agency's real, keyless `data.food.gov.uk/food-alerts` API (extending the "one adapter per JSON publication shape" precedent a third time, after Federal Register and openFDA), plus 3 generic geography/event-concept `news_search_rss` sources (Peru organic-investment, UK grower-season, USDA GAIN-report). **Real, generic query reformulation proven twice more**: a fully generic `"Peru organic blueberry expands OR invests"` query found the exact benchmark-cited NuBerry Farms investment article without ever searching "NuBerry"; a generic `"UK blueberry OR strawberry grower launches OR kicks off season"` query found Hall Hunter's season-launch article without searching "Hall Hunter" -- both real, live-verified, non-hardcoded discoveries. 4 real captures total: NuBerry Peru investment (BM-C-08), Hall Hunter UK season launch (BM-C-13), a real Peru $400k investment story found already sitting unprocessed in Round 1's own backlog (BM-M-03), and a rescued alternate-article capture of the Michigan blueberry-farm labor-trafficking settlement (BM-R-10, resolving one of TD-040's own two originally-cited cases). By geography: **Peru 10% -> 30%** and **UK 33% -> 67%** were the largest movements; Chile (20%), Morocco (33%), and South Africa (100%, already saturated on this benchmark's sample) honestly reported unchanged -- no US-only artifact, since 3 of the 4 real captures are non-US. Found, quantified, and cleaned up a real cross-pipeline duplicate-draft pattern that scales with processing volume (57 duplicates this round vs. 16 in Round 1, out of a much larger ~600+ item batch) -- while this mission's own manual cleanup was in progress, Codex's concurrent Collection Runtime + Data Integrity V1 mission (PR #70, `cd107b9`) independently landed a real fix for the identical gap (`_publisher_identity()` in `article_dedup.find_duplicate_article()`), credited and cross-referenced rather than duplicated (TD-046, status `resolved (by Codex, concurrently, PR #70)`). **Real mid-mission concurrency handled transparently**: canonical moved `35d8066` -> `704c18e` partway through (Cursor's Global Intelligence Search V1, PR #65, plus Codex's PR #70) -- this branch was rebased cleanly onto the new canonical (one real, resolved conflict in `TECHNICAL-DEBT-REGISTER.md`), Codex's `article_dedup.py` change was confirmed purely additive against this mission's own Round 1 `normalize_canonical_url()` fix, and a real Technical Debt Register ID collision was found and fixed (this mission's own draft TD-043/TD-044 entries collided with Cursor's already-canonical TD-043/TD-044 for unrelated Search-lane topics; renumbered to TD-045/TD-046, all cross-references fixed). Codex's new collection lock / pipeline registry (`data/configuration/collection_pipelines.json`) was investigated post-rebase and confirmed to cover only `scripts/run_collection.py` and the `monitor_*.py` recurring runners, not the `discover_media.py`/`run_recent_batch.py`/`process_discovered_media.py` scripts this mission used throughout -- no lock integration needed. 2 new Technical Debt entries (TD-045, TD-046, both described above). Full `pytest` (1,194 passed, 1 pre-existing environment-dependent failure unrelated to this mission's changes -- `test_variety_workspace.py::test_observations_runtime_without_inbox_is_honest`, already failing before this mission's rebase), `validate_records.py`, and `build_static.py` (1,527 pages, no draft leakage) all green on the rebased branch; the two hardcoded source-count tests and `collector-templates.json` updated for the 4 new sources (164 -> 168). UK FSA adapter idempotency proven with a real, live, run-twice test (100 items found both times, 0 newly discovered, 0 duplicates). Deployed to the VPS (fast-forward `704c18e` -> `41296c4`, Docker rebuild, all 4 new sources confirmed synced into the live runtime by the container's own startup log, `/healthz` 200) and verified live. Full mission report: `docs/v2/GLOBAL-QUALITATIVE-COVERAGE-EXPANSION-V2.md`.

V2 Global Intelligence Search V1 (2026-08-22, branch `cursor/global-intelligence-search-bd27`, PR #65, merge commit `912a100`, rebased onto `35d8066`): analyst navigation overlay + `/search` so a company, variety, geography, source, or named subject can be found without knowing which product screen holds it. Deterministic only — exact canonical/alias, title, linked entities, then weaker text; no embeddings/RAG. Results are grouped (Companies, Varieties, Geographies, Sources, Intelligence, Story Threads, Signals, Assessments) with Trusted / Pending / Story / Signal / Assessment labels preserved. Berry context prefers in-scope hits and keeps the rest under Also in Global. Live search may include inbox drafts and signal candidates; static/Pagefind remains published-only. Search does not call Morning Brief, `variety_footprint`, or full Story Thread recomputation. Canonical already owned TD-038 through TD-042 (qualitative coverage); Search limitations are recorded as TD-043 (cheap pending Story Thread clusters) and TD-044 (process-local search cache). Stop gate: do not begin AI/RAG search or Landscape.

Learner Mode roadmap integration (2026-08-22, branch `docs/learner-mode-roadmap-integration`, PR #68, merge commit `61f706d`) — **documentation/governance only; no implementation, schema, source, or UI work was done.** The externally-authored Learner Mode feature request (deep agronomy, production-process, harvest-technology, and taste/consumer-science knowledge across all four berries, connected to but semantically distinct from Competitive Intelligence) is now formalized in-repo at `docs/v2/feature-requests/LEARNER-MODE.md`, preserved verbatim with its full citation set. **Workstream K** was added to `docs/v2/INTELLIGENCE-EXPANSION-BUILD-GUIDE.md` (the source request named it "Workstream I"; that letter was already assigned to UI V2 in canonical, so K — the next free letter — was used instead, to avoid renumbering an established reference) covering all five pillars (Plant Biology & Agronomy; Pest/Disease & Cross-Cutting Process; Harvest Technology & AgTech; Taste & Consumer Science as its own explicit pillar, not folded into a generic trait section; Visual Content Sourcing, with real source/license/attribution requirements, not treated as decorative), the four knowledge classes (foundational / regional production practice / current technical guidance / consumer-sensory observations, each with distinct freshness semantics), the durable core rules (reuse existing Berry/Variety/Geography/Source/Evidence objects; no duplicate Berry/Variety records; Learner content is not automatically Competitive Evidence or a Signal and must not automatically increase Signal confidence; provenance/regional-non-universalization/licensing rules), an explicit roadmap-boundary discussion (Learner Mode vs. Variety Intelligence vs. Weather Intelligence, plus trait architecture, Commercial Observations, source monitoring, and Landscape), and the roadmap-only Phase K.1–K.5 build sequence (architecture/source audit → Blueberry+Raspberry knowledge pilot → glossary/"Explain this" integration → Strawberry+Blackberry expansion → ongoing emerging-technology/sensory-research monitoring) — explicitly not authorized for execution. `docs/v2/INTELLIGENCE-COVERAGE-MATRIX.md` gained a Learner Mode capability-maturity section: every cell `NONE` across all 4 berries, explicitly not credited merely because source URLs appear in the requirements document. `AGENTS.md` gained a concise, durable pointer for future agents working on agronomy/IPM/harvest-agtech/sensory/visual-learning content. `docs/v2/TECHNICAL-DEBT-REGISTER.md` is unchanged — no concrete existing architectural conflict was found, and adding speculative debt for a not-yet-built feature was explicitly out of this mission's scope. Validation: `git diff --check` clean, no merge-conflict markers, no secrets, every new/cross-referenced doc path verified to point to a real file, diff reviewed line-by-line (only one deliberate line changed outside pure additions: "ten workstreams" → "eleven workstreams") — no accidental removal of Cursor's or any prior mission's work. No runtime deployment (docs-only). **Current active priorities are unchanged** — see Next phase below; Learner Mode is a formalized future workstream, not started.

Global Qualitative Coverage Expansion V1 (2026-08-21, branch `feature/global-qualitative-coverage-v1`, PR #66, merge commit `c75c1cf`, rebased onto `b165344`): first qualitative discovery expansion, targeting the 50-event recall benchmark's dominant unresolved cause (SOURCE NOT MONITORED, 27 of 39) after Trade and Weather closed the quantitative-context gap. **Baseline re-confirmed, not re-derived**: no qualitative source ran between the Recall Benchmark V1 mission and this one, so the prior "after" state (15/50 = 30%) was mechanically unchanged, confirmed via file-timestamp evidence. **14 new bounded, reusable-query Sources** (150 -> 164): 13 Google News `news_search_rss` searches (Spanish-language Peru/Chile/Mexico, French-language Morocco, an English UK-growers query, a UK major-supermarket retailer-class query, South Africa production + trade queries, a Chile-Morocco trade-access query, two topic-only investment/acquisition queries, one labor/legal-risk topic query, a Spanish Spain query) and 1 new `government_recall_json` adapter (`app/services/media_discovery.py`) against openFDA's real, keyless food-enforcement/recall API -- chosen as reusable QUERY PATTERNS (geography+berry, risk-concept, retailer-class), not one Source per benchmark URL, per the mission's own explicit instruction not to overfit specific companies. **Real, generic acceptance-case proof for all 8 required categories** (no headline hardcoding): Chile+Morocco (one query, "Chile and Morocco strengthen ties..."), UK (a 4-retailer query found the exact real "Sainsbury's GBP1 British Strawberry offer"), South Africa (production + market-access queries found two exact real events), food-safety (openFDA found a real E. coli O145:H28 and a real Listeria frozen-blueberry recall generically), acquisition/investment (a topic-only query found "Colombia is seeking foreign investment..."), regulatory/trade-response (4 real events, including a Spanish-language Mexico query finding the exact 3.37-5.28% antidumping margin). **Language coverage explicitly tested, not assumed**: Spanish (Peru/Chile/Mexico) and French (Morocco) discovery live-verified to reach real local-language press (Agraria.pe, El Peruano, Redagricola, H24info, AgriMaroc) no English query could reach. **Two real self-caught corrections, both handled in place**: (1) a load-bearing bug in `article_dedup.normalize_canonical_url()` stripped every URL's query string, which would have silently collapsed every distinct openFDA recall (identical path, different `?search=recall_number:...` query) onto the first one processed -- reproduced live with the real E. coli and Listeria recalls, fixed and regression-tested. (2) `build_static.py`'s own leak self-check surfaced 16 of the new drafts (including the mission's own Peru acceptance-case match, BM-M-04) as exact-title duplicates of already-**trusted, published** records -- the same cross-pipeline-duplicate gap the PR #14 mission already documented (same real article, re-discovered under a different source registration); removed as untracked-inbox cleanup per that exact precedent, and honestly excluded from the recall count rather than double-counted -- Peru's own recall is reported unchanged at its 10% baseline this mission, not rounded up. **Event-level recall: 15/50 (30%) -> 26/50 (52%)**. By class: Commercial/Market 0%->37.5% (3/8), Regulatory/Trade 9%->45% (5/11), Reputation/Risk 33%->58% (7/12), Corporate 38%->46% (6/13), Genetics/Varieties unchanged at 83%. By geography: Chile/UK/Morocco all moved 0%->20-33%, South Africa 0%->100% (2/2), Mexico 36%->55%; Peru honestly stayed at 10% (mechanism proven, but the one real match was the duplicate above). **Real inbox-quality accounting**: 1078 items discovered, 255 processed within this mission's bounded run, 180 passed relevance screening (164 net after the 16-item duplicate cleanup), 53 correctly screened irrelevant (20.8%, in line with the established 26% baseline), 22 borderline; a random 25-item manual sample found zero noise. 5 new Technical Debt entries (TD-038 through TD-042): the dedup bug (status `fixed`), Google News RSS non-determinism, the relevance-screen metadata-thin boundary (flagged for Codex coordination, since it touches the recurring-collection pipeline), the CFIA access gap, and the deliberate one-Source-per-query architectural choice (no dynamic runtime query generator built, per the mission's own collector-infrastructure boundary). Coverage Matrix and the Recall Benchmark doc both updated with the corrected numbers. Full `pytest` (1160 passed on the rebased branch; 2 known environment-dependent failures unrelated to this change -- the pre-existing Windows-path one, TD-TEST-001, and a brand-new test in Cursor's concurrent Variety UI PR #60 that assumes an empty local `inbox/`, which this shared dev machine has not had since the earlier Variety Backbone V1 mission), `validate_records.py`, and `build_static.py` (1527 pages, no draft leakage, verified with the real unpiped exit code) all green; the two hardcoded source-count tests and `collector-templates.json` updated for the 14 new sources (150 -> 164). Rebased onto Cursor's concurrent Variety Intelligence UI V1 (PR #60, `b165344`) with zero conflicts; no UI changes, no collector-infrastructure refactor beyond the two narrowly-scoped, self-caused bug fixes above. Deployed to the VPS (fast-forward `b165344` -> `c75c1cf`, Docker rebuild, all 14 new sources confirmed synced into the live runtime by the container's own startup log, `/healthz` 200, public health check 302) and verified live.

V2 Variety Intelligence UI V1 (2026-08-21, branch `cursor/variety-intelligence-ui-bd27`, rebased onto `c83b3ff`): first analyst-facing Variety product on the approved AppShell. Not a cultivar catalog. `/entities/variety` is a dense intelligence index (berry / company / geography / rights / commercial-observation filters) with competition (`?view=compete`) and commercial-observation (`?view=observations`) surfaces. Detail keeps breeder / owner / rights holder / licensee / marketer distinct, renders USPTO/CPVO/other filings, evidence-backed traits with claim labels, commercial footprint from `variety_footprint` (once per detail, never per index card), and recent intelligence via shared `ReaderOffcanvas`. UK 18-observation pilot is rendered honestly: runtime may have 0 loaded drafts (gitignored `inbox/`); only Zara and Victoria are the named-variety cases; unnamed listings are first-class. Blackberry thinness is visible. No competitive score. Landscape is linked, not redesigned. Trade / Weather backend from PRs #61/#63 (`trade_observation`, `weather_observation`, Comtrade, NASA POWER) was preserved through rebase and not modified. Stop gate: do not migrate Landscape or admin.

Weather / Climate Context V1 (2026-08-21, branch `feature/weather-climate-context-v1`, PR #63, merge commit `f6a1780`): first quantitative weather-context layer, built directly in response to Trade Intelligence V1's own real, unexplained physical-market movements (Chile/South Africa blueberry export swings, Mexico strawberry decline). **Source audit** (live-tested): NASA POWER's keyless public daily point API (`power.larc.nasa.gov/api/temporal/daily/point`) is the only source that is global, requires zero credentials this session could self-provision, and answered every required test case -- NOAA Climate Data Online and ERA5/Copernicus CDS both require a self-registered account/token, the same access-barrier pattern as US Census in Trade Intelligence V1. Unlike Comtrade's one-period-per-request cap, POWER accepts an arbitrary contiguous date range per request (live-confirmed: a 10-year range returned in ~1.2s), making real acquisition far cheaper. **Observation model**: additive `weather_observation` object on `evidence.schema.json` mirroring `trade_observation` -- one draft per production-region window, a compact daily series plus a compact climatological baseline-by-month summary (never the raw multi-year baseline series). **Production-region mapping** (`data/configuration/weather_production_regions.json`, no invented farm coordinates): 9 named, publicly-documented regions (Maule/Chile, La Libertad/Peru, Western Cape/South Africa, Gharb-Loukkos/Morocco, Michoacan-Guanajuato/Mexico, plus California/Pacific Northwest/UK/Spain documented for completeness), 5 piloted with real live calls matching Trade Intelligence V1's own real lanes; a real second run proved full idempotency (5/5 duplicates, 0 new). **Real corroboration findings**: Chile's real -42.0%/-76.1% Feb/Mar YoY blueberry-export decline overlaps a real 3-day extreme-heat run (+7.18C, Dec 2025) 8-13 weeks earlier plus March precipitation at ~363% of baseline -- one real, proposed-only `evidence_links` `corroborates` entry was added; South Africa's real -84.4% Feb decline has **no matching weather anomaly in its own window**, honestly reported as unexplained rather than forced; the Mexico regulatory control case (already explained by the real antidumping Determination) correctly shows **no competing weather explanation**, proving the system distinguishes regulatory from weather context rather than attributing every movement to the first plausible story; Peru's real growth overlaps the same precipitation-excess condition type found in Chile's decline -- an honest complexity, not smoothed into a universal rule. **Leading-indicator proof**: Chile's December heat anomaly provided a real, honestly-computed 59-90 day lead time ahead of the Feb/Mar trade periods (a calendar calculation, not a forecast). **Interpretation-risk finding, self-identified**: the generic bidirectional `unusual_temperature_window()` check has low specificity at default thresholds (flagged all 7 real test windows including a growth case and the regulatory control) -- registered as TD-035, deliberately not featured as evidence in this mission's own findings. 8 new Technical Debt entries (TD-030 through TD-037): spatial granularity (~50km grid), production-region centroid uncertainty, baseline-period selection, NASA POWER's own model heterogeneity (MERRA2/GEOSIT reprocessing), near-real-time data latency with no backfill, the `unusual_temperature_window` specificity gap, the NOAA/ERA5 credential gap, and config-only (non-entity) production-region mapping. Coverage Matrix Weather updated NONE -> NONE -> PILOT, not marked OPERATIONAL. Full `pytest` (1140 passed, 1 pre-existing unrelated failure -- Windows path, TD-TEST-001), `validate_records.py`, and `build_static.py` (1527 pages, no draft leakage) all green; the two hardcoded source-count tests and `collector-templates.json` updated for the new source (149 -> 150). Variety UI/Landscape untouched throughout. Deployed to the VPS (fast-forward `fef9b31` -> `f6a1780`, Docker rebuild, the new source + weather config confirmed synced into the live runtime by the container's own startup log, `/healthz` 200, public health check 302) and verified live.

Global Trade / Customs Intelligence V1 (2026-08-21, branch `feature/trade-intelligence-v1`, PR #61, merge commit `fef9b31`, rebased onto `cd94fea`): first quantitative trade-flow layer, per the recall benchmark's own finding that Commercial/Market (0/8) and Regulatory/Trade (1/11) are the platform's weakest classes. **Source audit** (live-tested, not documentation review): UN Comtrade's keyless public preview API (`comtradeapi.un.org/public/v1/preview/C/{freq}/HS`) is the only source that is simultaneously official, requires zero credentials this session could self-provision, and was proven live against every required country-pair test case -- US Census requires a registered API key this session could not self-provision (live-confirmed `"Missing Key"` error, a real pivot point, not an assumption); Eurostat/UK HMRC were audited and found redundant with Comtrade's own UK/EU-member reporting for this mission's real test cases; Agronometrics deliberately excluded from raw figures per the mission's own instruction (secondary/commercial only). **HS taxonomy** (`data/configuration/trade_hs_taxonomy.json`, HS 2022 6-digit): strawberry is cleanly separable fresh/frozen (081010/081110); raspberry+blackberry are never separable at 6 digits (081020/081120, `berry_code_purity: multi_berry_combined`); blueberry shares its fresh code with cranberries (081040) and its frozen code is a generic "other fruit" basket (081190, worst case) -- all carried onto every draft, never silently combined. **Quantitative model**: additive `trade_observation` object on `evidence.schema.json`, mirroring the `patent_filing`/`commercial_observation` precedent -- one Evidence draft per (reporter, partner, flow, HS code) lane holding a `series[]` array of period entries, not one draft per monthly data point. **Real pilot**: 6 lanes (`data/configuration/trade_pilot_lanes.json`, US<-Mexico strawberry, US<-Peru/Chile blueberry, UK<-Morocco/South Africa blueberry, US<-Mexico caneberry), 2025-01..2026-06, all 6 produced real data (11-12 of 12 periods for 3 lanes; 7-10 of 12 for the other 3 after real, live-observed HTTP 429s on the undocumented Comtrade rate limit, reported as honest gaps not backfilled); a real second run proved full idempotency (6/6 duplicates, 0 new). Morocco/South Africa's own export statistics don't disaggregate by partner (`partnerCode: 0` world-aggregate only, live-confirmed) -- both lanes built on the UK-mirror side (the importer's own reported data) instead, the only side this mission found real bilateral granularity in. **Real derived findings** (`year_over_year_change`/`rolling_seasonal_comparison`/`unusual_movement`/`partner_flow_changes`, pure functions, nothing auto-promoted to a Signal): Mexico strawberry imports fell 36.4%/56.2% qty/value YoY in 2026-04, about a month after the real Federal Register antidumping Determination (2026-03-12) -- connected via one real, non-forced `evidence_links` `follows_up` entry at `status: proposed` only, a human reviewer decides whether to accept it; Chile blueberry exports fell 76.1% in March YoY while Peru grew 33.1% over the same window, directionally consistent with but not identical to the existing BM-M-02 benchmark event; South Africa/Morocco UK-bound volumes showed real, large swings at genuinely small absolute scale (thousands of kg, not millions). **Commercial/Market benchmark relation**: of the 8 benchmark events, this layer would have helped detect 1 directly (BM-M-02) and helped corroborate 4 more (production forecasts/tariff-shift stories) once they occur as real volume changes; it explicitly cannot and does not solve retail promotions, company-level production/acquisition news, or event announcements (BM-M-01/07/08), exactly as anticipated. 6 new Technical Debt entries (TD-024 through TD-029): HS ambiguity, the US Census key gap, the undocumented rate limit, no revision/resubmission handling, a 7-country geography lookup, and CIF/FOB value basis not yet asserted in derived metrics. Coverage Matrix Customs/Trade updated NONE -> NONE -> PILOT, not marked OPERATIONAL. Full `pytest` (1126 passed, 1 pre-existing unrelated failure -- Windows path, TD-TEST-001), `validate_records.py`, and `build_static.py` (1527 pages, no draft leakage) all green; the two hardcoded source-count tests and `collector-templates.json` updated for the new source (148 -> 149). Rebased onto Cursor's concurrent PR #57 (Monitor workspace, `cd94fea`) with zero conflicts; Variety UI/templates untouched throughout. Deployed to the VPS (fast-forward `cd94fea` -> `fef9b31`, Docker rebuild, the new source + trade config confirmed synced into the live runtime by the container's own startup log, `/healthz` 200, public health check 302) and verified live.

V2 Monitor workspace (2026-08-21, branch `cursor/v2-monitor-workspace-bd27`, rebased onto `43c8d0c`): migrated Watches, Alerts, and Source Health onto the approved AppShell. Watches remain inventory (existing Evidence `priority.monitoring` + analyst-queue overlay; companies/varieties/geographies/people only). Alerts remain action derived from proposed Signals, watch-matched candidates, new watch activity, and failing/blocked collection — no new store. Source Health is operational collection state (healthy / quiet / due / stale / failing / blocked / not configured for discovery), using generic `entity_types` metadata so mainstream_press/trade_press and government_regulatory records fit without hard-coded source ids. Watch recent intelligence reuses `ReaderOffcanvas`. Open candidates deep-link to Signal Review. `/queues/monitoring` does not rank Morning Brief, group Story Threads, or scan discovered items. Coverage Matrix stays the recall record; source volume is not presented as recall. Stop gate: Landscape and Variety UI untouched. Variety backend from PR #58 (`cpvo_registry`, `variety_footprint`, `commercial_observation`, `markets` relationship) was preserved through rebase and not modified.

Global Variety Intelligence Backbone V1 (2026-08-21, branch `feature/variety-intelligence-backbone`, PR #58, merge commit `a25383e`): built the backbone connecting Genetics -> Variety Identity -> Rights/Ownership -> Geography -> Commercial Presence -> Market Observations -> Competitive Signal, per the prior mission's own finding that Genetics/Varieties (83% recall) is the platform's clear strength. **Identity contract proven with real data, no schema change needed**: 132 real queries against all 58 tracked varieties' names/aliases against CPVO's public EU register produced 28 real matches and zero duplicate Variety entities -- the existing `patent_monitor.entity_link` matcher, already generic, was reused unchanged rather than rebuilt for varieties. **Ownership/rights model proven to keep roles separate, not collapsed**: `variety-drisblueseventeen` now has both a real `develops` (breeder) and a real `owns` (rights holder) relationship to Driscoll's, same underlying PBR-grant evidence, two distinct evidenced facts. Added `markets` to `relationship.schema.json`'s enum after finding a real, already-disclaimed predicate substitution in live data (`rel-berryworld-sells-eureka.json`) -- fixed in place, not duplicated; the other 5 declared-but-unenforced extension predicates were deliberately not added speculatively.

**Global registry audit** (US/UPOV/CPVO/UK/Australia, live-tested, not documentation review): only CPVO's public register (`online.plantvarieties.eu`, distinct from CPVO's own account-gated "Variety Finder" aggregator) is anonymously public with a real, working JSON API -- UPOV PLUTO requires a WIPO account, IP Australia's PBR search has a real public UI but no discoverable API (confirmed via live browser network-traffic inspection), UK's PVRO has no unified public database at all. **Integrated CPVO only** (`app/services/cpvo_registry.py` + `scripts/monitor_cpvo_registry.py`, architecturally parallel to Patent Monitor v2): 132 real queries found 28 real EU filings across blueberry/strawberry/raspberry (zero blackberry, an honest finding), all untrusted `inbox/evidence/` drafts, a real second run proved full idempotency (28/28 duplicates, 0 new). Real corroboration surfaced for the existing Allberry B.V. vs. Advanced Berry Breeding B.V. identity question (TD-ENT-002): 3 more real raspberry varieties (Shani, Rafiki, Sarafina) independently show the same "Allberry B.V." applicant.

**Commercial Observation model**: one small additive `commercial_observation` object on `evidence.schema.json`, mirroring the existing `patent_filing` precedent exactly -- no new entity type, no parallel trust system (reuses `source_authority`/`verification_state`/`does_not_prove`/`review_state`). **UK retail research** (Tesco, Sainsbury's, Waitrose, M&S, Morrisons, Asda) found variety-name exposure is genuinely rare -- generic own-label listings almost never name a cultivar; only premium/named lines do. Real, individually-verified pilot: **18 real observations** (via Open Food Facts, a public open-licensed product database mirroring real retailer listings -- not direct retailer scraping, which stayed explicitly out of scope), 16 of 18 correctly recorded `variety_entity_id: null` (honest, not padded), 2 real variety-identified cases (Driscoll's **Zara** strawberries -- the Guardian's "best overall supermarket strawberry" in a 10-retailer taste test; Driscoll's **Victoria** blackberries -- a HortWeek "Best New Variety" award winner grown in Kent by Berry Gardens) added as real Variety entities with real grounding trade-press Evidence, plus 6 new real UK Retailer entities and a new `geography-united-kingdom` entity (a real, previously-unnoticed gap: the platform's own first retail-observation pilot market had no Geography entity at all).

**Variety footprint + competitive query proof** (`app/services/variety_footprint.py`, pure functions, nothing persisted): real end-to-end chain proven -- `variety_footprint("variety-drisblueseventeen")` returns separate `breeder`/`owner_rights_holder` buckets, both Driscoll's, both real; `variety_footprint("variety-zara")` returns real UK observation/retailer/marketer data. Real competitive-query results against the live dataset: `competing_varieties_in_berry_market("berry-blueberry")` returns 6 real companies (Costa Group, Driscoll's, Fall Creek, Mountain Blue Orchards, Berry Blue LLC, BerryWorld) with 20+ real varieties; the same query for blackberry correctly returns just Driscoll's/Victoria, matching the mission's own honest "blackberry stays shallow" framing. `varieties_with_ip_activity_and_commercial_observation()` correctly returns empty -- the 28 CPVO-matched varieties and the 2 variety-identified UK observations don't yet overlap, reported as a real current-state gap (TD-023), not hidden.

Real identity/dedup findings documented in `docs/v2/VARIETY-INTELLIGENCE-BACKBONE.md` Part 13 rather than only in this summary: CPVO's own register shows the denomination "Sonata" registered three times for three unrelated species (cardoon/strawberry/barley) -- proof species-filtering is load-bearing, not decoration; "Malaika" (the tracked raspberry) correctly returns zero CPVO hits (a real negative, not a broken query); "Cargo" and "Blue Ribbon" (Fall Creek blueberry varieties) each have two genuinely distinct real CPVO filings, correctly kept separate by application-number keying rather than merged.

Technical debt updated in place: TD-018 through TD-023 and TD-ENT-004 added (trait-evidence linkage not schema-enforced; CPVO's `species`/`specieId` query params don't filter server-side, discovered live; no confirmed CPVO permalink; Open Food Facts' search endpoint returned HTTP 503 throughout this mission's research window while individual-product lookups worked reliably; UK variety-name exposure is structurally rare; registry-matched and observed varieties are currently disjoint sets; a real but correct Fall Creek breeding-program-vs-parent-company "duplicate-looking" pair). TD-ENT-001 and TD-ENT-002 updated with this mission's new real corroborating evidence, not duplicated. Coverage Matrix updated with real counts for Variety Knowledge, PVR/Registry, and Retail/Commercial Observations -- **none marked OPERATIONAL**, all PARTIAL/PILOT per measured real coverage, matching the mission's own explicit instruction not to promote a small pilot. A Cursor UI contract for a future Variety Competition page is documented (`docs/v2/VARIETY-INTELLIGENCE-BACKBONE.md` Part 17) -- no UI was designed or touched.

Full `pytest` (1110 passed, 1 pre-existing unrelated failure -- Windows path, unmodified canonical), `validate_records.py`, and `build_static.py` (1527 pages, no draft leakage) all green. Deployed to the VPS (fast-forward `e1550e1` -> `a25383e`, Docker rebuild, all 15 new trusted data files + 1 new source confirmed synced into the live runtime by the container's own startup log, `/healthz` 200, public health check 302) and verified live.

Mainstream News + Regulatory Coverage Recall Benchmark V1 (2026-08-21, branch `feature/mainstream-regulatory-recall-benchmark`, PR #55, merge commit `e1550e1`, rebased directly onto `6a3e7eb`): measured real intelligence recall against a 50-event benchmark (corporate/reputation/regulatory/genetics/commercial classes, all 4 berries), built from live external research, not selected because the OS already contained the events. A naive keyword-match pass against `data/evidence/` overstated recall by ~3x (58-60% vs. the real, manually-verified 22%) -- reported as a finding about measurement discipline, not used as a result. **Overall event-level recall: 11/50 (22%) before this mission -> 15/50 (30%) after**, entirely as untrusted `inbox/evidence/` drafts pending human review -- the publication-review trust gate was not touched or bypassed. Required acceptance cases (Driscoll's NYT feature, Driscoll's negative/reputational press, the Mexico strawberry antidumping proceeding): 0 of 10 fully captured before this mission (only 2 had any indirect signal) -> 6 of 10 with real discovered signal after (the NYT feature "Why Are Berries Everywhere, in Every Season? Driscoll's." by Julia Moskin; the PFAS class-action and greenwashing lawsuits; the Mamavation pesticide-testing report; and 4 real Federal Register documents covering the antidumping case's actual procedural timeline -- Institution, Extension, Initiation of the LTFV Investigation, and a Determination). All of it metadata/title/date/URL-only, per this project's paywall discipline -- no paywalled or bot-walled page was fetched in full anywhere in this mission (verified live against both nytimes.com, refused outright by tooling, and federalregister.gov's own document pages, which return a bot-wall to a standard fetch even though its JSON search API does not).

Smallest maintainable discovery layer, every piece proven against real network traffic, not simulated: a new `government_register_json` adapter (Federal Register's public `documents.json` API; its RSS endpoint returns a bot-wall, live-verified, so JSON was used instead); a new `news_search_rss` adapter (Google News search ported into the modern discover -> screen -> acquire -> draft pipeline, with real publisher-attribution recovery from Google's own `<source>` tag, mirroring the existing older-pipeline precedent exactly); an `always_body_check` override so a pre-scoped regulatory source's docket-only headline isn't dropped by the generic Stage A relevance gate before ever fetching a body; and a metadata-only fallback for genuine access-limited categories (`blocked`/`paywall`/`empty_body`) that was deliberately scoped to exclude transient network failures (`timeout`/`transport_error`/etc.) after a real regression test caught an overly-broad first version conflating the two. 5 new Sources registered (2 Federal Register searches, 3 Google News searches: Driscoll's, Costa Group -- proving the mechanism generalizes to a second company, and a generic berry-trade-remedy topic query). Real collection runs found 248 items across the 5 sources; 75 became real review-ready drafts after 9 confirmed cross-pipeline duplicates (same real article as an already-trusted, pre-`evidence_role` record) were found and removed, 50 were correctly screened irrelevant, and 44 stayed unconfirmed (access-limited or transient failures) -- a real, honest inbox-quality accounting, not an estimate.

Real findings registered as technical debt rather than silently absorbed or claimed solved: the 9 duplicates traced to an exact, previously-undiagnosed line (`MediaOrchestrationService._cross_pipeline_duplicates()` filters its candidate pool to `evidence_role == "publication_artifact"`, silently excluding older pre-`evidence_role` trusted records even from an exact canonical-URL match) -- added as a precise root-cause addendum to Cursor's existing TD-006, not a duplicate entry. Story Thread's 7-day date-proximity window (from PR #51's `_cross_subject_event_edge()`) cannot span a real regulatory case's multi-month procedural timeline -- the 5 real antidumping documents this mission found stay 5 separate single-member threads, not one developing story, reported honestly as unsolved (TD-THREAD-003) rather than claimed as the Section 6 "developing story" requirement being met, since it is not. One real relevance-screen false positive ("BlackBerry Bold 9780" phone, from the one topic-scoped rather than company/case-scoped query) is TD-015. By contrast, the Driscoll's PFAS/greenwashing reputational cluster (11 real articles, 2026-05-12 to 2026-07-27, all correctly company-primary-resolved) threaded correctly under the *existing* mechanism -- 2 genuine same-day multi-source threads, zero false merges on full manual inspection -- proof the conservative Story Thread design holds up under a real, high-volume mainstream dataset, not just synthetic fixtures.

Both `docs/v2/TECHNICAL-DEBT-REGISTER.md` and `docs/v2/INTELLIGENCE-COVERAGE-MATRIX.md` (created by this mission's own required deliverables, since neither existed on canonical when the mission started) collided with Cursor's concurrent PR #52, which merged mid-mission and independently created both files at the same paths with more numerically rigorous existing content. Resolved by rebasing onto the new canonical and merging this mission's findings *into* Cursor's now-canonical structure and ID scheme (continuing their TD-013+ / TD-THREAD-003 numbering, adding a precise addendum to their existing TD-006 rather than a duplicate entry) -- not overwritten, not left as a silent duplicate ground truth. The Coverage Matrix's Mainstream News row is corrected from Cursor's `OPERATIONAL — keyword-news volume, not recall-benchmarked` to `PARTIAL` now that real recall has been measured and does not support the stronger label; Regulatory/Government stays `PILOT`. Full `pytest` (1098 passed, 1 pre-existing unrelated failure -- Windows path, unmodified canonical), `validate_records.py`, and `build_static.py` (1516 pages, no draft leakage) all green on the rebased branch. Deployed to the VPS (fast-forward `6a3e7eb` -> `e1550e1`, Docker rebuild, all 5 new sources confirmed synced into the live runtime by the container's own startup log, `/healthz` 200, public health check 302) and verified live, not assumed.

V2 decision workflow Gate 0 (2026-08-21, branch `cursor/v2-decision-workflow-bd27`, PR #52): migrated Pending Review (`GET /pending` decision workspace; bulk dismiss never publishes), Reading Queue compact cards, and Assessments V2 list/detail onto the approved AppShell. Overlay `/api/` skips Brief; `/brief` stays `mode="full"`; `/pending` uses `mode="pending"`. Assessment berry scope is labeled from stored `market_ids` only (unscoped ≠ every berry). Compact footer no longer repeats `kind_label`. Landscape JS breadcrumb reads `data-berry-label` (Landscape itself unmigrated). Rebased onto later governance at `8158021` (PR #53 expansion guide + PR #51 Story Thread). The expansion guide is preserved verbatim from canonical. Companion register/matrix from this PR fold unique acquisition/entity/thread/test items from the withdrawn expansion-guide-session drafts (TD-THREAD-002, TD-ACQ-002..004, TD-ENT-001..003, TD-REVIEW-001, TD-TEST-001) without reopening UI-lane items: TD-001..004 and TD-011 stay resolved. Stop gate: no Landscape / Monitor / admin migration.

Story Thread Subject-Attribution Robustness (2026-08-20, branch `feature/story-thread-subject-attribution`, PR #51, merge commit `807e059`): fixed the generic false-separation weakness surfaced twice in real data across the Strawberry and Raspberry Vertical V1 missions -- headline phrasing sends `attribute_draft()`'s `primary_subject` to a company for one article and a variety for another describing the same real event ("COMPANY launches VARIETY" vs. "VARIETY expands into MARKET"), and `_strong_event_edge()`'s plain `primary_entity_id` equality check can never bridge that type mismatch, so genuinely related coverage stays permanently, silently separate. Added `_cross_subject_event_edge()` to `app/services/story_threads.py` -- one narrow, additional edge, not a loosening of the existing rule: fires only when the same company AND the same named variety are each independently confirmed on both sides (via the existing entity chips field or the entity's distinctive name appearing verbatim in the other title), plus the same tight date-proximity window `_strong_event_edge()` already uses. Same-company-only or same-variety-only still stays separate. Verified against real production data, not synthetic fixtures alone: Raspberry -- 3 of 4 real Malaika articles now correctly thread (up from 2), including the one whose Fruitnet headline led with the company name; the 4th (a genuinely separate earlier event, 238 days prior) correctly stays separate, outside the date window. Strawberry -- 1 new real thread (Planasa's own newsroom article, company-primary via `newsroom_identity` attribution, threading with Fruitnet's third-party report of the identical RedSayra market-share claim); the CBC/Driscoll's litigation cluster, already correctly limited to a 2-source same-day thread by the existing rules, stays exactly that size, confirming no over-merge of a multi-year saga. Blueberry -- zero new threads, reported honestly as the current corpus not currently exhibiting this specific failure mode within the date-proximity window, rather than forced. Exactly 2 new multi-source threads formed across all three berries combined; both manually inspected in full; zero false merges. 6 new regression tests using real observed cases (Malaika, RedSayra, Chambers/Cornell, Planasa/Blue Manila/Blue Maldiva): the real positive case, same-company-different-variety stays separate, same-variety-different-company stays separate, generic species-word overlap stays separate, the real company-newsroom-to-trade-reprint case, and different patent filings for the same breeder stay separate. Zero coupling to `signal_candidates.py`/`source_independence.py` -- Story Thread membership remains organizational only and does not change Signal confidence, trust gates, or Assessment logic. Full `pytest` (1070 passed, 1 pre-existing unrelated failure), `validate_records.py`, and `build_static.py` (1516 pages, unchanged -- no data touched this mission) all green. Deployed and verified live via an in-container smoke test reproducing the real Malaika case directly against the running app.

Also this session: adopted `docs/v2/INTELLIGENCE-EXPANSION-BUILD-GUIDE.md` as the governing roadmap for the post-foundation expansion phase (10 workstreams: variety intelligence, mainstream/regulatory recall, global PVR/registry expansion, retail-market observation, insider/jobs/conferences, customs/trade, weather/satellite, global competitive landscape, UI V2, and a dedicated Technical Debt & Reliability workstream). `AGENTS.md` now points to the guide at the top of the file. Companion drafts from that same session were withdrawn from PR #53 after discovering Cursor PR #52 already owned `TECHNICAL-DEBT-REGISTER.md` and `INTELLIGENCE-COVERAGE-MATRIX.md`; this PR restores those companions as live control files and folds the withdrawn draft's unique non-UI items rather than replacing the numerically rigorous Cursor versions or reopening TD-001..004.

Raspberry Vertical V1 (2026-08-20, branch `feature/raspberry-vertical-v1`, PR #49, merge commit `69ef563`): third real intelligence vertical, and the first to test repeatability directly. Added 5 real companies (Wish Farms -- in-house breeding led by ex-Driscoll's breeder Carlos Fear, 35 patents across raspberry/blackberry/strawberry; Global Plant Genetics -- real cross-berry licensing platform; James Hutton Ltd -- UK raspberry genetics owner GPG represents for Morocco/Tunisia; Chambers -- UK grower running a 50-variety/9-country independent raspberry trial; Berrytech -- Italian breeder of Amalia Rossa), 1 breeding program (Cornell University, source of three real raspberry releases), and 11 varieties with a named real breeder in Evidence: 3 Cornell releases (Double Gold, Crimson Night, Crimson Treasure) and 7 from Advanced Berry Breeding's own published variety list (Malaika, Zawadi, Sarafina, Rafiki, Baridi, Shani, Kwanza) plus Amalia Rossa/Berrytech -- ABB's own variety page had sat in trusted Evidence, unlinked, since an earlier session. Fixed two real, confirmed mistagged Evidence records found while building the baseline (a citrus mandarin article and a tomato-variety article, both auto-captured via a "Raspberry variety license" keyword search and tagged berry-raspberry despite zero berry content -- corrected to empty berry_ids, not deleted). Added one new generic trait, `trait-fruit-color`, grounded in Cornell's own "vibrant colors" coverage of Crimson Treasure -- a real gap in the original blueberry-derived trait vocabulary, applicable across all four berries. Explicitly re-tested Spanish (Freshuelva) and Italian (Nova Siri Genetics) sources for raspberry recall specifically rather than assuming the Strawberry mission's language-term fix transferred automatically -- it did, zero new gap found, zero further code change needed. Ran Patent Monitor v2 for real: 26 provider hits, 15 kept, 4 of 15 auto-linked to Driscoll's DrisRasp series with zero code changes; manually inspected 5 drafts and found one honest "unresolved assignee" case (two "ABB 135"/"ABB 136" patents assigned to "Allberry B.V.", not "Advanced Berry Breeding B.V." -- plausibly the same company given the shared "ABB" cultivar-naming convention and inventor, but not confirmed, so left unlinked rather than force-aliased on weak evidence). Story Thread grouping produced zero multi-member threads, with a real, understood reason: the same title-phrasing-driven primary-entity variance already observed limiting Strawberry's CBC thread (a company-led vs. variety-led headline for the same real event breaks the exact-primary-entity-match requirement) -- not forced. One inbox draft was briefly, then correctly, un-linked from a newly-created variety entity after review, since the draft was untrusted and hadn't gone through the human review gate -- a trusted entity should not be grounded in untrusted content alone, the same discipline already applied to patent drafts. **Repeatability result, the mission's central question**: every substantive change this session was DATA ONLY (entities, relationships, two evidence corrections) or reused an existing GENERIC BUG FIX (the Strawberry mission's language-term fix, verified rather than assumed) -- zero new `app/*.py` changes, zero new sources needed, zero berry-specific hacks. Full `pytest` (1064 passed, 1 pre-existing unrelated failure), `validate_records.py`, and `build_static.py` (1516 pages, Raspberry Landscape renders all new entities through the same generic infrastructure) all green. Deployed and verified on the live filesystem that all new entities/relationships synced into the bind-mounted runtime data.

Strawberry Vertical V1 (2026-08-20, branch `feature/strawberry-vertical-v1`, PR #46, merge commit `7507122`): turned Strawberry from the prior mission's portability proof into a real second intelligence vertical. Added 5 real companies (Nova Siri Genetics and CIV Italy -- breeders of Melissa/Marimbella and Flavia; Eurosemillas -- UC Davis's former strawberry master licensee; Rijk Zwaan -- a genuine 4-berry Dutch breeder; Freshuelva -- the Huelva growers' association, founded 1983, live-verified), 1 breeding program (Rutgers NJAES, source of the D'Light cultivar), and 5 varieties with a named real breeder in Evidence (RedSayra/Planasa, Melissa/Marimbella/Nova Siri Genetics, Flavia/CIV, D'Light/Rutgers) -- 21 existing Evidence records got real `entity_ids` for the first time. Found and fixed two real data bugs while cross-checking cross-berry company claims: Planasa's Advanced Berry Breeding subsidiary was tagged `berry-blueberry` despite its own description saying raspberry-only (now correctly raspberry, making Planasa a genuine 3-berry company); two Driscoll's strawberry-litigation Evidence records were tagged `berry-blueberry` from the original blueberry-only pilot era despite explicitly stating in their own `why_it_matters` that the case is a strawberry matter (now correctly strawberry, making Driscoll's a genuine 4-berry company backed by real per-berry evidence). Ran Patent Monitor v2 for real against all 4 berries: strawberry recall beat blueberry's (80 vs 55 provider hits) and real assignee-based auto-linkage worked with zero code changes, correctly linking 10 of 15 strawberry patent drafts to already-known companies; added a UC Davis breeding-program alias ("The Regents Of The University Of California", its real patent-assignee name) that resolved a previously-unresolved match. Ran real current-article ingestion against 4 sources and found a genuine, load-bearing gap: `relevance_screen.py`'s `berry_identity` category had zero non-English terms of any kind, so a real, live-verified Italian source (Nova Siri Genetics) was screened 0-for-5 relevant despite covering real strawberry breeding news; fixed by adding Spanish/Italian species terms (deliberately excluding Italian "more" -- it collides with the common English word even under word-boundary matching) -- a re-run of the same source then correctly found 3 of 5 relevant, surfacing a brand-new real variety (Elyson, cultivar NSG 48) for the first time; a second new Spanish-language source (Freshuelva) found 6 of 10 relevant under the same fix. Both new sources are real, live-verified RSS feeds wired with real `discovery.adapter` blocks, not just reference entries. Re-ran signal-candidate generation for real: a candidate spanning blueberry+strawberry was inspected closely per the mission's explicit instruction not to manufacture a cross-berry Signal, and reported honestly as two unrelated IP-activity streams for the same company coinciding in a rolling time window, not a coordinated cross-berry pattern -- the existing `does_not_prove` caveat already covers this. Story Thread grouping (tested with real `primary_subject` attribution applied via `draft_attribution.attribute_draft()`, matching how the live app actually computes it -- calling `group_story_threads()` on raw records without that step gives a false negative) produced 4 real multi-member threads, including a same-day two-source CBC-litigation thread. One real duplicate-draft finding (a live Fresh Fruit Portal item matching an already-trusted record via a Google-News-redirect URL under a different title suffix, caught by `build_static.py`'s own leak self-check) was handled per the exact precedent already established in an earlier session: removed as untracked-inbox cleanup, not a code fix. Full `pytest` (1064 passed, 1 pre-existing unrelated failure), `validate_records.py`, and `build_static.py` (1498 pages, Strawberry Landscape renders all new entities, no draft leakage) all green. Deployed and verified on the live filesystem that all new entities/relationships synced into the bind-mounted runtime data.

Multi-berry portability audit (2026-08-20, branch `feature/multi-berry-portability`, PR #43, merge commit `59af04f`): the question was whether the platform is a genuinely berry-neutral Intelligence OS or a blueberry system with generic names. Answer: the schema/service layer was already generic (no berry-specific fields or enums anywhere), but the *product surface* was hardcoded to blueberry in three separate places — the Landscape route (`/landscapes/berries/blueberry` was the only route, despite `landscape_context()`/`BerriesLandscapeService` already taking `berry_id` as a generic parameter), its nav link, and `build_static.py`'s static-page seed list — all now parameterized over the existing `BERRIES` dict. Also fixed a hardcoded "direct blueberry intelligence" Morning Brief decision label, a blackberry/raspberry gap in Story Threads' Jaccard filler-word list, and thin non-blueberry term coverage in patent relevance/trade-association matching. Separately, real Evidence for the other three berries already existed (165 strawberry, 21 blackberry, 20 raspberry records) but the entity/knowledge-graph layer (companies, varieties, breeding programs) was ~99% blueberry-only, leaving that evidence unlinked. Added real, evidence-grounded pilot entities: `berry-strawberry`/`berry-raspberry`/`berry-blackberry` (the platform referenced these `berry_ids` everywhere but shipped no resolvable entity, mirroring a gap `berry-blueberry`'s own record already documented as fixed for blueberry only); California Berry Cultivars and UC Davis Strawberry Breeding Program (built on the real Driscoll's v. CBC strawberry patent litigation, independently corroborated across 6 outlets); The Summer Berry Company (raspberry); Plant Sciences Genetics and an expanded University of Arkansas (blackberry) — proving Driscoll's, U Arkansas, and Plant Sciences Inc. can each span multiple berries in one entity record with no duplication. Two new generic trait entities (`trait-fruiting-habit`, `trait-flowering-habit`) fill a real gap: the original 10 traits were all measurement-oriented and blueberry-pilot-derived, with no entry for the primocane/floricane or day-neutral/short-day distinctions that actually differentiate raspberry/blackberry/strawberry varieties — added through the existing generic trait-entity mechanism, no schema change. Re-ran `scripts/generate_signal_candidates.py` for real; it produced a multi-berry corroborated candidate (`sigcand-repeated-company-activity-driscolls-*`, `berry_ids: [blueberry, strawberry]`) directly from the newly linked evidence with zero changes to the corroboration engine. Full validation: `pytest` 1064 passed (1 pre-existing, unrelated Windows-path failure; one export test failed once in a full-suite run but passed standalone/full-file/clean-rerun — a pre-existing test-order flake, not a regression), `validate_records.py` clean, `build_static.py` 1487 pages with all 4 berry Landscape pages rendering real content (60–64 KB each), `git diff --check` and secret scan clean. `tests/test_domain_pack.py`'s hardcoded live-entity count (164 → 173) and `domain-packs/berries/` manifest counts updated for the 9 new real entities. Deployed and verified on the live filesystem (not assumed) that all new entity files synced into the bind-mounted runtime data. Full report: see the published portability-audit artifact from this session.

Article-relevance precision fix + cross-pipeline dedup, merged as PR #14 (2026-08-18, branch `feature/freshness-company-news`, worktree `berry-intelligence-os-freshness`, merge commit `28444dd`, base rebased onto PR #13's merged login/session work at `7a4819c`): the recurring-refresh review queue was mixing high-confidence berry intelligence with generic produce-news firehose (root example: a solar-panel/pear-tree article with one incidental "raspberries" comparison ranked as equally relevant as real berry trade coverage). `app/services/relevance_screen.py` (v3) now requires a berry species/cultivar to recur across >=2 distinct paragraphs of the acquired article body (or be a named berry company) before tagging a draft `direct`; a single incidental mention paired with a real agtech/trade-policy/labor/weather-climate signal is tagged `adjacent` instead of presented as equally high-confidence (short articles, <=2 total paragraphs, are exempt from the recurrence rule — a real regression found via the full test suite, since no genuine false positive in this project's real data was ever that short). Re-validated against all 16 real article drafts from the prior refresh: 10 direct / 5 adjacent / 1 irrelevant (93.75%). New `app/services/article_dedup.py` adds deterministic cross-pipeline duplicate detection (normalized canonical URL first, then conservative normalized title + same source + same published date — never title similarity alone), wired into `MediaOrchestrationService.resolve_publication_artifact()`; a regression test written for "don't merge on title similarity" caught a real bug in the title-normalization regex itself (it treated the hyphen inside "Anti-Dumping" as a publisher-suffix separator, which would have silently merged a pecans story with a blueberries story) before it ever shipped. Real, honest finding from re-running the actual recurring collector post-fix: two "new" article drafts it created were in fact the same two real stories already trusted under a *different* source registration (a Google-News-redirect-URL alert feed vs. the publisher's own direct RSS) — different URLs and different `source_id`s, so neither the URL nor the title+source+date check could legitimately catch it without the kind of loose/fuzzy matching this task explicitly ruled out; both duplicate drafts were removed as untracked-inbox cleanup (not a code fix) and the gap is documented, not silently absorbed. Also surfaced, not yet acted on: one of the two pre-existing trusted records involved is missing the `evidence_role` field entirely (predates that field), and the real collector run — when run unbounded across all registered podcast sources — produced 562 new untranscribed podcast-episode drafts in one pass (a real back-catalog catch-up, not a bug, but a genuine "review queue at scale" consideration for future podcast-side relevance work, which was out of this task's explicit article-only scope). Full validation on the actual merged canonical (this PR + the independently-and-concurrently-merged PR #15 "live intelligence reader," which also touched `app/main.py`): 903/903 `pytest`, `validate_records.py` clean, `build_static.py` clean (1,474 pages, draft-leakage self-check passed), `git diff --check` clean, manual secret-pattern scan clean.
YouTube acquisition operational-resilience spike, concluded documentation-only, no code changes (2026-08-16, branch `spike/youtube-acquisition-resilience`, isolated worktree based on canonical `ff471145`): investigated whether a real bot-check failure hit during a bounded, real (non-mocked) collection pilot against `source-blueberries-tv-youtube` warrants a code/config response. Pilot evidence: first run acquired one item via `tier_2_youtube_auto_captions`; a second, shortly-after idempotency run correctly fell through to Tier 3 for one previously-untranscribed item and failed with yt-dlp's own `"Sign in to confirm you're not a bot. Use --cookies-from-browser or --cookies for the authentication."`, raised inside `youtube_media_acquisition._resolve_audio_format_real()` (via `_extract_info_real()`'s `yt_dlp.YoutubeDL.extract_info()` call), surfaced as `media_transcription.MediaAcquisitionError`, recorded by `media_orchestration.py` as `transcript_status="acquisition_failed"`, and classified by `collection_runner._classify_failure()` as `failure_class: "retryable"` (via the `acquisition_failed` default, not a `RETRYABLE_MARKERS` text match -- confirmed by reading both files). Verified this classification is correct and the runner's pre-existing generic retry/backoff/escalate-to-operator machinery (`--retry-limit`, default 3; `--retry-backoff-seconds`, default 1800) already handled it safely: the publication draft was left fully intact, the failure did not cascade to the other items in the run, and no malformed transcript was produced. Verified the pinned `yt-dlp==2026.7.4` matches the current upstream stable release (PyPI/GitHub release history checked directly) -- not a stale-pin defect. Verified via web research that `cookiefile`/`cookiesfrombrowser`/`extractor_args` (`player_client`)/`sleep_interval` are `yt-dlp`'s real, documented, operator-controlled mechanisms for this class of challenge, and confirmed zero lines of this repository's code currently wire any of them in. Verified this exact failure is a first occurrence in this project's real-network testing history: a prior real Tier-3 session (`YouTube Tier-3 (audio + local Whisper) real-world proof`, above) completed multiple real audio acquisitions (Comité de Arándanos de Chile) with no bot-check, and Tier 2 succeeded against the same channel moments before this failure in the same pilot -- one data point, correctly not overgeneralized, but consistent with a transient, reputation/volume-scoped challenge rather than a systemic adapter defect. **Conclusion: B -- documentation only.** Added a "YouTube platform-access challenges" section to `docs/v2/RECURRING-COLLECTION-RUNNER.md` recording the failure anatomy, why `retryable` is correct, and operator guidance (re-run later; no authenticated-session mechanism exists in this repository today; if the condition becomes recurring rather than a single instance, `yt-dlp`'s own `cookiefile`/`cookiesfrombrowser` options -- an operator's own legitimate, locally-supplied session, never a bypass -- are the correct minimal extension point, scoped to `youtube_media_acquisition.py` only). No code, test, schema, or `sources.json` changes; no credential/cookie/session mechanism was built; `podcast_rss` and the generic acquisition framework are untouched. Full `pytest`/`scripts/validate_records.py`/`scripts/build_static.py` re-run was not required (no code or test-exercised behavior changed) and was not performed.

Archive/index source-discovery research, concluded do-not-implement (2026-08-16, branch `feature/archive-source-discovery`, isolated worktree based on `feature/youtube-tier3-whisper-proof`'s tip, no code changes): investigated whether a third generic discovery-adapter class (a first-party archive/event-index adapter, alongside the existing `podcast_rss`/`youtube_feed`) is warranted, using real first-party web research (not name-based assumption) against every named candidate from this project's earlier planning-only acquisition-class taxonomy exercise -- International Blueberry Organization (IBO) Summit, BQI/BerryQuest International (Berries Australia), university field-day archives, Horticulture Week, Lubera Edibles, The Packer, The Fresh CrEd, Global Fresh Series, Fresh Takes on Tech (IFPA), RealAg Radio, Produce Buzzers, AgriTalk India, Agronometrics, CGTN/WION, VoxDev. Real, verified findings: 8 of the candidates (HortWeek, Lubera Edibles, The Fresh CrEd, RealAg Radio, The Packer, Global Fresh Series, VoxDev, and by strong platform evidence Produce Buzzers/AgriTalk India/Fresh Takes on Tech) are ordinary podcast-host-hosted shows with real, independently curl-verified RSS feeds (HortWeek/Acast, Lubera/Podigee, Fresh CrEd/Podbean, RealAg/WordPress, The Packer/Omny, Global Fresh Series/Captivate, VoxDev/Audioboom all returned real `<rss>`/`<feed>` XML) -- `podcast_rss`, zero new code, configuration only. The two candidates that looked most archive-shaped were checked hardest and both resolved the same way: IBO's own site publishes one-off yearly blog posts (no stable per-session URLs, no bounded index) and gates real speaker presentation PDFs behind IBO membership login (inaccessible-private, out of scope per this project's no-auth-bypass rule); the actual International Blueberry/Berry Seminar video content (Mexico/Chile/Peru/Morocco, 2023-2025) is produced by a separate third-party consultancy ("Blueberries Consulting") and published entirely on one YouTube channel -- real channel id `UCwypVNJp_DjjoLtqVCwIF4w` (`@BlueberriesTV`), confirmed via yt-dlp against two sampled real videos -- i.e. already fully `youtube_feed`-shaped, configuration only, zero new code. BQI (Berries Australia) has no published recordings at all (event-registration page only); Berries Australia's own "Resource Library" (the most database-like real archive page found in this whole investigation) still resolves every video item straight to a `youtu.be` URL. A parallel real check of Cornell Cooperative Extension's "Winter Fruit Webinar" list (not a named candidate, found while researching university field days, and containing a genuinely on-topic real hit -- "Blueberry Pruning and Soil Health") showed the identical pattern: a hand-maintained page of links, every video already YouTube-hosted across three different extension channels. University field days themselves (MSU, UGA) were confirmed in-person-only with no recorded video published anywhere -- genuinely inaccessible, not YouTube-shaped, not archive-shaped. Agronometrics is primarily a charts/data-story publisher (not spoken-word media, out of this module's scope) with an incidental YouTube channel. CGTN/WION have no bounded, monitorable first-party berry-relevant archive (scattered general-news segments only) -- correctly unresolved, not a crawl target. **Conclusion: C -- do not implement a third discovery adapter.** Zero of the 15 real candidates investigated required first-party HTML archive discovery once their actual publication architecture was verified; every real, publicly accessible one is already `podcast_rss`- or `youtube_feed`-shaped, and the remainder are genuinely inaccessible (membership-gated) or have no public archive at all (no recordings ever published). No `first_party_archive`-style adapter, no new abstraction, no new dependency, no new tests were added -- per this task's own explicit instruction, a well-evidenced negative conclusion is the complete, correct deliverable. No Evidence/Fact/Assessment/Recommendation created; no discovery/transcription/orchestration code touched; no Codex-owned review/extraction file touched. `sources.json` has no existing entries for 12 of the 15 candidates (Horticulture Week, Lubera Edibles, The Packer generic reference aside, Fresh CrEd, Global Fresh Series, Fresh Takes on Tech, RealAg Radio, Produce Buzzers, AgriTalk India, Agronometrics, CGTN, WION, VoxDev, BQI/BerryQuest) -- onboarding any of them as real Sources (not done here, out of this task's scope, which was the adapter question only) would be a future `sources.json`-only change once someone decides they're berry-relevant enough to track. Actionable, real, reusable finding for a future onboarding session: `youtube_feed` against channel id `UCwypVNJp_DjjoLtqVCwIF4w` (`Blueberries TV`) would be the correct, zero-new-code way to pick up IBO-summit-adjacent seminar video content, if/when that's judged worth adding.

Extraction evaluation harness (2026-08-16): the existing OpenAI-compatible provider now supports repeatable readiness probing, a 12-case company-neutral synthetic benchmark, deterministic sampled/full transcript preview, model/config comparison, structural and behavioral diagnostics, runtime/token accounting, and gitignored run artifacts under `inbox/evaluations/`. Preview never writes Evidence; explicit `--persist-proposals` delegates to the existing `TranscriptEvidenceExtractionService`. `docs/v2/ATOMIC-CI-EVALUATION.md` defines the compact human rubric and a proposed pre-automation quality bar. No endpoint was configured locally, so semantic model quality and real Lucentlands throughput remain unmeasured; no model output was fabricated.

YouTube Tier-3 (audio + local Whisper) real-world proof (2026-08-16, branch `feature/youtube-tier3-whisper-proof`, isolated worktree, based on `feature/youtube-source-portability`'s tip): the real, live-network proof this project's YouTube acquisition work had not yet had -- Tier 3 (caption-probe finds nothing usable -> resolve/download audio -> local `faster-whisper`) had only been exercised via offline/mocked tests before this session; both prior real proofs (Redagrícola, U. Arkansas) landed on Tier 2 (captions). No generic code was changed in `app/services/youtube_media_acquisition.py`, `media_transcription.py`, or `scripts/transcribe_media.py` -- Phase 2 inspection confirmed the existing Tier 1/2/3 dispatch, YouTube caption/audio acquisition, and `FasterWhisperProvider` wiring could already perform the complete run as written. Real findings from actually running it: (1) yt-dlp's format resolution works for ordinary channel uploads via its `android_vr` client fallback even with **no system JS runtime installed** (node/deno/quickjs/bun all absent) and **no system `ffmpeg`** -- confirmed empirically, `faster-whisper`'s bundled PyAV/CTranslate2 decoded a real downloaded `.m4a`/`.webm` audio stream directly; (2) that same `android_vr`-only fallback (a consequence of no JS runtime) reproducibly fails ("this video is not available") for every one of 8 tested North American Blueberry Council webinar-recording uploads even though they are genuinely public (oembed-confirmed) -- a real, video/channel-content-type-specific yt-dlp/environment limitation, not a code bug, worth a JS-runtime install (`node`/`deno`) if that publisher's content is ever needed; (3) an extensive, honest search for a real, legitimate, no-usable-caption *and narrated* YouTube video (~160 real videos checked live via yt-dlp across 20+ first-party berry/agriculture channels -- universities, trade associations, breeders, growers, conferences -- in English, Spanish, French, Polish, Portuguese, Kannada) found that YouTube's ASR auto-caption coverage in 2026 is now comprehensive for essentially all detected speech in every language checked, down to 13-second amateur clips and 2018-era uploads; the only genuinely zero-caption-track videos found (`info["subtitles"] == {}` and `info["automatic_captions"] == {}`, the real structural condition `_select_caption_track()` is built to recognize) were two ~164s institutional bumper videos on the Comité de Arándanos de Chile's real YouTube channel (new Source `source-comite-de-arandanos-de-chile-youtube`, oembed/channel-id verified), both of which real Whisper decode confirmed are instrumental-music-only with no narration. Running the real, unmodified Tier-3 pipeline against both (real yt-dlp audio resolution, real download, real `faster-whisper small`/CPU/int8 invocation) correctly produced a `TranscriptionError` ("zero segments... treated as a failure, not an empty result") both times -- real, unmocked confirmation that this project's "never produce a malformed transcript for non-speech audio" design holds against genuine non-speech content, and that a second identical run reused the cached audio download (unchanged file mtime/checksum) without re-downloading. This is real, valuable infrastructure proof (audio acquisition and Whisper invocation both genuinely exercised end-to-end for the first time, no ffmpeg required) but does **not** reach a real narrated normalized-transcript success case -- that remains proven only via the pre-existing offline/mocked test suite (`tests/test_youtube_media_acquisition.py`, `tests/test_media_transcription.py`), not against real content, despite this session's search. Live regression re-run (real network, not just tests) confirmed the Tier 1/2/3 hierarchy is undisturbed: Redagrícola's real "El arándano está conquistando a Ica" (`TRG0WsxJ1Lw`) still resolves to `tier_2_youtube_auto_captions` (382 segments, unchanged), Arkansas's "How to Pick Blueberries" still resolves to `tier_2_youtube_human_captions` (78 segments, English) -- both real re-discoveries, neither ever reaching Whisper -- and both RSS podcast sources (Lucentlands, Business of Blueberries) still discover normally via `podcast_rss`/enclosures, never touching the YouTube adapter. One offline test added (`test_resolving_parent_evidence_does_not_retranscribe`, closing a real gap in the existing "parent-binding must not retranscribe" coverage). `tests/test_domain_pack.py`'s hardcoded live-source count (124 -> 125) and `domain-packs/berries/collector-templates.json` updated for the one new Source, following the exact registration pattern `source-redagricola-on-the-road`/`source-university-of-arkansas-division-of-agriculture` already established. Full `pytest` (594 passed), `scripts/validate_records.py`, and `scripts/build_static.py` (1,471 pages, unchanged) all pass. No Evidence/Fact/Assessment/Recommendation created; no Codex-owned file (`media_orchestration.py`, `transcript_evidence.py`, `review_publish.py`, `ai_extraction.py`, `schemas/evidence.schema.json`) touched. Known downstream issue (Codex-owned, not fixed here, reproduces as documented by the prior branch's own `test_youtube_tier3_orchestration_cache_freshness_bug_reproduces_for_arkansas`): `media_orchestration.py`'s `_cache_matches_request()` compares against `select_enclosure_url()`, which every YouTube item leaves `None`, guaranteeing a false cache-miss at the orchestration layer on a YouTube Tier-3 rerun -- harmless (the video-id-keyed acquisition cache still prevents real re-download/re-transcription) but causes one redundant `transcribe_discovered_item()` call per rerun; safe fields for Codex's fix: YouTube video id, canonical URL, selected audio format id, media checksum, acquisition tier, transcript cache key, model, language, transcript content hash -- all real, stable, already recorded in the normalized transcript's `acquisition` block. Pending: a genuine real narrated Tier-3 success proof remains open; the most promising untried lever is monitoring a high-frequency first-party channel's feed in real time to catch a video within its ASR-processing window (minutes to hours), since no video older than ~5 days was found uncaptioned in this session's search.

YouTube caption/audio acquisition (2026-08-16, branch `feature/youtube-media-acquisition`, built in an isolated worktree parallel to Codex's concurrent AI-extraction-provider work, based on `feature/spoken-word-source-expansion`'s tip): closes the one real gap that phase left open -- a `youtube_feed`-discovered item (e.g. Redagricola's "On The Road" videos) had no path to a transcript at all, since it carries no RSS-style audio enclosure. New `app/services/youtube_media_acquisition.py` (generic, config-driven -- decides whether/how to act on an item solely from `raw_metadata.yt_video_id`, never `source_id`) adds a real Tier 2 (YouTube captions, via yt-dlp's public credential-free video-info API, no scraping, no OAuth) ahead of the existing Tier 3 (local `faster-whisper`, reused as-is -- no second STT integration): human/publisher-uploaded captions (`info["subtitles"]`) preferred over YouTube's own auto-generated ASR captions (`info["automatic_captions"]`), both normalized through the *existing* `media_transcription.parse_timed_text_captions()`; a new `_dedupe_rolling_auto_captions()` cleans up YouTube ASR's two-line "rolling window" VTT quirk (real Redagricola video TRG0WsxJ1Lw's auto-caption track: 740 raw cues -> 382 clean, non-redundant segments, verified by hand against the real content). Language selection never guesses a translated caption (`automatic_captions` always also carries 100+ machine-translated tracks) -- only the video's own detected spoken language, or a YouTube-marked `<lang>-orig` original, is ever selected; otherwise Tier 2 defers to Tier 3's own from-audio language detection. Tier 3 gained a YouTube-audio branch (`acquire_youtube_audio()`, same `AcquiredMedia`/`_media/` convention as the RSS path, but cached on the stable video id rather than the ephemeral signed CDN URL yt-dlp re-resolves every call -- a real, load-bearing difference from the RSS enclosure-URL cache). `scripts/transcribe_media.py` needed zero changes -- the existing single `transcribe_discovered_item()` call now transparently routes YouTube items through captions-then-audio-then-Whisper. `media_transcription.py`'s RSS-only code path never imports `youtube_media_acquisition` (or therefore yt-dlp) at all, verified directly. New pinned dependency `yt-dlp==2026.7.4` (a 2025.8.11 pin, current as of this project's other pins, was tried first and failed outright against real videos -- YouTube's extraction requirements change often enough that this pin should be expected to need periodic bumps). 28 new offline/mocked tests (`tests/test_youtube_media_acquisition.py`, fakes injected at the yt-dlp/HTTP boundary, no real network in pytest); all pre-existing media/discovery/transcription/orchestration tests (114) remain green, unmodified. Real, one-time proof performed by hand (not part of automated tests): the real Redagricola video "El arandano esta conquistando a Ica" (id `TRG0WsxJ1Lw`, 982s, Spanish, public, no manual captions but real YouTube ASR auto-captions available) was run through the real CLI unmodified -- `tier_2_youtube_auto_captions`, 382 real segments, `es`, monotonic timestamps, 0 very-short-text segments, real content verified by hand (opens "Ica siempre fue tierra de algodon, luego de esparragos..."); a second real CLI run confirmed the caption cache hit (no re-fetch). The resulting staged transcript was independently confirmed `TranscriptArtifact.from_dict()`-compatible once a synthetic `parent_evidence_id` was supplied (Codex's contract, not modified). No audio/video file was downloaded for this proof (captions succeeded, per this task's preferred path); the audio-fallback/Whisper mechanism and video-id-keyed cache were proven via the offline test suite plus one real, partial (range-request) reachability check against a real resolved audio-only CDN URL. `scripts/validate_records.py` and `scripts/build_static.py` (1,471 pages, unchanged) both pass. No Evidence, Fact, Assessment, or Recommendation was created; no Codex-owned file (`transcript_evidence.py`, `media_orchestration.py`, `review_publish.py`, `schemas/evidence.schema.json`, review templates) was touched. Known, undangerous limitation left for a future session: `media_orchestration.py`'s `MediaTranscriptionAdapter._cache_matches_request()` (not modified, per this task's scope) doesn't recognize a cached YouTube Tier-3 audio result as fresh (it compares against `select_enclosure_url()`, which is always None for a YouTube item) -- harmless today, since `transcribe_discovered_item()`'s own internal video-id-keyed cache still prevents any real re-download/re-transcription; it only means the orchestration layer re-invokes `transcribe_discovered_item()` once more than strictly necessary on a re-run. Pending: merge coordination with `feature/spoken-word-source-expansion` (already the base) and Codex's extraction-provider workstream; portability onboarding of a second YouTube-first source (e.g. U. Arkansas Blackberry Field Day) is unstarted and requires only a `sources.json` `discovery` block, no Python changes, per this phase's own design goal.

Provider-neutral AI extraction (2026-08-16): `OpenAICompatibleExtractionProvider` implements the existing transcript `ExtractionProvider` boundary with deterministic overlapping context windows, strict structured output, repository-ID allowlists, defensive parsing, partial-window error reporting, cross-window dedupe, `atomic-ci-v1` prompt versioning, and runtime/token metrics. The existing operator command accepts externally configured compatible endpoints/models and still writes only deterministic untrusted drafts to `inbox/evidence/`; human review remains mandatory. No compatible endpoint or extraction credentials were configured locally, so the implementation was proven against mocked HTTP responses and the real cached Lucentlands transcript was not sent to a model. No discovery/source files or trusted production records changed. See `docs/v2/ATOMIC-CI-MODEL-EXTRACTION.md`.

Source expansion and generic YouTube acquisition (2026-08-16): the source registry now configures Lucentlands and Business of Blueberries through the generic `podcast_rss` adapter, and Redagricola plus University of Arkansas Division of Agriculture through the generic `youtube_feed` adapter. YouTube media acquisition prefers human/publisher captions, then platform auto-captions, then audio through the existing local Whisper path; dispatch uses platform identity rather than Source IDs. Real proofs produced 382 Spanish auto-caption segments for Redagricola and 42 English human-caption segments for Arkansas without trusted writes. `yt-dlp==2026.7.4` is pinned because YouTube extraction compatibility is operationally maintenance-sensitive. The second publisher required Source/configuration changes and tests only, with no new service logic. Cache freshness is now acquisition-aware: the transcription layer persists a typed source fingerprint (platform item, RSS enclosure, publisher transcript, or canonical URL), validates model/language and locally available checksum metadata, and exposes one cache-validity boundary to orchestration. Unchanged RSS, human/auto caption, and simulated YouTube audio/Whisper caches are reused; changed acquisition identity, model, requested language, local checksum metadata, or `--force` invalidates them. Remote bodies at an unchanged RSS URL and YouTube caption edits with no public revision identifier still require `--force`; orchestration does not perform expensive live polling merely to load a valid normalized transcript.

Spoken-word transcription/orchestration integration (2026-08-15): `MediaTranscriptionAdapter` connects the public `load_transcript_artifact()` / `transcribe_discovered_item()` API to `MediaOrchestrationService`. A compatible normalized cache is reused before any provider is constructed; missing or changed cache inputs delegate to the transcription service, and dry-run never transcribes. The existing Lucentlands episode resolved to its trusted publication parent and reused the real 1,212-segment `faster-whisper:small` transcript without changing its normalized/raw/audio hashes or timestamps. The bound transcript validated and crossed the extraction boundary; no trusted records were created.

Media transcription layer (2026-08-15, branch `feature/spoken-word-transcription`, built in an isolated worktree parallel to Codex's concurrent Evidence-architecture work): the bridge between a discovered media item and Codex's provider-neutral `TranscriptArtifact` contract (`app/services/transcript_evidence.py`, not modified). `app/services/media_transcription.py` implements the acquisition hierarchy in order -- Tier 1 publisher-declared transcript (reuses `media_discovery.acquire_raw_transcript_artifact()`, then normalizes fetched WebVTT/SRT into timed segments), Tier 2 platform-captions normalization (given an already-legitimately-obtained captions document only -- this phase deliberately does not build automated platform search/discovery, since that requires either credentials or scraping, both out of scope), Tier 3 local speech-to-text via `faster-whisper` (new pinned dependency, CPU/CTranslate2, `tiny`/`base`/`small`/`medium`/`large-v2`/`large-v3` selectable, `small` default). Raw audio downloads to `inbox/discovered_media/_media/` (gitignored, filename keyed by item id, never title); raw provider-specific artifacts (fetched transcript bodies, raw Whisper segment output) stage in `inbox/discovered_media/_transcripts/`; normalized, provider-neutral output stages in `inbox/transcripts/`, either as a `staged_transcript` (`parent_evidence_id: null`, since Phase 12 explicitly does not gate local speech-to-text on a reviewed publication-artifact Evidence id existing yet) or, once a real Evidence id is known, a fully resolved `transcript_artifact` that satisfies `TranscriptArtifact.from_dict()` verbatim -- `resolve_parent_evidence()` promotes a staged transcript to resolved in place without re-running acquisition. Two independent content-addressed caches (media download by URL, transcription by item id + media checksum + engine + model + language) make re-runs idempotent; `--force` bypasses both. `scripts/transcribe_media.py` is the CLI runner (`--item`, `--model`, `--parent-evidence-id`, `--resolve-parent`, `--force`). One small additive change to `media_discovery.py`: `raw_metadata.enclosures` now also carries each enclosure's direct media URL (previously only MIME types were captured, which was a real gap blocking audio download -- verified no existing test asserted the old field shape). 34 new offline/mocked tests (`tests/test_media_transcription.py`, deterministic fake `TranscriptionProvider`, no real Whisper invocation, no live network); full `pytest` (505 passed, 0 failed), `scripts/validate_records.py`, and `scripts/build_static.py` (1,471 pages, unchanged) all pass. Real, one-time Phase-17 dry run performed by hand (not part of automated tests): discovered the real Lucentlands feed for real (159 episodes), located episode 102 ("Scaling the Blueberry Industry...") by title/date match, re-confirmed no publisher transcript exists on the real feed, downloaded its real 42.7MB audio enclosure from Spotify-for-Podcasters/Anchor's own CDN, ran real local `faster-whisper` "small" CPU transcription (~44 min wall time for a 44.5 min episode), and produced a real, fully-resolved `TranscriptArtifact` (1,212 segments, `en`, 99.8% duration coverage, monotonic timestamps, verified with real episode content -- "blueberry" x41, "Fall Creek" x4, "genetics" x14, "Africa" x38) that `TranscriptArtifact.from_dict()` accepts cleanly, linked to the real `ev-lucentlands-scaling-blueberry-industry-2025`. No atomic Evidence was proposed, no trusted Evidence/Fact/Assessment/Recommendation was created, and `TranscriptEvidenceExtractionService.run()` was never called. No downloaded audio or generated transcript was committed (all under the pre-existing gitignored `inbox/`). Pending: merge coordination with both the acquisition-layer branch and Codex's Evidence-architecture workstream; a small future bridge script to feed a resolved transcript straight into `scripts/extract_transcript_evidence.py` would complete the `discover -> transcribe -> extract proposals` operator workflow.

Spoken-word media discovery/acquisition layer (2026-08-15, branch `feature/spoken-word-acquisition`, built in an isolated worktree parallel to another workstream's concurrent Evidence trust-boundary work): the upstream automation edge that discovers real podcast episodes, normalizes their metadata, deduplicates idempotently, and stages them outside `data/` -- stopping deliberately before anything becomes Evidence, a Fact, or an Assessment. `app/services/media_discovery.py` implements one generic `podcast_rss` discovery adapter (fetch RSS -> normalize -> compute a durable dedupe identity -> upsert a staging record), driven by an optional `discovery: {adapter, feed_url}` block on a Source's own `data/configuration/sources.json` record rather than hardcoded per-source Python -- so a second podcast RSS source plugs in by adding that block to its registry entry, no code change required. Proved against `source-lucentlands-podcast`'s real public feed (`https://anchor.fm/s/c9b3aba4/podcast/rss`, resolved via Apple's public iTunes Lookup API since the publisher's own site has no machine-readable feed link -- recorded on the Source record for provenance). Dedupe prefers, in order: publisher/feed GUID, platform item id (reserved for future adapters), normalized canonical URL, then a metadata hash -- never title alone; proven idempotent by running discovery repeatedly against an unchanged and a metadata-changed feed. Staging records live at `inbox/discovered_media/` (one JSON file per item, plus `_state/` per-source operational metadata and an opt-in `_transcripts/` for raw transcript artifacts), following the project's existing `inbox/` untrusted-intake convention -- never read by `scripts/validate_records.py` or `scripts/build_static.py`. A read-only reconciliation hook (`find_possible_evidence_matches()`) flags, but never acts on, candidate matches against already-published Evidence (e.g. the existing hand-entered `ev-lucentlands-scaling-blueberry-industry-2025`), so future automation won't blindly re-discover what a human already captured, without importing Evidence semantics into the staging layer. Transcript/caption availability detection reads only the standards-based Podcasting 2.0 `<podcast:transcript>` feed tag (no scraping, no Whisper); optional raw-transcript-body acquisition is implemented but opt-in (`--fetch-transcripts`) and untriggered by the real Lucentlands feed today (it declares no transcript tag). `scripts/discover_media.py` is the CLI runner. 23 new offline/mocked tests (`tests/test_media_discovery.py`); full `pytest` (458 passed), `scripts/validate_records.py`, and `scripts/build_static.py` (1,471 pages, unchanged by this work) all pass on this branch. No Evidence, Fact, Assessment, or Recommendation record was created or mutated; no `schemas/evidence.schema.json`, review/publish, or Evidence-rendering code was touched. Pending: merge coordination with the concurrent Evidence trust-boundary workstream once both land.

Audio/Video Intelligence data-model foundation (2026-08-15, commit `2f9580c`): `evidence.schema.json` gained two optional/additive fields — `media_format` (enum: `web_article`/`podcast`/`video`/`conference_video`, orthogonal to the pre-existing `source_type`, which describes outlet kind rather than physical form) and `transcript` (an object with `status`/`language`/`source`/`text`/`url`, ahead of any actual transcription pipeline; `status: "not_available"` is deliberately distinguishable from the field being absent). Neither is backfilled onto existing records and neither introduces a new Entity Type — podcast/video remain forms Evidence takes, not things Evidence points at. `media_format` is wired into the existing `filter_evidence()`/`filter_options()` mechanism and the Newsfeed filter bar (`app/main.py`, `app/templates/feed.html`), alongside `source`/`berry`/`geography` — no new query service or UI view. Source-owns-many-evidence and evidence-links-many-entities/geographies use pre-existing relationship fields (`source_id`, `entity_ids`, `geography_ids`) — proven, not built new. Proven entirely via 12 new temp-repo-fixture tests (`tests/test_media_evidence.py`); no fictional podcast/video records were added to the live `data/evidence/` dataset, per this project's no-fictional-intelligence-in-the-live-dataset principle. Identified but explicitly not implemented in this phase: a future `Person` entity (podcast hosts/guests, conference speakers — not modeled anywhere yet) and a real transcript-ingestion pipeline (Whisper/YouTube/RSS — the `transcript` field only describes state, nothing populates it).

Phase 2B.3 acceptance-review fixes (2026-08-14): an independent review of Phase 2B.3's original implementation (commits `aa0f0c6`/`486008d`) against its own task brief found 5 real gaps and closed all of them. (1) Review/publish's persistence orchestration was still fully embedded in the `review_publish()` route handler — extracted into `ReviewPublishService` (`app/services/review_publish.py`), a workflow service that receives repositories/a Unit-of-Work factory and returns a plain `PublishResult`; the route is now limited to HTTP form-parsing and response shaping. (2) `JsonUnitOfWork` was constructed ad hoc inside the route — `app/composition.py` now exposes `get_unit_of_work(data_dir, schemas_dir, *repository_names)`, the one place a caller with multi-object transactional intent builds that seam. (3) A genuine data-loss bug: `move_draft_attachments()` physically moved attachment files out of `inbox/` *before* the transactional block began, so a rollback after that point left the files moved but the structured writes undone — a retry's own `move_draft_attachments()` call would then find nothing to move and silently publish with zero attachments. Fixed with `restore_draft_attachments()` (reverses the move), invoked by the service if the transaction fails after attachments were moved; proven by a new regression test (`test_publish_attachment_survives_structured_failure_and_retry`) that injects a mid-transaction failure with a real uploaded attachment and confirms both the rollback and the subsequent retry. (4) The Unit-of-Work test suite was missing an explicit "successful (non-rollback) update transaction" case — added. (5) The original Phase 2B.3 work did not stop for review before proceeding into BL-034 and BL-035 (and further unrelated feature work), despite its own task brief saying not to — noted here, not undone, since that work is independently sound (see below).

Phase 2C.2 / BL-034 (2026-08-14): independent schema-validating in-memory repositories for all nine families; shared JSON/memory contract coverage; Source parity; defensive-copy mutation isolation; unchanged Core query, Berries domain, and Intelligence Package exporter consumers. Inspection fixed one real JSON leak: `get()`/`list()` now deep-copy cache-owned records. Production composition remains JSON-backed.

Phase 2C.1 / BL-035 (2026-08-14): deterministic JSON export of all nine operational families through repository interfaces, plus materialized Claims; explicit exclusion of 5 fictional Entities and 3 fictional Evidence records; validated content hash and zero unaccounted orphans; content-identical re-import through fresh temporary JSON repositories. The regenerable live artifact is `generated/intelligence-package-v2-2026-08-14` (3,259,738 bytes; 1,944 primary-family records plus 54 Claims). Attachments are optional and omitted; Workspace is compatibility manifest metadata because it is not yet persisted.

Phase 2B.3 (2026-08-14): BL-033 completed. Evidence validate/purge and every persisted Core-object create/update/delete path now cross repository interfaces. Review/publish uses one UoW across Entity, Fact, Relationship, and Evidence writes; matched existing Entities are restored from prior-value snapshots if a later write fails. Acceptance verification moved draft deletion into the UoW so unlink failure compensates structured writes and leaves the draft retryable. Inbox drafts, attachments, and blocked-domain configuration remain documented filesystem exceptions because Phase 2 defines no repositories for them. 361 tests, record validation, and the 1,463-page static build pass.

Previously: Phase 2B.1 (2026-08-14) — the record-repository layer at `app/repositories/`. Before that: Phase 2A (2026-08-14) — `docs/v2/PHASE-2-REPOSITORY-REQUIREMENTS.md`, D-012 (analytical scope). Before that: tablet navigation breakpoint fix; Phase 1.5B (`BL-026` – `BL-029`).

**Findings worth knowing about (documented, not silently resolved):**
- **Review/publish's existing-Entity update gap is resolved.** `JsonUnitOfWork` captures prior record values for updates and restores them during reverse-order rollback. Draft deletion is the final operation inside the UoW, so deletion failure rolls structured writes back and leaves the draft retryable.
- **Attachment moves are now compensated, not just "best-effort."** The acceptance-review pass found and fixed a real bug: attachments are moved out of `inbox/` before the transactional block starts (the Evidence record's `attachments` field must be known before it can be schema-validated), so a rollback after that point needs its own compensation — `ReviewPublishService` now calls `restore_draft_attachments()` if the transaction raises after attachments were moved, so a retry finds them back where `move_draft_attachments()` expects. This is a small, targeted compensating action co-located with the one real call site that needs it, not a generalized new subsystem — attachments still have no repository and none was created.
- **Review/publish now has an explicit transaction boundary outside the route.** `ReviewPublishService` (`app/services/review_publish.py`) owns entity match/create/update orchestration, Fact/Relationship/Evidence creation, and the Draft-success handoff; `app/main.py`'s `review_publish()` route is limited to form parsing and turning the service's `PublishResult` into an HTTP response. `app/composition.py`'s `get_unit_of_work()` is the one place a `JsonUnitOfWork` gets constructed, mirroring `get_repositories()`/`get_query_services()`/`get_domain_services()`.
- **One direct filesystem read remains in an application read path, and it's justified, not an oversight:** `list_drafts()` (Review Queue, duplicate detection) still reads `inbox/evidence/` directly via `load_json_files()`, because Phase 2B.1 explicitly did not build a `DraftRepository` (only 9 named object types were in scope). `docs/v2/PHASE-2-REPOSITORY-REQUIREMENTS.md` Part 10.3 documents this as deliberate, not deferred-and-forgotten.
- A single, low-materiality ordering nuance: `EvidenceRepository.list()` (built in Phase 2B.1) sorts by published/captured date descending, matching `published_evidence()`'s existing sort exactly — but `all_evidence()` (unsorted, raw file-path order, before this task) now inherits that same sort too. Verified against every `all_evidence()` call site in `app/main.py`: none depend on its order except `published_evidence()` itself (which already re-sorted identically) — the one exception is `find_possible_duplicates()`'s reviewer-facing duplicate-warning list, whose display order was never a specified or tested contract. Flagged here per this task's transparency requirement, not silently absorbed.

**In progress:**
Nothing. The spoken-word operator pipeline and repeatable extraction evaluation harness are implemented. A configured compatible model endpoint and scheduling remain operationally absent.

**Next:**
Configure a compatible model endpoint, run the benchmark plus a five-window Lucentlands sample, and apply the documented human rubric before authorizing recurring extraction. Phase 3 remains separately unauthorized.

**Next implementation action:**
Not started — both Phase 3 and any Audio/Video ingestion phase require separate authorization. PostgreSQL remains untouched.

**Blocked by:**
None.

**Known-good V1 reference:**
Tag `v1-blueberry-reference` → commit `432a96bd4efce1991df83b60aa1587154ba19528`. Unaffected by Phase 1A/1B/1.5A/1.5B/2A/2B.1/2B.2 (all work on `v2/intelligence-os`, `master` untouched).

**Architecture documents:**
Accepted (`docs/v2/00-README.md` through `10-BACKLOG.md`, 2026-08-13). `docs/v2/PHASE-2-REPOSITORY-REQUIREMENTS.md` (2026-08-14, Part 9 addendum added 2026-08-14, Part 10 addendum added 2026-08-14) is the authoritative Phase 2B implementation spec.

**Tests at baseline:**
1,110 passed, 1 pre-existing/unrelated failure (`pytest`, on canonical `a25383e`) — including offline real-model-provider, evaluation-harness, article-ingestion/dedup, freshness, corroboration/signal-candidate, domain-pack, Story Thread cross-subject-matching, mainstream/regulatory discovery, CPVO registry, and variety-footprint contract tests. `scripts/validate_records.py` passes with zero schema errors. `scripts/build_static.py` succeeds (1,527 pages, all 4 berry Landscape pages included) and excludes unreviewed inbox/evaluation/draft content (verified by its own leakage self-check). Production remains JSON-backed; PostgreSQL untouched.

**Important decisions — status:**
(IDs match `docs/v2/08-DECISION-LOG.md`)
- D-001 through D-009 — **ACCEPTED**
- D-010 — Claim stays a `fact.classification` value — **ACCEPTED (Option A)**, implemented
- D-011 — Recommendation and Evidence Priority coexist permanently — **ACCEPTED**, implemented
- D-007 (declarative Domain Packs, narrowed Phase 1 scope) — **ACCEPTED**, implemented
- D-012 (explicit analytical scope, separate from provenance) — **ACCEPTED**, schema-level implemented (Phase 2A); query-level `ScopeQueryService.explicit_scope()`/`records_by_entity_intersection()`/`scope_disagreements()` implemented and tested (Phase 2B.2), but not yet wired into any live route filter — Landscape's Assessment/Recommendation branch still uses the legacy derived-intersection rule only, preserving its exact existing behavior

No decisions remain open. No PostgreSQL or write-migration work has begun. AI-assisted transcript extraction is implemented only as an untrusted proposal boundary; it does not alter the human trust model.
