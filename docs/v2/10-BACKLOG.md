# Intelligence OS — Backlog (V2)

**Status:** Reviewed and accepted with revisions, 2026-08-13; Phase 0 executed 2026-08-13; Phase 1A (schema foundation, BL-010 – BL-015) executed 2026-08-14; Phase 1B (Domain Pack foundation, BL-018 – BL-023) executed 2026-08-14; Phase 1.5A (intelligence-object activation, BL-024 – BL-025) executed 2026-08-13; Phase 1.5B (synthesis prototype, BL-026 – BL-029) executed 2026-08-13; Phase 2A (repository contract & scope semantics, BL-036 – BL-037) executed 2026-08-14; Phase 2B.1 (record repository foundation, BL-030/BL-030-source/BL-030-uow/BL-034a) executed 2026-08-14. `BL-001`, `BL-002`, `BL-010` – `BL-029`, `BL-030`, `BL-030-source`, `BL-030-uow`, `BL-034a`, `BL-036`, `BL-037` are `Done`. **Phase 1 and Phase 1.5 are fully complete; Phase 2A is complete; Phase 2B.1 (record-repository foundation) is complete; Phase 2B.2 (query services and read-path migration) is not started; no application route has been migrated onto the repository layer yet.** This translates `07-IMPLEMENTATION-ROADMAP.md`'s phase-level goals into bounded, individually-completable work items — none of them "giant undifferentiated tasks," per the roadmap's own constraint. **Planning revision**: inserted a new Phase 1.5 (BL-024 – BL-029); added an early minimal Intelligence Package exporter to Phase 2 (BL-035); replaced Phase 3's dual-write items with the simplified seven-step sequence (BL-040 – BL-048); replaced BL-052 with a bounded review task; retired BL-053 (moved to Phase 1.5 as BL-024). **Phase 1A note**: implementing BL-014 surfaced that `app/main.py`'s live `/signals` route validates against `signal.schema.json` at runtime and depends on values this task's original data-only compatibility check missed — resolved via union enums rather than replacement; see BL-014's own row for detail. **Phase 1B note**: entity-role-vocabulary.json's actual live-data scope (65 roles, spanning every entity type) turned out broader than the blueberry package's P-9 proposal (16 roles, company-scoped) — the pack documents both, plus 10 unresolved synonym clusters, rather than picking one silently. **Phase 1.5A note**: no compatibility surprises this time — the six staged signals' evidence/entity/strategic-question references were verified to resolve against live data before import (not assumed from schema validity alone), and Assessment/Recommendation routes were new (no prior live-app constants to reconcile against). **Phase 1.5B note**: zero schema changes were needed to build the Landscape/Company/Variety synthesis views — see `docs/v2/PHASE-1-5-PROTOTYPE-FINDINGS.md` for the one real schema gap found (Assessment/Recommendation have no domain-scope field) and a data-hygiene finding (8 fictional V1 seed-data records mixed into live data folders with no structural flag, hard-coded-excluded from the Landscape).

**Size key**: S = a few hours to ~1 day of focused work. M = a few days. L = a week or more, or high uncertainty — an `L` item is itself a signal it may need splitting further once scoped in detail.

---

## Phase 0 — Freeze / reference baseline

| ID | Title | Purpose | Dependencies | Acceptance criteria | Status | Size |
|---|---|---|---|---|---|---|
| BL-001 | Tag V1 reference baseline | Guarantee a recoverable, known-good V1 state before any V2 work begins | None | Named git tag pushed; tests/validation/static build all pass against it in isolation | **Done** (2026-08-13 — `v1-blueberry-reference` tags commit `432a96b`, pushed to `origin`; 122/122 tests, zero schema errors, static build succeeded, all verified at that commit) | S |
| BL-002 | Document V1 baseline's CI independence | Confirm V2 branch work can't accidentally affect V1's live deployment | BL-001 | V1's `deploy-pages.yml` continues to succeed from the tag with zero changes required | **Done** (2026-08-13 — `deploy-pages.yml` confirmed unmodified since commit `518efc7`, well before the tag; every step it runs was reproduced locally at the tagged commit with matching results; `v2/intelligence-os` branch created from the tag and pushed, ready for V2 work without touching `master`) | S |

## Phase 1 — V2 domain definitions and abstractions

**Scope note (confirmed on review, 2026-08-13)**: the items below already implement only the six Domain Pack surfaces concretely required by the Berries reference build (manifest, entity types, predicates, taxonomies, strategic-question templates, collector templates) — no items exist here for report templates, visualization configuration, or advanced filters, consistent with D-007's narrowed Phase 1 scope. Those remain specified in `04-DOMAIN-PACK-SPEC.md` but unimplemented until Phase 1.5 or Phase 4 shows a concrete need.

| ID | Title | Purpose | Dependencies | Acceptance criteria | Status | Size |
|---|---|---|---|---|---|---|
| BL-010 | Write `assessment.schema.json` | New core object per `03-DOMAIN-MODEL.md` | BL-001 | Schema validates a hand-written example Assessment record; requires ≥1 `fact_ids` | **Done** (2026-08-14 — `schemas/assessment.schema.json`; valid + invalid examples in `tests/test_v2_schemas.py`) | S |
| BL-011 | Write `recommendation.schema.json` | New core object per `03-DOMAIN-MODEL.md`, reflecting D-011's resolved decision/action semantics (distinct from Evidence Priority) | BL-001 (D-011 resolved, see BL-017) | Schema validates a hand-written example; requires ≥1 `assessment_or_signal_ids`; does not overlap `evidence.priority`'s fields | **Done** (2026-08-14 — `schemas/recommendation.schema.json`; `action_type` is a free string, not a core enum, per D-007; `priority` is a flat scalar, structurally distinct from `evidence.priority`, verified by test) | S |
| BL-012 | Extend `evidence.schema.json` | Unified review-state enum, `source_tier`, `event_date`, `information_confidence` — all optional/additive | BL-001 | Full V1 evidence dataset (1,263 records) validates unchanged against extended schema | **Done** (2026-08-14 — all four fields added as optional; 1,263 live records validate unchanged) | M |
| BL-013 | Extend `relationship.schema.json` | Add `confidence` field (P-3, blueberry import proposal) | BL-001 | Full V1 relationship dataset (204 records) validates unchanged | **Done** (2026-08-14 — optional `confidence` field added; `notes` untouched; 204 live records validate unchanged) | S |
| BL-014 | Extend `signal.schema.json` | Enforce `minItems: 2` on `evidence_ids`; richer status enum | BL-001 | The 6 staged blueberry signals validate against the new schema | **Done, with a flagged compatibility finding** (2026-08-14 — see `PROJECT-STATUS.md` and the commit message for full detail: `app/main.py`'s live `/signals` route validates against this schema at runtime and has its own hard-coded `signal-` id prefix and strength/status vocabularies, which data-only compatibility checking missed at first. Resolved via union enums/id-pattern, not replacement, so both the live app and the 6 staged signals validate. `evidence_ids` minItems 1→2 was kept exactly as specified since it was an explicit requirement, not an incidental mismatch — this does change what the live form accepts (a single-evidence signal no longer validates); one existing test's fixture data was updated to submit 2 evidence ids, using the form field's existing multi-value support, with zero route/template changes) | S |
| BL-015 | Tighten `strategic-question.schema.json` status enum | `active/answered/retired` per P-6 | BL-001 | Full V1 strategic-question dataset (9 records) validates unchanged | **Done** (2026-08-14 — no bridge needed: all 9 live records use `status: active` only, verified directly before changing the enum) | S |
| BL-016 | Resolve D-010 (Claim schema) | Unblock Phase 1 schema work | BL-001 | Recorded decision in `08-DECISION-LOG.md` | **Done** (2026-08-13 planning review — Option A accepted: Claim stays `fact.classification`, no separate schema) | S |
| BL-017 | Resolve D-011 (Recommendation vs. `evidence.priority`) | Unblock Phase 4 planning | BL-001 | Recorded decision in `08-DECISION-LOG.md` | **Done** (2026-08-13 planning review — permanent coexistence, distinct triage-vs-decision semantics; no mechanical conversion) | S |
| BL-018 | Write `domain-pack.schema.json` | Validates the manifest shape from `04-DOMAIN-PACK-SPEC.md` | BL-010 – BL-015 | A hand-written minimal manifest validates; a manifest missing a required section fails validation | **Done** (2026-08-14 — `schemas/domain-pack.schema.json`; generic, contains no berry-specific structure, verified by test; report templates/filters/viz config deliberately not required) | M |
| BL-019 | Extract Berries entity types into Domain Pack `entity-types.json` | Replace hard-coded `SOURCE_ENTITY_TYPES`/implicit types with declared config | BL-018 | File lists all 9 live entity types (`CURRENT-STATE-AUDIT.md` Section 6), each entity in live data resolves to a declared type | **Done** (2026-08-14 — all 9 types, verified against all 162 live entities directly, not against documentation; corrected two illustrative-but-unverified trait assumptions from `04-DOMAIN-PACK-SPEC.md`'s own example — see file comments) | S |
| BL-020 | Extract Berries relationship predicates into Domain Pack `relationship-predicates.json` | Include V1's 10 plus the 6 from P-2 | BL-018 | File lists 16 predicates; every live relationship (204 records) resolves to a declared predicate | **Done** (2026-08-14 — 16 predicates; subject/object type pairs for the 9 live-used predicates taken from actual observed pairs in all 204 relationships, not guessed) | S |
| BL-021 | Build Berries taxonomy files (roles, trait vocabulary) | Per P-9 and `04-DOMAIN-PACK-SPEC.md` Section 3 | BL-018 | Entity-role vocabulary file matches the 16 roles the blueberry import package already proposed | **Done, with documented open questions** (2026-08-14 — entity-role-vocabulary.json: 65 roles, the union of all live-observed roles (55) and P-9's proposed 16, not just the 16 alone — live usage was found to span every entity type, broader than P-9's company-scoped proposal; 10 unresolved synonym clusters documented, not silently merged. trait-vocabulary.json: exactly the 10 real trait entities, with 1 documented overlap (eating-quality vs. soluble-solids/titratable-acidity)) | M |
| BL-022 | Migrate 9 live strategic questions into `strategic-question-templates.json` | Per `04-DOMAIN-PACK-SPEC.md` Section 4 | BL-018 | All 9 questions present, content unchanged | **Done** (2026-08-14 — all 9 represented exactly once, title/description verified byte-identical to live records by test; live records themselves untouched) | S |
| BL-023 | Build Berries `collector-templates.json` from the 120-source registry | Per `04-DOMAIN-PACK-SPEC.md` Section 5 | BL-018 | All 120 sources represented with their type/priority/berry/region metadata intact | **Done** (2026-08-14 — all 120 represented, 0 excluded (verified: every source had a structurally complete, templatable shape); `collector_type` field separates the platform-level mechanism (rss/keyword_search, both automatable) from reference_manual (86 sources with no automated collector today, per `ARCHITECTURE.md`) — generated programmatically from `data/configuration/sources.json` for exact fidelity, not hand-transcribed) | M |

## Phase 1.5 — Intelligence UX prototype

**Added on review, 2026-08-13.** All items run against the current JSON-backed repository — no PostgreSQL. See `07-IMPLEMENTATION-ROADMAP.md`'s Phase 1.5 for why this phase exists and what it validates.

| ID | Title | Purpose | Dependencies | Acceptance criteria | Status | Size |
|---|---|---|---|---|---|---|
| BL-024 | Import the 6 staged blueberry Signals (JSON-backed) | Closes a gap open since 2026-08-04; earliest possible real-data test of the refined Signal schema | BL-014 (Phase 1 complete) | All 6 visible and correctly linked in the running JSON-backed app | **Done** (2026-08-13 — all 6 imported into `data/signals/`, stale `not_importable_reason` dropped, every other field preserved exactly (status stays `proposed`, `reviewer` stays `null`, no upgrades); all evidence/entity/strategic-question references confirmed resolving against live data before import; validated against `signal.schema.json`; new `tests/test_intelligence_lineage.py` proves all 6 present, no duplicate ids, all references resolve) | S |
| BL-025 | Create one real, human-authored Assessment and one real Recommendation (JSON-backed) | Validates Assessment/Signal/Recommendation semantics against real usage before Phase 2/3 commit further engineering on top of them | BL-010, BL-011, BL-024 | Both exist, reviewed/published, with a correctly-traced `Recommendation → Assessment/Signal → Facts → Evidence → Source` chain | **Done** (2026-08-13 — Assessment `assessment-financial-capital-entering-berry-genetics-ownership` (medium confidence, 4 supporting facts, 1 counterevidence fact) and Recommendation `recommendation-treat-costa-driscolls-as-structurally-linked` (`escalate_to_commercial_review`, medium priority), both human-authored, reviewer `SamsonFive`; full lineage Recommendation → Assessment + imported Signal → Facts → Evidence verified live in the running app and by `tests/test_intelligence_lineage.py`'s `test_the_real_recommendation_traces_full_lineage_to_evidence`. Also built: Assessment and Recommendation list/detail/create routes and templates, reusing the Signal create-form pattern; nav entries; trust-distinguishing badges (fact/claim/assessment/signal/recommendation/counterevidence) applied across all templates that render these types) | M |
| BL-026 | Prototype a Blueberry/Berries Landscape view (JSON-backed) | First real test of a rollup/synthesis view, informing Phase 2's repository-interface query needs | BL-025 | Reachable in the running app; synthesizes multiple records into one view | **Done** (2026-08-13 — `GET /landscapes/berries/blueberry`, `app/templates/landscape.html`. Synthesizes Signals, Assessments, Recommendations, Strategic Questions, a company competitive-field rollup, a variety rollup, a geographic-footprint table, "recent meaningful movement" evidence, and an evidence-coverage/limitations summary — all from real data, with 8 known V1 fictional seed-data fixtures explicitly excluded (`SEED_FIXTURE_ENTITY_IDS`/`SEED_FIXTURE_EVIDENCE_IDS`, `app/main.py`). No composite "competitive strength" or "top variety" score anywhere — every count is a labeled coverage indicator, rows sorted alphabetically, verified by test) | M |
| BL-027 | Prototype a richer Company intelligence/portfolio view (JSON-backed) | Closes `CURRENT-STATE-AUDIT.md` Workflow B's identified gap (no portfolio rollup on a company page) | BL-025 | Shows a company's varieties/patents/relationships as a grouped view, not just the current interleaved activity timeline | **Done** (2026-08-13 — enhanced the existing generic `entity.html`/`entity_detail()` rather than forking a separate Company template, per this task's "remain generic cleanly" guidance: added "Intelligence touching this record" (Signals/Assessments/Recommendations), "Portfolio & network" (all relationships as direction-honest, evidence-linked edges — `grouped_relationships_for_entity()`), and "Strategic questions this bears on" sections, all entity-type-agnostic and reused unmodified by every entity type, not just companies. Relationship predicates render exactly as recorded (a `licenses` edge is never shown or treated as `owns`)) | M |
| BL-028 | Prototype a richer Variety intelligence view, including a first attempt at trait provenance (JSON-backed) | Closes `CURRENT-STATE-AUDIT.md` Section 5's identified gap (trait-to-variety linkage isn't structured/queryable) | BL-025 | Shows a variety's traits with their supporting evidence/facts, distinguishable from unrelated mentions | **Done** (2026-08-13 — added a variety-only "Trait profile" table to `entity.html`, resolving the real (already-imported, previously unrendered) `attributes.traits[]` structured data, with an honest source-of-claim column distinguishing OWNER/MARKETER CLAIM from independently-sourced measurement from UNRESOLVED (`variety_trait_profile()`); added "Breeding program & IP" resolving `attributes.breeding_program_id` and best-effort-matching `attributes.patent_number` against live patent entities (`variety_patent_link()`, returns None rather than guessing). "Why it matters" surfaces only real linked Signals/Assessments via the generic Intelligence section — no free-form interpretation generated from counts) | M |
| BL-029 | Document Phase 1.5 findings | Feeds Phase 2's repository-interface design directly, per this phase's stated purpose | BL-024 – BL-028 | Written document covering: what rollup queries were actually needed, what Domain Pack configuration was genuinely necessary, what data-model gaps appeared — referenced by name in Phase 2's design work, not filed unread | **Done** (2026-08-13 — `docs/v2/PHASE-1-5-PROTOTYPE-FINDINGS.md`: 9 missing query capabilities, a CORE/DOMAIN PACK/DEFER classification of every berry-specific assumption introduced, 1 concrete schema gap (Assessment/Recommendation have no domain-scope field), a DATA-GAP-vs-APPLICATION-GAP split including a real data-hygiene finding (8 fictional V1 seed-data records mixed into live folders with no structural flag), UX findings, and a 9-item concrete Phase 2 repository-interface requirements list) | S |

## Phase 2A — Repository contract & scope semantics

**Added 2026-08-14, on review.** Inserted before implementation begins, for the same reason Phase 1.5 was inserted before Phase 2 originally: a repository interface designed from planning-doc assumptions rather than the real, observed application query patterns risks a generic-CRUD abstraction that doesn't fit the actual shapes Phase 1.5 already exercised. See `docs/v2/PHASE-2-REPOSITORY-REQUIREMENTS.md` for the full deliverable.

| ID | Title | Purpose | Dependencies | Acceptance criteria | Status | Size |
|---|---|---|---|---|---|---|
| BL-036 | Inventory real data-access patterns and design the repository/query/domain-service contract | Ground Phase 2B's implementation in observed application behavior, not generic CRUD | Phase 1.5 complete | `docs/v2/PHASE-2-REPOSITORY-REQUIREMENTS.md` exists, covering every acceptance criterion this task's own brief specified | **Done** (2026-08-14 — 23 access patterns inventoried and classified Core/Domain-Pack/UI-composition against live `app/main.py` code, not just Phase 1.5's own findings doc; repository/query/domain-service boundary specified; presentation-layer separation documented; error/integrity semantics specified; seed-data Phase 3 gate proposed; Phase 2B code organization (`app/repositories/`, `app/queries/`, `app/services/berries/`) proposed; repository contract-test suite specified precisely for Phase 2B to implement, not written now, per this task's own conditional instruction) | M |
| BL-037 | Resolve Assessment/Recommendation/Signal analytical-scope gap (D-012) | Phase 1.5B's findings identified this as the one concrete schema gap the synthesis prototype surfaced; must be resolved before Phase 2B's query-service `list()`/scope-filter signatures are finalized | BL-036 | Decision recorded (`08-DECISION-LOG.md` D-012); explicit, optional, additive scope fields added to the three schemas; zero validation regressions against live data; legacy-record behavior documented | **Done** (2026-08-14 — D-012 accepted; `domain_ids`/`market_ids`/`geography_ids` added to `assessment.schema.json`/`recommendation.schema.json`, `domain_ids`/`geography_ids`/`berry_ids` (formalized) added to `signal.schema.json`, all optional; `scripts/validate_records.py` and 5 new schema tests confirm zero regressions; the one real Assessment/Recommendation and six imported Signals are unchanged in substance, correctly left with no explicit scope declared) | S |

## Phase 2B.1 — Record repository foundation

**Added 2026-08-14, on review.** The first implementation slice of Phase 2B, scoped narrowly to record repositories only (no query services, no route migration, no PostgreSQL) — see `docs/v2/PHASE-2-REPOSITORY-REQUIREMENTS.md` Part 9 for the implementation findings this slice produced.

| ID | Title | Purpose | Dependencies | Acceptance criteria | Status | Size |
|---|---|---|---|---|---|---|
| BL-030 | Implement record repositories per core object type (`app/repositories/`) | The seam Phase 3 swaps behind | BL-036, BL-037 | Implements the CORE PERSISTENCE contract specified in `docs/v2/PHASE-2-REPOSITORY-REQUIREMENTS.md` Part 3.2 for every object type listed in Part 7.2; documented | **Done** (2026-08-14 — `app/repositories/base.py` (contracts + `RecordNotFound`/`DuplicateRecord`/`InvalidRecord`/`StorageError`/`ReferentialIntegrityError`/`TransactionError`), `app/repositories/json/base.py` (shared JSON mechanics), and one repository per object type: `entities.py`, `evidence.py`, `facts.py`, `relationships.py`, `signals.py`, `assessments.py`, `recommendations.py`, `strategic_questions.py`, `sources.py`. Existing JSON file locations/formats fully preserved — verified via `scripts/validate_records.py` and live-data read-parity checks (162 entities, 1,263 evidence, 6 signals, 120 sources all matched pre-existing counts exactly)) | M |
| BL-030-source | Encapsulate Source's collection-file storage behind the same logical contract | Prove the abstraction is real for the one documented storage exception (`docs/v2/PHASE-2-REPOSITORY-REQUIREMENTS.md` Part 1.3, W4) | BL-030 | `JsonSourceRepository` exposes get/list/create/update/delete identically in shape to every other repository; callers cannot tell the backing store is one rewritten collection file, not one-file-per-record; 13 dedicated tests prove no collateral loss across sequential mutations, using only temporary fixture data | **Done** (2026-08-14 — `app/repositories/json/sources.py`; `tests/repositories/test_source_repository.py`, 13 tests; live 120-source registry never touched, verified via `git status`) | S |
| BL-030-uow | Define the Unit-of-Work / transaction-boundary seam | Phase 2A found review/publish is the only multi-object-family write in the app; design the boundary before Phase 2B.3 has to invent it mid-migration | BL-030 | A Unit-of-Work contract exists; the JSON implementation's exact guarantees (best-effort compensation, not real atomicity) are documented plainly, not implied to be stronger than they are; review/publish is **not** migrated onto it in this slice | **Done** (2026-08-14 — `app/repositories/unit_of_work.py`; `JsonUnitOfWork` performs best-effort reverse-order delete of records created earlier in the same `with` block on failure, raises `TransactionError` chained to the original exception if cleanup itself fails, and does not attempt to compensate `update()`/`delete()` calls (no prior-value capture) since Phase 2B.1's only real multi-object pattern is create-only; 6 tests in `tests/repositories/test_unit_of_work.py`) | S |
| BL-034a | Implement the repository contract-test suite (JSON backend) | Guarantee JSON and future PostgreSQL backends behave identically | BL-030 | Implements every check specified in `docs/v2/PHASE-2-REPOSITORY-REQUIREMENTS.md` Part 8.2 (same lookup/filters/ordering/create/duplicate-rejection/update/validation-rejection/delete/draft-publish-behavior), passing against the JSON backend; written to be runnable unchanged against a future second backend | **Done** (2026-08-14 — `tests/repositories/test_json_repository_contract.py`, 109 tests parametrized across all 8 one-file-per-record repositories, plus the 13 Source-specific and 6 Unit-of-Work tests above: 128 new tests total. Not yet run against a second backend, since none exists yet (BL-034, below) | M |

## Phase 2B.2 — Query services and read-path migration

**Done (2026-08-14).** Cross-object read operations (Part 3.3) implemented and the application's read paths migrated onto the repository/query/domain-service layers — see `docs/v2/PHASE-2-REPOSITORY-REQUIREMENTS.md` Part 10 for the implementation findings this slice produced (final R1-R18 mapping, boundary-discipline decisions, Part 11's W1-W6 write-call-site audit). BL-034 and BL-035 were not part of this task's scope and remain not started.

| ID | Title | Purpose | Dependencies | Acceptance criteria | Status | Size |
|---|---|---|---|---|---|---|
| BL-030a | Implement query services (`app/queries/`) | Cross-object read operations Part 1's inventory found (reverse references, timelines, lineage traversal) | BL-030 | Implements every CORE QUERY operation in `docs/v2/PHASE-2-REPOSITORY-REQUIREMENTS.md` Part 3.3, including the new `lineage()` operation named there for the first time | **Done** (2026-08-14 — 7 query services in `app/queries/`: `ReferenceQueryService`, `EntityIntelligenceQueryService`, `LineageQueryService`, `TimelineQueryService`, `ScopeQueryService`, `CoverageQueryService`, `SearchQueryService`; 20 new tests in `tests/queries/test_query_services.py` against temporary repository fixtures, covering reverse references, entity-intelligence retrieval, lineage traversal with missing/partial ids, explicit vs. legacy-derived scope (D-012), scope disagreement, domain-neutral coverage counts, and composition-cache isolation) | M |
| BL-030b | Implement Berries domain services (`app/services/berries/`) | Isolate Berries-specific rollup/synthesis logic from the generic query layer | BL-030a | Every DOMAIN-PACK / DOMAIN SERVICE function in `docs/v2/PHASE-2-REPOSITORY-REQUIREMENTS.md` Part 3.4 moved here verbatim from its current `app/main.py` location, calling only the repository/query interfaces, no direct storage access | **Done** (2026-08-14 — `app/services/berries/geography.py` (`REGIONS`/`REGION_LOOKUP`/`geography_region()`/`evidence_regions()`/`entity_regions()`/`berry_label()`), `variety.py` (`variety_trait_profile()`/`variety_patent_link()`), `landscape.py` (`BerriesLandscapeService`, every `landscape_*` sub-pattern moved verbatim); all re-exported into `app.main`'s namespace under their original names, zero behavior change) | M |
| BL-032 | Refactor evidence/entity routes onto the repository/query/domain-service layers | Remove direct file I/O from route handlers | BL-030a | No route performs direct file I/O (verified by code search); all existing tests (333 as of Phase 2B.1) pass unmodified in assertion | **Done** (2026-08-14 — `app/composition.py` is the single composition boundary, cached per `(data_dir, schemas_dir)` so test `DATA_DIR` monkeypatching stays isolated; every storage-touching loader in `app/main.py` (`all_evidence`, `all_entities`, `all_facts`, `all_relationships`, `all_signals`, `all_assessments`, `all_recommendations`, `load_strategic_questions`, `load_sources`) now delegates to a repository; `signal_detail`/`assessment_detail`/`recommendation_detail` use `LineageQueryService`; code search confirms the only remaining direct filesystem read in an application route/helper is `list_drafts()` (Drafts have no repository — Phase 2B.1 explicitly scoped to 9 named object types, justified in Part 10.3); 333 pre-existing tests pass unmodified, plus 20 new query-service tests = 353 total; `scripts/validate_records.py` and `scripts/build_static.py` (1,463 pages) unchanged) | L |
| BL-034 | Build a second (in-memory) repository implementation for test speed | Prove the seam is real, not a renamed function call | BL-030 | Test suite can run against either implementation without route code changes; BL-034a's contract suite passes against it unmodified | Not started | S |
| BL-035 | Build a minimal Intelligence Package exporter against the repository interface | Early proof of the migration-safety mechanism and the downstream-system export contract, ahead of the full report/API/export UI (Phase 6) | BL-030 | Exports the current dataset to `05-INTELLIGENCE-PACKAGE-SPEC.md` format; a re-import round-trips without information loss (`source-lineage.json`'s `orphan_check` empty); **must not include unmarked fictional/demo records** (`09-RISK-REGISTER.md` R-12) if this exporter is what ultimately produces Phase 3's seed archive | **Done** (2026-08-14 — deterministic JSON exporter/service, CLI, validator, explicit 8-record demo exclusion, zero live orphans, and content-identical temporary-repository round trip; live artifact regenerable under `generated/`; BL-034 remains open) | M |

## Phase 2B.3 — Transactional writes and remaining route migration

**Done (2026-08-14).** Review/publish now uses `JsonUnitOfWork`; existing-entity updates use prior-value snapshot-and-restore compensation. All remaining persisted core-object writes use repository interfaces. Draft, attachment, and blocked-domain filesystem writes remain explicit exceptions because no repositories exist for those non-core storage surfaces.

| ID | Title | Purpose | Dependencies | Acceptance criteria | Status | Size |
|---|---|---|---|---|---|---|
| BL-033 | Refactor remaining routes (facts, relationships, sources, review/publish, signals, assessments, recommendations, strategic questions, landscape, search) onto the interfaces | Complete the abstraction | BL-032 | Same acceptance bar as BL-032, applied repo-wide; review/publish (`docs/v2/PHASE-2-REPOSITORY-REQUIREMENTS.md` Part 1.3, W6) migrates onto `JsonUnitOfWork` (BL-030-uow), not five independent single-record saves | **Done** (2026-08-14 — single-record writes delegate to repositories; Evidence validate/purge use update/delete; Source collection rewrites are expressed through Source repository CRUD; review/publish uses one UoW across Entity/Fact/Relationship/Evidence writes; matched existing Entities are snapshot-restored on rollback; 354 tests, record validation, and 1,463-page static build pass) | L |

## Phase 3 — PostgreSQL parity migration

**Revised on review, 2026-08-13** — replaces the originally-proposed dual-write approach with the bounded seven-step sequence below (`08-DECISION-LOG.md` D-001, `07-IMPLEMENTATION-ROADMAP.md` Phase 3). No extended period with two simultaneously-written operational stores.

| ID | Title | Purpose | Dependencies | Acceptance criteria | Status | Size |
|---|---|---|---|---|---|---|
| BL-040 | Generate Postgres schema from Phase 1 JSON schemas | Ground truth stays the JSON schema, not a hand-written DDL | Phase 1, Phase 2 complete | Every field/enum/required-optional distinction preserved; reviewed against schemas by hand | Not started | M |
| BL-041 | Implement Postgres-backed repository | Second real implementation of Phase 2's interface | BL-040, BL-030 | Passes the same test suite BL-034's in-memory implementation passes | Not started | L |
| BL-042 | **Step 1** — Freeze and archive a complete, validated Intelligence Package from the current JSON-backed system | The migration safety mechanism; reuses BL-035's exporter for real, not just as a proof-of-concept | BL-035, Phase 1.5 complete | A complete, validated Intelligence Package exists, covering every live record including Phase 1.5's additions (Signals, Assessment, Recommendation) | Not started | S |
| BL-043 | **Step 2** — Load the archived package into PostgreSQL (one-time) | Seed V2's operational store; not an ongoing sync | BL-041, BL-042 | All records from the archive present in Postgres, verified by automated count-and-content check | Not started | M |
| BL-044 | **Step 2 detail** — Apply the `Evidence` review-state migration mapping during load | Per the exact mapping in `06-MIGRATION-MAP.md` | BL-043 | Every evidence record's new `review_state` matches the documented mapping rule for its V1 `status`/`validated`/`auto_captured` combination | Not started | M |
| BL-045 | **Step 3** — Deterministic JSON → Postgres → canonical JSON round-trip parity check | The core mitigation for R-01/R-11, run against the frozen archive, not a live moving target | BL-043 | Running the check reports zero discrepancies between the archived package and the round-tripped Postgres content | Not started | L |
| BL-046 | **Step 4** — Run the complete application test suite against the Postgres repository implementation | Proves behavior parity, not just data parity | BL-045 clean | Every existing test (122 plus whatever Phase 1/1.5/2 added) passes against Postgres | Not started | M |
| BL-047 | **Step 5** — Run V2 on a branch/staging environment using Postgres for a bounded acceptance period | Real usage verification, explicitly time-boxed | BL-046 | Staging period runs its full defined duration with zero discrepancies or regressions; any issue found resets the clock rather than being waived | Not started | L |
| BL-048 | **Steps 6–7** — Cut V2 over to PostgreSQL; preserve the V1 tag and archived JSON package indefinitely | Completes the migration without ever running an extended dual-write period | BL-047 clean for its full duration | App fully functional read/write against Postgres only; V1 git tag and archived Intelligence Package both verified still present and documented as permanent, post-cutover | Not started | M |

## Phase 4 — Intelligence/synthesis layer (productionization)

**Revised on review, 2026-08-13.** This phase now productionizes what Phase 1.5 already prototyped against JSON — it does not build these objects/views for the first time. BL-053 (originally "import the 6 signals") is retired here; that work moved to Phase 1.5 as BL-024, since it needed to happen before Postgres, not after.

| ID | Title | Purpose | Dependencies | Acceptance criteria | Status | Size |
|---|---|---|---|---|---|---|
| BL-050 | Productionize Assessment create/review/approve UI on Postgres | Rebuilds Phase 1.5's (BL-025) prototype properly, informed by its findings (BL-029), not designed from scratch | Phase 3 complete, BL-025, BL-029 | An analyst can create, review, and publish an Assessment through the UI, handling more than the single Phase 1.5 example | Not started | M |
| BL-051 | Productionize Recommendation create/review/approve UI on Postgres | Same, for Recommendation | Phase 3 complete, BL-025, BL-029 | An analyst can create, review, and publish a Recommendation through the UI, correctly enforcing the `assessment_or_signal_ids` requirement | Not started | M |
| BL-052 | **Replaced on review** — Bounded review of the 124 curated priority-tagged evidence records | Per D-011 (ACCEPTED): Evidence Priority and Recommendation coexist permanently with distinct semantics — no mechanical conversion. This task examines existing curated priority records and creates a Recommendation *only* where an actual, action-oriented recommendation is genuinely supported by accumulated intelligence (an Assessment or Signal justifies it) | BL-051 | A documented review of all 124 records exists, stating how many were reviewed, how many warranted a real Recommendation and why, and — just as importantly — why the rest didn't; `evidence.priority` itself is untouched | Not started | M |
| ~~BL-053~~ | ~~Import the 6 staged blueberry Signals~~ | **Retired — moved to Phase 1.5 as BL-024**, since it needed to happen before Postgres, not after | — | — | Retired | — |
| BL-054 | Productionize the Berries Landscape / Company / Variety views on Postgres | Completes Phase 1.5's (BL-026/027/028) prototypes, addressing the specific gaps their findings (BL-029) identified | BL-050, BL-051, BL-026, BL-027, BL-028, BL-029 | Each productionized view demonstrably addresses a specific gap named in Phase 1.5's findings document, not just a re-skin of the prototype | Not started | L |
| BL-055 | Fix `/work-queue` vs. `/review` count discrepancy | Named directly in `CURRENT-STATE-AUDIT.md` as a live, current bug | Phase 3 complete | Both pages agree on backlog size, sourced from the same query | Not started | S |
| BL-056 | Add visual distinction for `disputed` status (facts and relationships) | Named directly in `CURRENT-STATE-AUDIT.md` Section 7/9 as a rendering gap | Phase 3 complete | A disputed fact/relationship is visually distinguishable from an active one without reading small print | Not started | S |

## Phase 5 — AI and collector framework

| ID | Title | Purpose | Dependencies | Acceptance criteria | Status | Size |
|---|---|---|---|---|---|---|
| BL-060 | Define AI provider interface | Core Design Principle #5 | Phase 4 complete | Interface covers propose-structure, propose-assessment, summarize at minimum | Not started | M |
| BL-061 | Implement first AI provider integration behind the interface | Prove the abstraction against a real provider | BL-060 | A real structuring proposal (Fact/Claim/Relationship) can be generated and lands in the review queue as `ai_proposed: true` | Not started | M |
| BL-062 | Implement `AI Job` object with cost/timing tracking | Direct mitigation for R-04 | BL-060, Phase 3's Postgres | Every AI Job records provider, cost, timing, outcome, and reviewer decision once reviewed | Not started | M |
| BL-063 | Define Collector interface | Core Design Principle #6 | Phase 4 complete | Interface covers `collect(source_config) -> list[RawCapture]` at minimum | Not started | S |
| BL-064 | Re-implement RSS collection behind the Collector interface | Re-home proven V1 logic, don't rewrite it | BL-063 | Identical output to V1's `check_source()` RSS behavior against the same feeds | Not started | M |
| BL-065 | Re-implement keyword-search collection behind the Collector interface | Same, for the Google-News-based path | BL-063 | Identical output to V1's `check_source()` keyword-search behavior | Not started | M |
| BL-066 | Implement `Collection Job` as a stored, auditable object | Replaces fire-and-forget background task | BL-064, BL-065 | Every collection run is visible, auditable, with status/error/count recorded | Not started | M |
| BL-067 | Generalize `resolve_real_summaries.py`'s crash/retry discipline into shared job-framework error handling | Reuse proven engineering rather than re-inventing it per job type | BL-062, BL-066 | Both AI Jobs and Collection Jobs share the same retry/circuit-breaker/crash-recovery code path | Not started | L |
| BL-068 | Second-provider swap test | Proves D-005/R-05's zero-lock-in claim | BL-061 | Configuring a second AI provider requires zero application code changes | Not started | S |

## Phase 6 — Reports, exports, and API

| ID | Title | Purpose | Dependencies | Acceptance criteria | Status | Size |
|---|---|---|---|---|---|---|
| BL-070 | Design and implement versioned read API (`/api/v2/...`) | Core Design Principle #11 | Phase 3 complete | Every core object type has a working, filterable read endpoint | Not started | L |
| BL-071 | Implement minimal authentication | Direct mitigation for R-10. **Hard rule, not phase-conditional**: no writable Intelligence OS instance may be exposed to the public internet without authentication in front of it | Phase 3 complete | Write access requires authentication; `reviewer`/`approved_by` fields reference real Users; verified this gate is enforced before any public-internet exposure, regardless of phase | Not started | M |
| BL-072 | Build Report generation from Domain Pack templates | Closes the "customized intelligence reports" product-direction requirement | Phase 4 complete (Assessment/Recommendation exist to cite) | At least one report template generates a correct, fully lineage-traceable report against live data | Not started | L |
| BL-073 | Implement Intelligence Package export (JSON) | Core of `05-INTELLIGENCE-PACKAGE-SPEC.md` | BL-070 | Full-dataset export produces a valid manifest + all record types + `source-lineage.json` with empty `orphan_check` | Not started | M |
| BL-074 | Implement Intelligence Package export (JSONL, CSV) | Format completeness per the spec | BL-073 | Same content as JSON export, correctly reshaped, documented flattening notes for CSV | Not started | M |
| BL-075 | Implement Intelligence Package import (three-gate: dry-run/apply/approve) | Generalizes `import_package.py`'s proven pattern | BL-073 | A package exported by BL-073 can be re-imported into a fresh Workspace and round-trips content-identically | Not started | L |
| BL-076 | Round-trip validation test: full dataset export → import → compare | The actual proof this all works | BL-075 | Zero content differences between source and round-tripped Workspace | Not started | M |
| BL-077 | Document and publish a working external-client example against the API | Proves "downstream systems can consume this" | BL-070, BL-071 | A runnable example script (even minimal) successfully reads structured intelligence via the API | Not started | S |

## Phase 7 — Second-domain validation

| ID | Title | Purpose | Dependencies | Acceptance criteria | Status | Size |
|---|---|---|---|---|---|---|
| BL-080 | Choose and scope the second Domain Pack | Decision point: unrelated industry (recommended) vs. second berry | Phase 6 complete | A named domain, a named small dataset target, a named owner | Not started | S |
| BL-081 | Build the second Domain Pack manifest and all contribution-surface files | Per `04-DOMAIN-PACK-SPEC.md` | BL-080 | Validates against `domain-pack.schema.json`; uses only declarative contribution surfaces, zero core code changes | Not started | L |
| BL-082 | Populate a small real dataset for the second domain | Enough to prove the mechanism | BL-081 | Tens of entities, real evidence, at least a few facts/relationships, sourced from real public information | Not started | M |
| BL-083 | Run a full lifecycle pass for the second domain | Collect → structure → review → publish → report → export | BL-082, BL-072, BL-075 | One complete pass succeeds end-to-end | Not started | M |
| BL-084 | Regression-check the Berries Domain Pack throughout | Prove multi-domain coexistence, not sequential replacement | BL-081 – BL-083 | Berries continues to function unaffected at every step | Not started | S |
| BL-085 | Record findings against `04-DOMAIN-PACK-SPEC.md` | Any point where core code needed to change is a spec defect to fix | BL-083 | Written findings document; any core-code touch is either fixed (spec updated, code reverted to declarative) or explicitly, deliberately accepted as a spec gap | Not started | M |

## Phase 8 — SaaS-readiness (if justified)

| ID | Title | Purpose | Dependencies | Acceptance criteria | Status | Size |
|---|---|---|---|---|---|---|
| BL-090 | Justification decision: is Phase 8 needed now? | The actual first "acceptance criterion" for this phase, per `07-IMPLEMENTATION-ROADMAP.md` | Phase 7 complete | A named reason, a named requester, and a scoped requirements document — or an explicit decision to not proceed yet | Not started | S |

*(Further Phase 8 items are deliberately not broken out here — writing detailed backlog items for infrastructure that may never be justified would itself violate the "don't add infrastructure merely because it's fashionable" instruction. BL-090's outcome determines whether this section grows.)*
