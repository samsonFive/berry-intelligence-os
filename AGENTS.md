# Berry Intelligence OS

Local-first, evidence-based competitive intelligence for berry crops.

**Expansion phase:** Before planning new acquisition, domain-depth, variety, alternative-data, or UI V2 work, read `docs/v2/INTELLIGENCE-EXPANSION-BUILD-GUIDE.md`. Unresolved platform debt must be recorded in `docs/v2/TECHNICAL-DEBT-REGISTER.md`; source/domain expansion should update `docs/v2/INTELLIGENCE-COVERAGE-MATRIX.md`. Variety backbone (Workstream C) is `docs/v2/VARIETY-INTELLIGENCE-BACKBONE.md`. Trade is `docs/v2/TRADE-INTELLIGENCE-V1.md`. Weather is `docs/v2/WEATHER-CLIMATE-CONTEXT-V1.md`. Do not alter Variety backend/domain schemas, CPVO registry, `variety_footprint`, `commercial_observation`, Trade/Customs (`trade_observation`, Comtrade adapter), or Weather (`weather_observation`, NASA POWER adapter) unless a genuine blocker is found.

**Learner Mode (Workstream K, V1 bounded slice implemented 2026-08-23 -- see durable rules below):** Any agent working on agronomy, plant biology, IPM, harvest/agtech, sensory/flavor science, or visual learning content must read `docs/v2/feature-requests/LEARNER-MODE.md` and `docs/v2/INTELLIGENCE-EXPANSION-BUILD-GUIDE.md` section 12a first. Learner Mode is explanatory knowledge, not a trust shortcut into Signals or Assessments. Only Pillars 1 (Plant Biology & Agronomy, partial) and 4 (Taste & Consumer Science) have real content today; Pillars 2, 3, and 5 (Pest/Disease, Harvest Tech/AgTech, Visual Content) remain requirements-only.

## Cursor Cloud specific instructions

Canonical branch is `v2/intelligence-os`. Do not commit to it directly.

Feature branches can fall behind later governance (expansion guide, `PROJECT-STATUS.md`, `AGENTS.md` pointer). Rebase onto `origin/v2/intelligence-os` before merge. Preserve the canonical expansion guide verbatim. Reconcile `TECHNICAL-DEBT-REGISTER.md` and `INTELLIGENCE-COVERAGE-MATRIX.md` — do not reopen resolved UI-lane items (TD-001..004) and do not replace evidence-class counts with withdrawn drafts that marked keyword-news as `NONE`.

### Local vs cloud runtime

Cloud agents do not automatically see an operator's local `inbox/` runtime. `inbox/` is gitignored. Drafts, discovered media, transcripts, and collection run artifacts exist only on the machine that created them unless they were copied into a snapshot or explicitly provisioned.

Do not assume cloud agents can replay a previous batch, open a local review-ready draft, or inspect Whisper output from another environment.

Secrets such as `PERPLEXITY_API_KEY` are unavailable in cloud unless explicitly provisioned for that environment. Never print or persist secrets.

### Trust gates

Publication review and Atomic Evidence review remain mandatory human gates. Haiku (`anthropic/claude-haiku-4-5`) may be used for **non-trusted** publication enrichment only. It is not extraction-qualified. Never auto-qualify a model and never fall back to an unqualified extractor.

Atomic extraction qualification V2 requires the exact provider, model,
endpoint family, sanitized endpoint, prompt version, extraction version,
material settings, synthetic benchmark, and Atomic Evidence Gold Set SHA to
match an explicit human-approved marker. Automated threshold success never
creates that marker. Raw qualification responses/review packets stay under
gitignored `inbox/qualifications/` and must not enter static output. Normal CI
uses deterministic fixture responses only; never add live model calls to the
required PR checks. Recurring extraction remains disabled unless an operator
separately opts in with a currently matching marker.
`docs/v2/ATOMIC-EVIDENCE-GOLD-SET-V1.md` is the human-owned benchmark;
`benchmarks/atomic-evidence-gold-set-v1.json` is its executable representation.
Run `python scripts/materialize_atomic_gold_set.py --check` before candidate
comparison and never hand-edit a second competing fixture. A live Gold run sends
source text to the configured model endpoint and requires explicit authorization
for that destination; transport probing alone does not grant data-export scope.

Duplicate publish of an already-trusted publication id is not a 500: identical identity returns already-published success; conflicting identity returns a 409 review page and does not overwrite trusted data.

### Operating path

1. Discover: `python scripts/discover_media.py --source <source-id>` or `scripts/run_recent_batch.py`
2. Relevance screening is cheap metadata triage stored on the discovered item. Clearly irrelevant items should not be Whispered.
3. `scripts/process_discovered_media.py --item <id> --relevance-gate --enrich --max-tier 2` creates/reviews drafts without Whisper. `--max-tier 3` enables local speech-to-text.
4. Intelligence feed is `/work-queue` (Live Intelligence). The daily starting view is `/brief` (Morning Brief). Feed `Enter`/`o` opens the slide-over Reader when the V2 overlay is present; `/intelligence/{id}` remains the full reader. Pending cards can also be judged in the feed: `j`/`k` move, `a` promote, `s` keep/save, `r` dismiss/reject. Those actions POST the existing `/review/{id}/publish|save|reject` paths; they do not skip publication review. In `work_queue.html`, pending form fields live on `item.review_values` — do not use `item.values` (Jinja treats that as `dict.values`). Company/entity **Recent intelligence** uses the same reader in the live app; the public static snapshot still links trusted items to `/evidence/{id}` and never includes pending drafts. Advanced publication review remains `/review?kind=publication` and `/review/{id}`. Approve / Save / Reject have `+ Next` variants on the advanced form. AI suggestions are untrusted and visible with provenance. Feed ranking consumes stored `relevance_tier` (`adjacent` ranks below direct / unscreened spoken) and existing card `relevance_band` — do not invent a second relevance model.
5. `python scripts/collection_status.py` reports discovered / relevant / skipped / transcript-ready / enrichment-ready / publication-review-ready / trusted / extraction / atomic / retry / intervention counts.

`scripts/run_recent_batch.py` continues past per-item failures (YouTube anti-bot, missing captions, API errors). Do not treat one failed item as a batch abort. Do not report "0 failures" when transcript acquisition is blocked; review-ready without transcript is a valid state.

Standard lint/test/build commands remain those in `README.md`: `pytest`, `python scripts/validate_records.py`, `python scripts/build_static.py`.

### CI merge gate

Pull requests into `v2/intelligence-os` must report successful `Change scope`,
`Repository integrity`, `Static public safety`, and `Python tests` checks on the
current PR head before merge. Local validation is useful evidence but is not a
substitute for those GitHub checks. If checks are absent, diagnose the workflow
trigger or Actions state instead of bypassing the gate. If canonical moves,
reconcile the branch, push the new head, and wait for a fresh check set on that
SHA. Markdown-only changes use the workflow's explicit fast path; every other
change receives full tests, record validation, and static-public safety checks.

GitHub Pages deploys from `.github/workflows/deploy-pages.yml` on push to `v2/intelligence-os` or `master`. Public URL: `https://samsonfive.github.io/berry-intelligence-os/`. The static Scanner is a trusted published snapshot; it never includes `inbox/` drafts, untrusted enrichment, or the local review workbench.

Remote interactive review (Scanner, publication review, Approve/Save/Reject) is the Docker deployment in `deploy/`, documented in `docs/07-static-deployment/REMOTE-INTERACTIVE-DEMO.md`. It uses `BIOS_REMOTE_INTERACTIVE` plus **application-session login at `/login`**. Browser HTTP Basic Auth is **not** the default UX. `BIOS_SESSION_SECRET` is required in remote session mode (must not be the review password). `BIOS_BASIC_AUTH` remains optional/emergency only and must stay off in front of `/login`. Do not expose an unauthenticated localhost app. Do not commit `demo-runtime/` or `deploy/.env`.

In this Cloud Agent VM, `docker` usually needs `sudo` (the `ubuntu` user is not in the `docker` group). Host port 8000 is often already taken by a leftover local uvicorn; set `BIOS_APP_PORT` (for example `18000`) instead of killing that process by name. VPS compose binds the app to `127.0.0.1` by default so Docker does not publish 8000 on the public interface. `docker compose config` interpolates `BIOS_REVIEW_PASSWORD` and `BIOS_SESSION_SECRET` into rendered YAML — do not paste that output.

Remote interactive login is `GET /login`. Unauthenticated `/work-queue` redirects there. The session cookie is Secure only when the request is HTTPS or `X-Forwarded-Proto: https` (Caddy does this). Do not enable `BIOS_BASIC_AUTH` in front of `/login`. Remote interactive mode fails closed without `BIOS_REVIEW_USERNAME`, `BIOS_REVIEW_PASSWORD`, and `BIOS_SESSION_SECRET`.

### V2 shell (approved)

V2 product direction is **approved**. Do not reopen AppShell, context bar, Compact vs Grid, ReaderOffcanvas, or Company Profile.

Migrated V2 surfaces: Morning Brief, Live Intelligence Grid/Compact, ReaderOffcanvas, Signal Review, Company Profile, Pending Review (`/pending`), Reading Queue, Assessments, **Monitor** — Watches + Alerts (`/queues/monitoring`), Source Health (`/sources`), **Variety Intelligence** (`/entities/variety`, `/entities/variety/{id}`, `?view=compete`, `?view=observations`), **Global Intelligence Search** (`#v2SearchOffcanvas`, `/search`, `/api/search/global`), **Claim Testing** (`/queues/testing`, `/queues/testing/{id}`), **Commercial Positions** (`/queues/commercial_position`).

Do **not** migrate Sources inventory-config admin or admin/system except global regression fixes. **Landscape's prior embargo has been lifted (2026-08-23)**: the underlying trusted-intelligence base (Company/Variety Intelligence, Timelines, Compare, entity-linking precision, Signals, Assessments, rights/IP, commercial observations) was judged trustworthy enough to support an executive synthesis layer over it — see **Landscape V2** below for what was actually built and its durable rules. Do not begin AI/RAG/vector search, a Trade/Weather UI, or admin work in the same change as a Landscape mission.

**Atomic Evidence review is source-centered.** `/review?kind=atomic` groups proposals by parent publication. Exact source wording (Source says) stays distinct from the normalized proposal (System proposes). Trust actions are human, proposition-specific, and never bulk. Queue/list paths must not hydrate full article or transcript bodies; only the selected source batch hydrates parent metadata plus that source's proposals. Pending Atomic proposals, excerpts, model output, and review decisions stay private and must not appear in static/public output. Model/provider metadata is secondary and never a substitute for evidence. Atomic review is not Source Fidelity review: do not mix recovered-body authenticity decisions into Atomic approve/reject.

**Claim Testing ≠ Learner Mode.** `/queues/testing` verifies concrete source claims on tagged published Evidence (`priority.testing.level != none`) with analyst dispositions in `inbox/analyst_queue_state.json` (`needs_testing` / `pass` / `fail` / `defer`). A Claim is not a Fact; Pass is not a Fact; reprints clustered by `source_independence` are not independent corroboration. Supporting vs contradicting Evidence is only what stored `evidence_links` already record — do not invent counts. Trial/geography/variety scope on the record must not be presented as a universal trait. Reuse ReaderOffcanvas and Company / Variety / Geography profiles; do not duplicate them. Testing review state (dispositions, reviewer, history) must not leak into static/public output. Learner Mode V1 (Workstream K) is now a real, bounded, deployed feature — see its own paragraph further down for what it actually covers; `/queues/testing` itself still does not add glossary/agronomy/"Explain this" content of its own.

**Commercial Positions ≠ Position objects.** `/queues/commercial_position` is tagged published Evidence (`priority.commercial_position.level != none`), grouped by company for scanning. It is an intelligence inventory, not a queue and not a competitive score. Tag priority is not truth confidence. Facts, Signals, and Assessments stay labeled as themselves. Trade / commercial observations are context (`does_not_prove`, `berry_code_purity` stay visible). Do not invent a Position schema here. Position proposals remain Recommendation Accept/Edit/Reject. Link Claim Testing when the same Evidence is also testing-tagged; do not duplicate adjudication. Blackberry thinness is shown from stored tags, not filled. Static output is published-only (no proposed Signals, no `ai_proposed` Assessments).

**Watches are inventory / monitoring intent**, not Alerts and not KPI tiles. They answer what is being watched, why, what changed recently, and whether there is new actionable activity. Supported watch-match entity types remain `company`, `variety`, `geography`, `person` — do not invent types. Pause/remove/resume overlay `inbox/analyst_queue_state.json`; never mutate trusted Evidence `priority.*` to dequeue.

**Alerts are action.** Derive them from existing stores (proposed Signals, watch-matched Signal Candidates, new watch activity, failing/blocked Source Health). Do not create a parallel Watch/Alert intelligence store. Confirm/Dismiss on proposed Signals is the existing alert workflow and does not change Signal JSON status. A Watch never confirms a Signal. Alerts deep-link into existing decision surfaces (Pending, Signal Review, Reader, Assessment, Source Health) — do not duplicate those workflows inside Alerts.

**Source Health ≠ coverage/recall.** `/sources` is operational collection health (healthy / quiet / due / stale / failing / blocked / not configured for discovery). Quiet means a successful check found nothing new. Failing means collection errored. Blocked means publisher rejection. Manual means no discovery adapter. Never present source count, item volume, or health buckets as intelligence recall. The Coverage Matrix (`docs/v2/INTELLIGENCE-COVERAGE-MATRIX.md`) remains the authoritative maturity record. Source class chips come from existing `entity_types` / type metadata — do not hard-code recall-mission source ids.

Watch recent intelligence reuses `#v2ReaderOffcanvas` (`data-open-reader` / `[data-intel-card]`). Do not invent a Watch-specific article modal. Open Signal Candidates deep-link to `/signals/review` or `/signals/candidates/{id}`.

`/queues/monitoring` must not call `build_morning_brief`, `annotate_feed_semantics` / Story Thread grouping, or `list_discovered_items`. Source Health may scan discovered items for last-item dates; Watch/Alert pages use per-source discovery JSON only for failing/blocked alerts. `/queues/testing` and `/queues/commercial_position` must not call `build_morning_brief`, Story Thread grouping, Global Search indexing, `list_discovered_items`, `variety_footprint`, or relevance screening.

Bootstrap 5.3 is vendored at `app/static/vendor/bootstrap/` for offcanvas/collapse/grid conventions only. Do not import Mirbal, Mooli, or chart/calendar vendor bundles. Nav action counts use `.v2-count-action` (not purple inventory pills). Berry context is a selector, not a per-crop theme; unmigrated Landscape in Library still follows the selected berry (`/entities/berry` when Global). Desktop hamburger collapses the persistent sidebar into an accessible icon rail (`title` + `aria-label` + action counts); below 1100px it opens `#v2NavOffcanvas`.

Feed overlay is `#v2ReaderOffcanvas` and is shared with Company Recent Intelligence, Pending Review, Reading Queue, Watch recent intelligence, Variety recent intelligence / commercial observations, Global Search intelligence hits, Claim Testing, and Commercial Positions. Do not invent a Testing-specific article modal. `j`/`k` while it is open load adjacent `[data-intel-card]` items into the same pane. Escape closes it and returns focus to the triggering control. Compact is throughput mode (SOURCE · TIME / headline / metadata / marks; actions on hover, keyboard-current, or focus). Status marks and record type stay distinct: do not repeat `kind_label` in the compact footer. Do not invent a Variety-specific reader modal.

**Global Search is navigation, not a trust layer.** `#v2SearchOffcanvas` (click, `/`, Ctrl/Cmd+K) and `/search` group Companies / Varieties / Geographies / Sources / Intelligence / Story Threads / Signals / Assessments. Alias matches resolve to the canonical entity — do not create duplicate Company or Variety rows per alias. Result state must stay visible: Trusted, Pending, Story, Emerging signal, Confirmed signal, Assessment. Pending must never look trusted because it matched. Live `/api/search/global` may include inbox drafts and signal candidates when `include_private=1`; static/Pagefind search may only index published `data/` pages. Do not write a private/inbox search index into `generated/`. Do not call `build_morning_brief`, full Story Thread grouping of the published feed, `variety_footprint` per hit, or collection scans on the search path. Berry context prefers in-scope hits and keeps unmatched objects under **Also in Global** unless the analyst turns that off. Source hits go to Source Health (`/sources#source-{id}`), which is still collection health, not recall. Do not build embeddings, vector DB, RAG, or conversational Q&A in this surface.

Variety index uses one-pass `build_variety_indexes()` and must not call `variety_footprint` once per card. Detail may call `variety_footprint` once. `/entities/variety` must not call `build_morning_brief`, Story Thread grouping of the whole feed, or `list_discovered_items`. Global berry context filters the generic `/entities/variety` route; do not add berry-specific Variety routes. Commercial observations are Evidence (draft = pending review), never Facts. Aliases and commercial names belong on one Variety entity — do not create duplicate pages per alias. Breeder (`develops`), owner / rights holder (`owns`), licensee (`licenses`), and marketer (`markets`) stay distinct; do not flatten them to “Company.” Unnamed `variety_entity_id: null` is a first-class listing state (TD-022). IP∩observation overlap is currently empty (TD-023) — show that, do not hide it. The 18 UK Open Food Facts drafts live in gitignored `inbox/` and may be absent in Cloud Agent runtimes; do not fabricate them into `data/`.

**Variety Profile Intelligence V2** adds a `#variety-intelligence` section to the Variety detail page: `present_variety_intelligence()` (`variety_workspace.py`) groups Facts whose `entity_ids` co-occur with the variety and a real `trait-*` entity (`data/entities/traits/*.json`) into Product/sensory, Postharvest/quality, and Production/agronomic, via the small closed-vocabulary `TRAIT_TO_GROUP` lookup — not NLP over free text, and not a new Trait schema. A trait id not in that mapping falls into "Other observations" rather than being silently dropped; do not widen the mapping speculatively (TD-085). Commercial/market observations stay only in the existing Commercial footprint section — do not duplicate `_commercial_observation.html` cards inside Variety intelligence. Every row keeps its real trust class (`FACT`/`CLAIM` badge from `Fact.classification`, never flattened to one type) and real attribution (`Evidence.source_type`, humanized via `SOURCE_TYPE_LABEL` — there is no structured retailer-vs-marketer-vs-company-self-report field yet, TD-087; do not invent one). Evidence coverage ("N trusted observations · N sources · N geographies") spans product/performance + rights + commercial footprint and is a count, never a competitive or quality score — do not add one. Multiple observations about the same trait render as separate cards; never collapse them into one normalized value unless a trusted Fact itself already states that normalization. `present_variety_intelligence()` must only ever be fed `facts_for_entity()`/`all_facts()` (Facts have no draft state) and `all_evidence()`/`published_evidence()` (trusted-only, `data/evidence/`) — never wire `inbox/` Evidence into it. `build_variety_indexes()` gained an optional `facts` param plus a one-pass `product_evidence` count and `has_product_evidence`/`has_signal` index filters, following the exact same one-pass, no-N+1 discipline as the existing `rights`/`observations` walks. Real measured coverage as of this mission: 25 of 41 blueberry varieties have a trait-tagged Fact; 0 of 12 raspberry, 0 of 6 strawberry, 0 of 5 blackberry do (TD-086) — sparse non-blueberry profiles are the honest current state, not a bug in this feature.

**Learner Mode V1** (`/learn`, `/learn/{slug}`, `app/services/learner.py`) is a bounded, deterministic vertical slice of Workstream K -- 10 starter concepts (Flavor, Firmness, Shelf life, Bloom, Fruit size/caliber, Texture, Color, Precocity, Double cropping, Winter production) stored as structured JSON records under `data/learn/concepts/*.json` (mirroring the existing `data/entities/traits/*.json` pattern), not hardcoded template strings and not a new persisted trust schema. Every concept carries a `knowledge_class` from the four governance-defined classes (`foundational_knowledge`, `regional_production_practice`, `current_technical_guidance`, `consumer_sensory_observations`) and a `pillar` from the five governance pillars -- reuse these exact vocabularies for any future concept; do not invent new ones without updating `PILLAR_LABELS`/`KNOWLEDGE_CLASS_LABELS` in `learner.py`. A concept page is always rendered with an explicit "EDUCATIONAL KNOWLEDGE" mark and never uses a FACT/CLAIM/SIGNAL/ASSESSMENT badge -- Learner content must never masquerade as a trust object. "Related berry intelligence" (`related_intelligence_for_concept()`) only surfaces trusted Facts whose `entity_ids` co-occur with both a concept's declared `trait_ids` and a real Variety -- the exact same recall mechanism `present_variety_intelligence()` already uses, reusing already-loaded `facts`/`entities`/`evidence_by_id` with zero additional corpus scans; a concept with no `trait_ids` (Bloom, Texture, Precocity, Double cropping, Winter production today) honestly shows "No trusted intelligence currently linked" rather than falling back to keyword text search. "Explain this" is wired one direction only for this mission: `_present_fact_observation()` (`variety_workspace.py`) exposes a `trait_chips` list (`{id, name}` pairs) alongside the existing `trait_names`, and `_variety_profile.html` calls the `learn_href_for_trait` Jinja global (registered in `main.py` from `learner.py`'s `learn_href_for_trait_id()`) per chip -- this keeps `variety_workspace.py` with zero dependency on the Learner service; the mapping is a trait-id-to-concept-slug index built from each concept's own declared `trait_ids`, so a trait with no matching concept renders no link rather than a broken one. Search (`search_concepts()`) is a plain case-insensitive substring match over name/alias/pillar/summary -- no semantic search, no ranking model, by design. Learner Mode is wired into `build_static.py`'s public GitHub Pages output (unlike Variety/Company Compare's deliberate live-only precedent) because its content is a small, finite, enumerable set, not a combinatorial id-selection space; static pages reuse the same trusted-only `facts_all`/`evidence_idx` the entity loop already loads. Only Pillars 1 and 4 have real content as of this mission; Pest/Disease, Harvest Technology/AgTech, and Visual Content Sourcing (Pillars 2, 3, 5) remain requirements-only in `docs/v2/feature-requests/LEARNER-MODE.md`.

**Landscape V2 — Executive Competitive Overview V1** extends, does not replace, the existing per-berry `BerriesLandscapeService` (`app/services/berries/landscape.py`, `/landscapes/berries/{berry_slug}`) — the pre-V2 embargo on redesigning Landscape is lifted, but the existing single-berry page's proven sections (Executive Readout, What We Think, Actors to Watch, Competitive Pressure, Regional Briefing, What Changed, Signals, Intelligence Agenda, Explore) are reused, not rebuilt. New in V2: `landscape_context_all_berries()` powers a genuinely new cross-berry `/landscapes` ALL lens (`landscape_all.html`) — deliberately lighter than the single-berry aggregation (no theme matrix, no regional briefings), bounding Actors to Watch/Recent Moves to a handful per berry rather than every record. `landscape.html` has a load-bearing, tested architectural invariant (`tests/test_synthesis_views.py::test_landscape_static_and_live_rendering_share_one_context_pipeline`): it must never contain the literal string `static_build` anywhere, guaranteeing live and static rendering never diverge — any future addition to that specific template (not `landscape_all.html`, which has no such constraint) must render unconditionally rather than branching on `static_build`. Both `landscape_context()` and `landscape_context_all_berries()` were measured at 2-8s uncached (O(companies × evidence) + O(varieties × evidence) per berry) against real production-scale data — well over the ≤2s target — so both routes now go through `_cached_landscape_context()`/`_cached_landscape_context_all()` (`app/main.py`), a folder-signature-keyed cache mirroring the existing `_NAV_WORK_CACHE` pattern; warm requests measured at ~350-650ms. The cache key covers every `data/entities/*`, `data/evidence`, `data/relationships`, `data/signals`, `data/assessments`, `data/recommendations`, `data/strategic-questions`, and `data/facts` folder (the last added during Executive Readout V1 after discovering Landscape's own `evidence_coverage.disputed_fact_count`/`confidence_distribution` read Facts but the cache key had never included that folder — a real, now-fixed staleness gap, see TD register) — extend it if a future Landscape change reads a new folder. Evidence Coverage is now first-class, not buried in the collapsed Explore layer: `evidence_coverage.coverage_caveat` is prominent copy stating "Captured intelligence coverage, not market activity" and that most trusted Evidence today is a thin description rather than full text — never write "low competitor activity" from thin captured coverage. `competitive_themes` gained an `explain_href` per theme via a small closed `THEME_LEARN_SLUGS` dict mapping only the theme labels that have a real matching Learner Mode concept (Flavor/Sweetness, Firmness/Shelf Life, Fruit size) — Yield/Production and Climate adaptability correctly render no link rather than a fabricated one. `compare_variety_ids` (top 4 berry varieties by evidence count) feeds a "Compare this berry's varieties" deep-link into the existing Variety Compare V1 — Company Compare integration is not yet wired since Company Compare V1 has not merged to canonical as of this mission. No blended competitive-strength/threat/momentum/innovation score exists anywhere in Landscape, cross-berry or per-berry — every count is a labeled coverage indicator, per the module's own long-standing docstring.

**Executive Intelligence Readout V1** (`/readout`, `app/services/executive_readout.py`) is a third, deliberately distinct synthesis surface: Morning Brief (`/brief`) answers "what should I inspect today" (per-analyst triage, `rank_item()`'s work-queue heuristics, `last_seen_at` state); Landscape (`/landscapes`) answers "what does the captured competitive environment look like" (coverage/actors/moves); Executive Readout answers "what are the most important trusted developments and analyst interpretations to communicate upward" — corpus-wide, not per-analyst, not per-berry. Never invents interpretation: `top_assessments()`/`top_signals()` read real `Assessment`/`Signal` records as-authored and render `NO_ASSESSMENT_MESSAGE`/`NO_SIGNAL_MESSAGE` ("No analyst assessment captured." / "No confirmed or proposed Signal captured.") when none exist — do not backfill an absent `why_it_matters`/`would_change_our_view`/`rationale` with generated text. `what_changed()` is a genuinely new corpus-wide "recent" primitive (14-day window over Evidence `published_date`/Signal `first_seen`/Assessment `created_at`) — deliberately not reusing Morning Brief's per-analyst `last_seen_at` state nor Landscape's berry-scoped `recent_movement`; Facts deliberately have no independent row-kind here given TD-088/TD-089's already-measured sparse `Fact.event_date` population (TD-096) — they stay traceable via Assessment `supporting_fact_count` and their parent Evidence's own row, never hidden. "Who/what matters" reuses Landscape's own cross-berry `actors_to_watch`/`berry_rows` directly (via `_cached_landscape_context_all()`) rather than recomputing a second version. "What we actually know" reuses `CoverageQueryService` primitives (`evidence_source_breakdown`, `fact_confidence_and_disputes`) applied corpus-wide — never the expensive `extraction_backlog.inventory()` classifier (TD-093/TD-094 already established that's unsafe to run per-request). `_cached_readout_context()` (`app/main.py`) reuses the same `_LANDSCAPE_CACHE`/`_landscape_cache_key()` as Landscape (same trusted folders, same invalidation) rather than a third parallel cache. Presentation mode (`?present=1`) is fully self-contained in `executive_readout.html`'s own inline script + `.v2-presentation-mode` CSS class — no `base.html` changes, since Landscape's `evidence_coverage`/`_v2_sidebar.html`-adjacent invariants are untouched. No competitive-strength/threat/momentum/innovation score anywhere.

**Manager Brief Pack V1** (`/brief-pack`, `app/services/brief_pack.py`) is a presentation/composition surface, not a new trust object and not AI-generated narrative -- it assembles operator-selected, real canonical ids (companies, varieties, Signals, Assessments, Learner concept slugs) plus a berry/time-window context into one screen-shareable briefing, reusing existing sections rather than re-deriving them: `readout_what_changed()`/Landscape's own `actors_to_watch` for the readout/landscape snapshots, `variety_footprint()`/`present_variety_intelligence()` (the exact Variety Compare V1 primitives) for compact Variety snapshots, `ROLE_BUCKETS`/`grouped_relationships`-style role-bucketing for compact Company snapshots. Every section keeps its real badge (EVIDENCE/FACT/SIGNAL/ASSESSMENT/EDUCATIONAL KNOWLEDGE) — `company_snapshot()`/`variety_snapshot()`/`signal_snapshot()`/`assessment_snapshot()`/`learner_callout()` each return `None` for an unresolvable id (reported as "Not found," never silently dropped) rather than fabricating a stand-in. **V1 persistence model is URL-state only** — the query string is the entire pack, by explicit mission scope (TD-097); there is no `data/brief_packs/` store, no pack id, no save/rename. A brief pack's own `coverage_caveat` is a fixed string (not a per-pack disputed-fact/unresolved-SQ count) — a small selection is too specific for a meaningful corpus-wide dispute count of its own; never backfill it with a fabricated `0`. Composition was measured at ~1.9s uncached per unique selection (`all_facts()` alone ~635ms, called fresh with no wrapping cache) — `_cached_brief_pack()` (`app/main.py`) fixed this by extending the same `_LANDSCAPE_CACHE`/`_landscape_cache_key()` Landscape/Readout already use, keyed additionally by the exact selection tuple, bringing warm (same-URL) reloads to ~650ms — a deliberate choice given a brief pack's whole point is being reopened/reshared at the same URL repeatedly. Presentation mode and print CSS both reuse Executive Readout's `.v2-presentation-mode` mechanism (extended, not duplicated, in `v2.css`) plus a new `@media print` block (browser Print → PDF only; no server-side PDF generation, per explicit mission scope). Live-only by design, same rationale as Variety/Company Compare — not wired into `build_static.py` (an unbounded id-selection combinatorial space, not a finite enumerable set like Learner Mode).

**Source / Entity Intelligence Timeline V1** adds a shared `#intelligence-timeline` section (`_intelligence_timeline.html` + `_timeline_row.html`, included identically by both `_company_profile.html` and `_variety_profile.html`) built from `entity_intelligence_timeline()` (`app/queries/timeline.py`) — a derived, read-only view, not a persisted TimelineEvent object; it takes only already-loaded lists the route/build_static already fetch (`linked_evidence`, `entity_facts`, `entity_relationships`, `entity_signals`, `entity_assessments`, `evidence_idx`), adding zero new corpus scans. Event-date preference per kind is load-bearing and must not be weakened: Evidence/rights/commercial use `published_date`/`observed_at` only (no `captured_date` fallback, matching the existing `entity_activity()` convention — a captured-not-published record stays undated rather than misdated); Fact prefers `event_date` over `created_at`; Relationship uses `effective_date`; Signal uses `first_seen`/`last_updated`; Assessment uses `created_at` as its genuine semantic date (not a fallback). Any row whose date came from anything other than its own primary field carries `is_fallback_date=True` and the template shows "(recorded date)" — never present a fallback timestamp as an unqualified real-world date. Real measured population: Fact `event_date` ~24% (45/186), Relationship `effective_date` ~11.5% (27/234), Signal `first_seen`/`last_updated` 0% (0/6) — see TD-088/TD-089. A Fact or Relationship whose `evidence_ids` resolves to an Evidence row already present in this same entity's own `linked_evidence` is grouped as a `derived_items` child under that Evidence row (one source-event cluster, trust classes still individually badged) rather than shown as a third, repetitive flat entry; one pointing elsewhere keeps its own row with a `lineage` link instead — do not force grouping across entities' separate evidence pools. Items with no real date at all (not even a fallback) go to an explicit "Undated / date not established" section — never assign one arbitrarily. No competitive/importance/momentum score exists or should be added; chronology and provenance are the entire feature. Complements, does not duplicate, Variety Intelligence V2 — Timeline answers *when*, Variety Intelligence answers *what*.

**Variety Compare V1** (`/entities/variety/compare?ids=variety-a,variety-b,...`, up to `COMPARE_MAX_VARIETIES` (4)) is registered in `main.py` **before** `/entities/{entity_type}/{entity_id}` — any future route change must preserve that order, or `"compare"` will be swallowed as a literal `entity_id` and 404. `present_variety_compare()` (`variety_workspace.py`) reuses `variety_footprint()`, `present_variety_intelligence()`, and `identity_fields()` per selected variety rather than re-deriving anything; callers pass the same already-loaded base lists every other entity route already fetches once, so a 4-variety comparison costs the same as a 2-variety one in practice (measured ~800-1100ms either way) — do not add a per-variety corpus re-fetch. Canonical IDs in the query string are the entire selection state (dedup + cap to 4 + report overflow/invalid ids explicitly) — no server-side session, no private runtime state. Product/performance rows are pivoted from `present_variety_intelligence()`'s already-trusted trait groups by real trait **name**, not category, so one Fact naming several traits legitimately appears under each of its own dimensions — never collapse multiple observations for one variety/dimension into a single abstracted value, and never render a missing cell as anything other than "No trusted observation captured." Absolutely no synthesized score, ranking, or "winner" — this is comparison, not competition. Live-only by design: not wired into `build_static.py` (no finite way to enumerate every possible variety combination), matching Reading Queue/Pending Review/Global Search's own existing live-only precedent — do not attempt to statically pre-render it.

**Company Compare V1** (`/entities/company/compare?ids=company-a,company-b,...`, up to `COMPARE_MAX_COMPANIES` (4), `app/services/company_workspace.py`) mirrors Variety Compare V1's proven pattern and must preserve the same discipline. Registered in `main.py` **before** `/entities/{entity_type}/{entity_id}` — do not reorder. `present_company_compare()` is derived/read-only: it reuses `variety_footprint()`, `present_variety_intelligence()`, `identity_fields()`, and the `ROLE_BUCKETS` role-distinction discipline per portfolio variety rather than re-deriving any of that logic, and performs no new corpus scan of its own — callers pass the same already-loaded base lists every other entity route fetches once (measured ~30ms for 2 companies, ~85ms for 4, sub-linear growth, not per-company re-scanning). Canonical IDs in the query string are the entire selection state (dedup + cap to 4 + explicit `invalid_ids`/`overflow_ids` reporting) — no server-side session. `_company_portfolio_roles()` deliberately does **not** reuse `company_profile_context()`'s single-profile `company_varieties` field, which is hardcoded to the `develops` (breeder) predicate only (TD-099) — Compare walks all `ROLE_BUCKETS` roles so breeder/owner/licensee/marketer/grower/distributor stay distinct and never collapse to "has variety," even though this means Compare can show a fuller portfolio for a company than that company's own profile page currently does. Berry portfolio chips reuse the Company entity's authored `berry_ids` field as-is (same source as the single profile, TD-100) — not derived from Relationships. Variety counts, geography counts, and evidence counts are captured-coverage counts, never a score: "Coverage, not performance" is load-bearing copy, not decoration — do not add a competitive-strength, innovation, portfolio, threat, or momentum score of any kind. Rights/IP is filing counts only, never implying commercialization or innovation quality. Product/performance intelligence is a coverage-count summary per dimension (reusing `present_variety_intelligence()` group keys across a company's linked varieties) — explicitly not a full dimension-by-dimension comparison table and never "Company X has better flavor." Each compared company's "Compare {name}'s varieties" deep-link (shown when it has 2+ portfolio varieties) points into the existing `/entities/variety/compare?ids=...` route with real variety ids — do not duplicate Variety Compare's own logic here. Each company's "View full timeline" links to its existing `#intelligence-timeline` section rather than embedding the full chronology. The optional role matrix (`.v2-compare-table-wrap`, horizontally scrollable in its own container) lists only roles actually represented by at least one compared company. Live-only by design, same rationale as Variety Compare — not wired into `build_static.py`. Reachable from `entity_list.html`/`_company_profile.html` (pre-existing), and now also from `landscape_all.html`'s Actors to Watch ("Compare these companies", first 4 unique actor ids) and `brief_pack.html`'s Selected companies section ("Compare selected companies", first 4 selected ids) — both gated `{% if not static_build %}` since Company Compare stays live-only; per-berry `landscape.html` deliberately does not get this link, since its tested `static_build`-literal-string invariant (see Landscape V2 paragraph above) forbids adding any new `{% if %}` branch to that specific template.

**Company Variety Portfolio Intelligence V1** (`/entities/company/{id}/portfolio`, `present_company_portfolio()` in `app/services/company_workspace.py`) is Company Compare's single-Company analogue: Compare answers "how do 2-4 Companies differ," Portfolio answers "what does this one Company's captured Variety/genetics footprint look like." Reuses the exact same primitives Compare already proved rather than re-deriving anything: `_company_portfolio_roles()`/`_portfolio_variety_ids()` (all `ROLE_BUCKETS`, not the develops-only `company_profile_context()` field, TD-099), `variety_footprint()`, and `present_variety_intelligence()` per portfolio Variety. Each Variety row shows every real role this Company holds toward it (a Variety with, e.g., both a `develops` and an `owns` relationship shows "Breeder" and "Owner / rights holder" separately, never collapsed), rights/IP filing counts and commercial-observation counts as two genuinely separate fields (never implying a filing means commercial availability), product/sensory vs. production/agronomic evidence-coverage counts (pivoted from `present_variety_intelligence()`'s own `TRAIT_GROUP_BUCKETS` keys), captured geography count, and per-Variety Signals. Varieties are grouped by berry (`berry_groups`) with a client-side-only berry filter (`#portfolio-berry-filter`, plain JS show/hide by `data-berries` — no server round trip, so it works identically in the static build). "Recent portfolio moves" is a bounded (`PORTFOLIO_RECENT_MOVES_LIMIT` = 10), date-sorted union of dated rights/IP filings, commercial observations, and company-linked Evidence across the whole portfolio — not a second Timeline implementation; the existing `#intelligence-timeline` section on the Company/Variety profile pages remains the full chronological record, reached via "Full timeline"/each row's "Variety timeline" link. Evidence Coverage section reports `total_varieties`/`varieties_with_evidence`/`varieties_with_rights`/`varieties_with_commercial`/`varieties_with_signals`/`varieties_sparse` as plain counts only — no portfolio/innovation/genetics/threat/market-power score exists or should be added. **Unlike Variety/Company Compare, Portfolio is static-safe and IS wired into `build_static.py`**: it is a finite, enumerable one-page-per-Company set (like every `entity.html` profile page), not a combinatorial id-selection space, and every input (`published_evidence()`/`all_facts()`/`all_signals()`/`all_assessments()`) is already trusted-only — so its integration links render unconditionally rather than being gated `{% if not static_build %}` (the "Compare"/"Compare these varieties" links into the still-live-only Compare routes remain gated). Reachable from `_company_profile.html`, `entity_list.html`, `company_compare.html` ("View portfolio" per card), `landscape_all.html`'s Actors to Watch, and `brief_pack.html`'s Selected companies section; per-berry `landscape.html` deliberately does not get this link either, for the same `static_build`-literal-string invariant reason Company Compare's link was excluded there. Measured against real production data: Planasa (3 varieties, 2 berries) ~23ms, Driscoll's (5 varieties, 3 berries, one multi-role Variety) ~34ms — well under the 1s warm target, no caching needed.

**Geography / Market Intelligence V1** (`/geographies`, `/geographies/{id}`, `app/services/geography_workspace.py`) is Company/Variety Intelligence's Geography analogue: "what do we currently know about this market/geography," never a claim about total real-world market activity — absence of evidence must never mean absence of activity. Deliberately lives in a new module, not `app/services/berries/geography.py` (which already holds `geography_region()`/`REGION_LOOKUP`/`entity_regions()`): that file sits early in the import graph (`app.composition` -> `berries.landscape` -> `berries.geography`), and importing `variety_workspace`/`company_workspace` from it (to reuse `_is_rights_record`/`_is_observation`/`_rights_kind`/`_party`/`_humanize_source_type`, exactly as Company Compare/Portfolio already proved) creates a real circular import — found the hard way while building this mission. `geography_workspace.py` imports `geography_region()` from `berries/geography.py` (the safe direction) instead. A Geography's linked Evidence is derived the same dual-check way `evidence_regions()` already does (`geography_ids` array OR plain `entity_ids` membership) — never a new lookup convention. Companies represented combine the existing `operates_in` relationship predicate (same one `company_profile_context()`'s `company_geographies` already uses) with any Company named in geography-linked Evidence's `entity_ids` — either is real evidence of presence, not just one. Varieties represented are derived the same way, each carrying a `why_shown` reason (Rights / IP, Commercial observation, or the humanized source type) rather than a bare unexplained mention. Rights/IP and commercial observations are two genuinely separate sections — a filing here never implies commercial availability. The regional rollup (`region_href`/`region_name`) only links to a broader Geography when a real entity's name exactly matches the country's derived `geography_region()` label (e.g. Spain -> "Europe" -> `geography-europe`, a real entity) — deliberately no rollup for the four `REGIONS` labels with no matching entity (`Americas`, `Oceania`, `Middle East & Africa`, `Asia`), since inventing one would be exactly the "do not invent hierarchy if records don't support it" mistake this mission's brief explicitly warned against. `type` ("Country" vs. "Region / other") is derived from `attributes.iso_3166_1_alpha_2` presence, a real data-driven signal — not a hardcoded country list; two real geographies (Canada, Colombia) currently lack that attribute despite being countries and so are honestly (if imperfectly) labeled "Region / other," a known data-completeness gap, not a code bug. No market-attractiveness, competitive-intensity, or market-power score exists or should be added; every count is captured-coverage only. Static-safe and wired into `build_static.py` for the same reason Company Portfolio is (a finite, enumerable 19-entity set built entirely from trusted-only data), unlike Variety/Company Compare. Reachable from `_v2_sidebar.html`'s "Geographies" nav entry (now pointing at `/geographies` instead of the generic `/entities/geography` list) and `_company_profile.html`'s "Geographic activity" section; each Geography's Company row links onward to that Company's Portfolio. Deliberately not wired into every other geography mention in the codebase (e.g. Variety Profile's commercial-observation geography chips still use the generic `_party()` helper's `/entities/geography/{id}` href, since `_party()` is shared across every entity type and special-casing geography inside it would be a wider, riskier change than this mission's scope) — a deliberate, documented scope cut, not an oversight. Measured against real production data: `geography_index()` (all 19 geographies) ~84ms, `geography_detail()` for Spain (49 linked Evidence, 10 companies, 5 varieties) ~4ms — comfortably under the 1s warm target, no caching needed.

**Strategic Question + Decision Workspace V1** (`/strategic-questions`, `/strategic-questions/{id}`, `app/services/strategic_question_workspace.py`) organizes already-trusted Facts, Evidence, Signals, Assessments, and Recommendations around a Strategic Question -- it does not generate judgment, and has no runtime LLM dependency. Reuses the `strategic_question_ids` field already authored on Evidence/Signal/Assessment/Recommendation (proven live: every Signal, Assessment, and Recommendation in the current corpus already carries it) rather than inventing a competing Decision schema. **Facts have no `strategic_question_ids` field of their own** -- a Fact appears in "What we know" only via a linked Assessment's own `fact_ids`, never via any other inference; a Fact with no Assessment pointing at it is correctly absent, not a bug. "What we think" (Assessments) preserves `rationale`/`why_it_matters`/`would_change_our_view` exactly as authored and the `AI PROPOSED`/`REVIEWED` distinction (see Presentation Credibility Audit V1 above) -- an absent field is never backfilled with invented text. "What we don't know" (`gaps`) is five defensible zero-count checks only (no Fact/Evidence/Signal/Assessment/Recommendation) -- never an AI-inferred gap. "What would change our view" is the deduplicated set of real, non-empty `would_change_our_view` values from linked Assessments only -- an empty list renders an honest "No analyst has recorded..." empty state, never a fabricated counterfactual. Scope (`company_scope`/`variety_scope`/`geography_scope`) is derived from the union of `entity_ids` across every linked Evidence/Signal/Assessment/Recommendation/Fact, bucketed by real `entity_type` via the shared `_party()` helper -- no free-text scope duplication. Source trace is every Evidence id any linked Fact/Signal/Assessment/Recommendation actually cites via its own `evidence_ids`, plus Evidence directly scoped to the question -- no orphaned executive claim. No readiness/confidence/decision composite score exists or should be added; every count in the coverage panel is descriptive only. Real production data spans genuinely rich (`sq-global-genetics-reach`: 9 facts, 73 evidence, 2 signals, 2 assessments, 2 recommendations, zero gaps) to genuinely sparse (`sq-premium-flavor`/`sq-premium-trait-challengers`: 0 facts/signals/assessments/recommendations despite captured Evidence, all 4 non-evidence gaps shown honestly) questions -- both render correctly with no fabricated content. Static-safe and wired into `build_static.py` for the same reason Company Portfolio/Geography are (a finite, enumerable 9-record set built entirely from trusted-only data; Facts/Signals/Assessments/Recommendations have no draft state reachable from these presenters) -- this was already true of the prior, thinner `strategic_question_detail.html` before this mission, so no privacy posture changed. Wired into Global Search (`app/services/global_search.py`'s `SearchPools.strategic_questions`, a new `strategic_questions` group) -- the search-document cache key now also covers `data/strategic-questions` (a real staleness gap found and fixed alongside this, matching the same class of bug the Landscape cache's own Facts-folder gap was in an earlier mission). "View strategic question" links (using the first linked id when a record carries more than one, since none of these host surfaces show a full multi-question list) were added where cheap: `landscape.html`'s Explore Signals/Assessments rows (which also had a bare, un-fixed "ASSESSMENT" badge missed by the Presentation Credibility Audit -- fixed here too), Executive Readout's Assessment/Signal sections, and Manager Brief Pack's Signal/Assessment sections -- none of those three surfaces were redesigned. Company Profile already had a "Strategic questions this bears on" section (`entity_strategic_questions`, computed generically for every entity type by `entity_synthesis_context()`); Variety Profile was missing it despite having the same data available and now has it too; Company Portfolio and Geography Detail gained their own equivalent via a new optional `strategic_questions` keyword arg on `present_company_portfolio()`/`geography_detail()` (backward-compatible default `None`, so no existing caller broke).

**Analyst Review Session V1** (`/review-ops/session*`, `app/services/review_session.py`) was already substantially built (originally shipped via PR #147, before the Strategic Question mission) when this durable-rules paragraph was added -- audit first, don't rebuild what already exists. A session is **navigation, progress, and resume state only, never a trust object**: `SESSION_DIRNAME`/`current.json` files under `inbox/review_sessions/` hold only item ids, hrefs, filters, and completion bookkeeping, never article/transcript bodies (`_without_bodies()` strips a closed `FORBIDDEN_KEYS` set recursively before every save -- proven live with `SECRET ... MUST NOT PERSIST` markers in tests). All three queue types (`publication`/`source_fidelity`/`atomic`) stay separate, each reusing its own canonical selection logic verbatim (`rank_item()`/`assign_pending_triage()` for Publication, `build_queue_rows()`/`review_status()` for Source Fidelity, source-batch grouping by `parent_evidence_id` for Atomic) -- no second sorting/ranking model was invented. **Progress is never session-local truth**: `reconcile_session()` re-derives each item's real outcome from live canonical state (draft status, `review_events`, Source Fidelity `review_status()`) on every session-page load, so a decision made outside the session (or a stale session reopened later) is recognized correctly without requiring a session-side completion flag to agree -- `TERMINAL_PUBLICATION`/`TERMINAL_FIDELITY`/`TERMINAL_ATOMIC` define exactly which canonical states count as done, so a non-terminal Save never advances session progress. `skip_current()` only appends to the session's own local `skipped` list -- it never touches canonical queue state, confirmed by a test that skips an item and reasserts its untouched `status: pending` on disk. No route under `/review-ops/session*` renders a publish/affirm/approve/reject control; the template's own header states "This page does not publish, affirm, approve, or reject." Not wired into `build_static.py` (absent from that file entirely) -- private/live-only by construction, same as every other operator surface. Added in this pass: `list_recent_sessions()` (`HISTORY_LIMIT` = 5, sorted by `created_at` descending, explicitly excluding the currently-active session since Review Operations already shows that via its own separate "Resume" card) for the "modest recent-session history" the original build didn't yet have, and `EMPTY_QUEUE_MESSAGES` (queue-specific empty-state copy, e.g. Atomic's "No Atomic review batches available. Extraction remains disabled." instead of a generic message) via `present_session()`'s new `empty_message` field.

**Analyst Watchlist + Monitoring Workspace V1** (`/watches`, `app/services/watchlist.py`) is a private, per-analyst list of Company/Variety/Geography/Strategic Question ids the analyst is personally tracking -- **not a trust object and not a second review queue**: no route under `/watches*` renders or accepts a publish/affirm/approve/reject/confirm-signal control (`test_no_trust_action_on_watchlist_page` checks for the actual controls, not the page's own honest disclosure prose, which legitimately says "it never publishes, affirms, approves, or rejects anything"). Deliberately distinct from the pre-existing, unrelated `/queues/monitoring` ("Watches" in the sidebar) concept -- that is a shared, editorial, per-Evidence-item priority-triage dimension (`priority.monitoring.level`, one of `PRIORITY_DIMENSIONS`), not a personal watchlist; the two were kept from colliding by using unambiguous copy ("Add to watchlist" / "Remove from watchlist" / "My Watches") rather than reusing "Watch"/"Watches" text, without touching or renaming the pre-existing feature. Persisted the same way `inbox/analyst_queue_state.json` already is (see this file's own "Reading state ... is independent of trust" rule below) -- one shared, private, atomically-written `inbox/watchlist_state.json`, body-free by construction (a Watch record only ever has `watch_type`/`object_id`/`created_at`/`last_seen_at`, so there is no field for a body to leak through). "New since last check" only trusts Evidence's `published_date` (never `captured_date` -- a historical reacquisition must never masquerade as a new development) and Assessment's `created_at` (the same reliable semantic date the Intelligence Timeline already established); Signal has no comparably reliable date field (`first_seen`/`last_updated` are ~0% populated in the real corpus), so `signal_count` stays a plain total and no "new Signal" claim is made. Monitoring-degraded status is only ever surfaced for a Company watch with a real `Source.linked_competitor_ids` link (reusing `failing_source_health_rows()` from `app/services/monitor_workspace.py`, not a new calculation) -- never inferred for a Company with no direct source coverage. `mark_watch_seen()` fires only from the dedicated `/watches/open` redirect route, on an explicit analyst open -- never merely because `/watches` itself rendered. Default sort favors watches with genuinely new intelligence first, then most recent activity (both explicit, chosen via visible filter chips); Alphabetical is available as an explicit alternative. No score, importance rank, or AI-derived attention flag exists or should be added. Not wired into `build_static.py` -- private/live-only, same as Review Operations and every other operator surface.

**Saved Brief Packs + Presentation Workspace V1** (`/brief-packs`, `/brief-pack?...&pack_id=...`, `app/services/saved_brief_packs.py`) closes TD-097 (re-verified still accurate at mission start by reading the live route, not by trusting the earlier-recorded finding) by adding an optional, purely additive persistence layer on top of Manager Brief Pack V1's unchanged URL-state pipeline -- it does not touch `compose_brief_pack()`, `_cached_brief_pack()`, or `brief_pack.py` at all. A saved pack stores **selection/composition state only** -- `title`/`context_note`/`berry_id`/`window_days`/`company_ids`/`variety_ids`/`signal_ids`/`assessment_ids`/`concept_slugs`/`strategic_question_ids`, the exact same ids the query string already carried -- never a resolved snapshot of any Fact/Evidence/Signal/Assessment/Company/Variety body (`test_saved_pack_stores_selection_ids_only_no_resolved_content` checks the stored keys directly). Persisted privately under `inbox/brief_packs/{id}.json`, same private/operator-scoped precedent as `inbox/watchlist_state.json` and `inbox/review_sessions/`. **Every saved pack is a LIVE BRIEF, never a historical snapshot**: `pack_query_string()` is the one and only place a saved pack turns into a page render -- it expands the saved selection into literally the same query parameters `/brief-pack` already accepted, so reopening a pack always re-runs `compose_brief_pack()` against current trusted data (proven live: `assessment_snapshot()` reflects an `ai_proposed` flip immediately, with nothing cached in between). This is also why missing/removed objects never crash a reopened pack -- `compose_brief_pack()`'s existing `invalid_companies`/`invalid_varieties`/etc. lists (already built for the URL-state V1) handle a stale id exactly the same way whether it came from a bookmarked URL or a saved pack; no new missing-object logic was written. Full historical snapshotting stays out of scope for V1 -- Print -> PDF remains the archival mechanism, unchanged. Save As (no `pack_id`, or `save_mode=new`) always creates a new record; Save Changes (`save_mode=update` with a `pack_id`) updates that one record in place -- opening a saved pack and choosing "Save as new pack" instead never mutates the original (proven live). The only removal path is `archive_pack()`/`unarchive_pack()` (soft, reversible) -- V1 deliberately has no permanent-delete route, so a mistaken archive is never a data-loss incident. `duplicate_pack()` always allocates a fresh id and never shares storage with its source. Presentation reuses the exact existing `brief_pack.html` renderer and `?present=1` flag -- no second renderer was written, and the Saved Packs index's `present_href` already carries `&present=1`, so Open Saved Brief Packs -> choose a pack -> Present is a two-click path. Backward compatibility is exact: `pack_id` is a new optional query param with no default behavior change, so every `/brief-pack?...` URL that worked before this mission still renders identically without it. Not wired into `build_static.py` -- confirmed absent from `generated/`, private/live-only like every other operator surface.

**Intelligence Synthesis Workspace V1** (`/signals/{id}`, `/assessments/{id}`, `app/queries/lineage.py`) audited the Evidence -> Signal -> Assessment -> Strategic Question -> Brief Pack chain before writing anything and found nearly all of it already built: `signal_detail()`/`assessment_detail()` (already static-built, already live since before this mission) already showed supporting Evidence, supporting Facts, linked Entities, linked Strategic Questions, the `AI PROPOSED`/`REVIEWED` badge, and (Assessment-only) supporting Signals and Counterevidence -- via the pre-existing `LineageQueryService` (`app/queries/lineage.py`), itself purpose-built to replace three near-identical inline resolutions in these exact three routes. This mission closed exactly two real, schema-supported gaps rather than building a new workspace: (1) a Signal detail page could not show which Assessments cite it -- `LineageQueryService.resolve_linked_assessments()` only resolves forward (given ids, fetch records), so a genuine reverse lookup was added, `resolve_assessments_citing_signal(signal_id)`, scanning Assessment's own `signal_ids` field (there is no stored back-reference on Signal to keep in sync, by design -- the citing record always names the cited one, never the other way). Real production data currently has **zero** Assessments with `signal_ids` populated (confirmed by `tests/test_intelligence_lineage.py`'s own `test_every_assessment_signal_id_resolves` comment), so this section honestly shows "No analyst assessment has cited this signal yet." on every real Signal today -- proven correct via a fixture-injected test, not fabricated for the demo. (2) Assessment's `why_it_matters` field (in the schema, read by `strategic_question_workspace.py` and `brief_pack.py`, populated on 5 of the current real Assessments) was silently dropped by `assessment_detail.html` -- now rendered as a "Why it matters" section, absent (not blank) when the field is absent. A "sensible path into Saved Brief Packs" was added as a plain `/brief-pack?signals={id}` / `?assessments={id}` link on both pages (`{% if not static_build %}`-gated, since `/brief-pack` is private/live-only) -- reuses the exact existing query-string composition contract, no new persistence, no new renderer. Both routes and `scripts/build_static.py`'s matching static contexts were kept in sync (`citing_assessments` computed identically in both) to avoid the exact static-vs-live divergence bug class an earlier mission (Landscape V2) hit. No new trust route, no new review queue, no confidence/importance score, no AI-promotion path was added -- these are read-only detail pages with one new read-only reverse-lookup section and one previously-dropped read-only field.

**Strategic Question Decision Workspace V1** (extends `/strategic-questions/{id}`, `app/services/strategic_question_workspace.py`) audited the existing "Strategic Question + Decision Workspace V1" (see paragraph above) and Intelligence Synthesis Workspace V1 (see paragraph above) first and found What we know / What we think / What we are watching / What we don't know / the Watchlist toggle already fully built and live -- this pass closed three real, explicit-mechanism gaps rather than rebuilding: (1) a **Tensions / contradictions** section, using only two already-schema-supported mechanisms, never semantic-similarity inference -- `Assessment.counterevidence_ids` (the same dual Fact/Evidence reference convention `assessment_detail.html` already established, aggregated across every Assessment linked to the question) and accepted `Evidence.evidence_links` entries with `predicate == "contradicts"` (a `proposed`/`contested` link is not yet an established tension and is correctly excluded). Two real production Strategic Questions (`sq-emerging-varieties`, `sq-competitor-expansion`) already have real counterevidence via `assessment-blueberry-genetics-commercialized-through-platforms`, so this section is proven on genuine data, not a fixture-only demo. (2) Each Signal row now carries `evidence_count` (a plain `len(evidence_ids)`, matching the mission's "show supporting Evidence count/context" ask -- not a new query, the count was already on the record). (3) A "sensible route into Saved Brief Packs" -- a plain `/brief-pack?title=...&signals=...&assessments=...` link (`static_build`-gated, same pattern as the pre-existing Watch/Unwatch toggle) built entirely from ids `strategic_question_detail()` already resolves; `compose_brief_pack()` itself was not touched. **Found and fixed a real bug while building this**: naming a dict key `"items"` breaks Jinja dot-access (`row.items` resolves to `dict.items()`, the bound method, not the key) -- the counterevidence rows use `counterevidence_items` instead. No new persistence, no trust route, no auto-resolution of the Question's `status`, no confidence/importance score.

**Collection Operations & Freshness Control V1** (`/collection-ops`, `app/services/collection_ops.py`) is a thin view-model over EXISTING collection runtime/status machinery -- it introduces no second health calculation, no new trust concept, and (beyond one small bounded reader) no new persistence. `build_status_report()` is a direct, un-modified call into `CollectionStatusService.build(persisted_only=True)` (the same fast, no-item-scan path `scripts/collection_status.py` already uses without `--audit-items`); `list_recent_runs()` reads the run-summary JSON files `CollectionRunner.OperationalStateStore.save_run()` already writes to `inbox/operations/runs/*.json` for every run, bounded to the newest 10 and stripped of each summary's own `items[]` list so this stays a compact history, never a second detailed audit view or a body-bearing log. Degraded/failing Sources reuse `failing_source_health_rows()` verbatim (the same function Watchlist already uses) -- no new Source health logic exists. **`ENABLE_SOURCE_POLLING` is deliberately never presented as "is recurring collection enabled"**: that env var gates only the legacy in-process `source_polling_loop()` (writes `auto_captured: true` evidence directly, bypassing the intake pipeline), a completely different mechanism from the systemd-timer-driven `CollectionRunner`/`scripts/run_collection.py` path the rest of this page is about -- the app process has zero runtime visibility into whether that systemd timer is enabled, so the page says so honestly (a deployment-config question, not a UI toggle) rather than conflating the two. "Run now" (`POST /collection-ops/run`) shells out to `scripts/run_collection.py --all --max-items N --json-summary` (the exact CLI the production scheduler already invokes) rather than re-wiring `CollectionRunner`'s heavy construction (Whisper model, AI gateway completer, extraction-gate resolution) inside the always-on web process -- this mirrors `app.services.pipeline_scheduler.run_due_pipelines()`'s own existing subprocess-invocation pattern. `--enable-extraction` is never passed under any circumstance. The route checks lock state via a fresh `build_status_report()` call *before* attempting to spawn anything -- if a run is already active (not stale), it refuses immediately without spawning a process at all. A stale lock is left to `CollectionRunner`'s own existing self-recovery on its next real invocation; V1 deliberately has no manual "clear lock" action. GET never mutates anything (no run, no retry, no lock touch) -- `/collection-ops` and `/today`'s new one-line link into it are pure reads. Not wired into `build_static.py` -- private/live-only, same as every other operator surface.

**Public Intelligence Coverage Assurance V1** (`/coverage-assurance`, `app/services/coverage_assurance/`) is a reconciliation layer over existing machinery, not a second monitor: reuses `source_lifecycle.is_collection_eligible`, `source_freshness.classify_source_freshness`, and -- critically -- imports its 9-class miss taxonomy directly from `app.services.recall_audit.classify` (Independent Missed Intelligence Discovery + Recall Audit V1's module) rather than reimplementing it; an earlier abandoned draft of this feature had built its own parallel 7-class taxonomy before that module existed, and that duplication was removed during salvage. A body-free Source Universe (`data/configuration/source_universe.json`, `schemas/source-universe.schema.json`) records strategically relevant publishers whether or not they are onboarded -- `body`/`article_body`/`html` are schema-forbidden fields, never scraped content. Runtime reconciliation classifies every cited-or-known host as COLLECTED / KNOWN NOT COLLECTED / UNKNOWN SOURCE IDENTITY / INTENTIONALLY EXCLUDED; technical health (freshness-derived) is kept structurally separate from intelligence yield (ACTIVE/DEGRADED/UNOBSERVED/UNKNOWN/NOT_APPLICABLE from a 90-day observed-item window plus an optional explicit `yield_expectation_days` -- no invented publication frequency, ever). Benchmarks load from two places and merge: private `inbox/coverage_assurance/benchmarks/*.json` and committed `data/imports/*/benchmark.json` (so a real audit's output, e.g. the recall audit's own `benchmark.json`, is picked up automatically once merged, no code change needed). `FORBIDDEN_COMPLETENESS_CLAIMS` is exported as a module constant for tests to check against the report/matrix data -- it must never itself be embedded in the report's own returned dict (an earlier draft did this, made its own "no completeness score" test self-contradictory, and that was the actual root cause of that draft's "3 failures, not finished" checkpoint note). No candidate-Source proposal mechanism, no `/today` or `/varieties/coverage` cross-page integration, and no live web-search catch-net querying in V1 -- deliberately bounded scope; see docs/v2/PUBLIC-INTELLIGENCE-COVERAGE-ASSURANCE-V1.md's "Scope boundaries" section. Authoring-only (403 when not); absent from `build_static.py`.

Trade (`trade_observation`) and Weather (`weather_observation`) are Evidence drafts on the existing Evidence schema, not Facts and not V2 product surfaces. Do not persist derived trade/weather conclusions or auto-promote them to Signals. Raspberry/blackberry are not separable at HS-6 (TD-024); blueberry/cranberry share 081040. NASA POWER production regions are config centroids, not farm coordinates (TD-030/TD-037). Do not start a Trade/Weather UI.

Derived intelligence calculations must not silently run on unrelated synchronous page paths. Overlay `/api/intelligence/{id}/reader` skips Morning Brief entirely. HTML nav badges use cheap repository/state counts (`work_counts`, open pending, emerging candidate statuses, new-since-last-seen) plus the folder-signature `_NAV_WORK_CACHE`. Do **not** call `build_morning_brief` to paint nav badges. `/brief` stays `mode="full"`. `/pending` uses `mode="pending"` (pending ranking + story threads). `/queues/reading` may use `mode="nav"` for its own page buckets. Do not cache trust/review state beyond that signature; a publish/dismiss/read must recompute counts.

Active technical debt belongs in `docs/v2/TECHNICAL-DEBT-REGISTER.md`. Source or domain expansion updates `docs/v2/INTELLIGENCE-COVERAGE-MATRIX.md` with proven counts only — never fabricate coverage.

Pending Review is a **decision workspace**, not a Feed clone: Review now / Review soon / Adjacent / Likely ignore / Older backlog. Bulk dismiss requires explicit selection and never publishes or auto-rejects. Advanced publication form remains `/review?kind=publication`.

Reading state remains in `inbox/analyst_queue_state.json` and is independent of trust. Do not create another reading-state store.

Assessment is analyst interpretation of Facts. Signal ≠ Assessment. Signal Candidate ≠ Signal. Confirming a candidate does **not** create an Assessment. Berry scope is stored `market_ids` only; absent `market_ids` means undeclared / company-wide, not “every berry.” Do not infer berry from title, rationale, or company names, and do not hide unscoped Bottom Line rows.

### Analyst workspace

Nav purple pills (`.nav-action`) are **action counts** with a resolution workflow. Grey `.nav-inventory` figures are catalogs, not uncleared work.

Morning Brief (`/brief`) is the daily starting point after login. It ranks existing intelligence; it does not create a second store and does not mark items read or trusted when viewed. Last-seen lives in `inbox/analyst_queue_state.json` under `meta.brief` (`last_seen_at` plus a compact `source_states` snapshot used only to detect source failure/recovery). **New** (activity after last visit) and **Important** (high-value unresolved regardless of age) are separate sections.

Watch “Because” copy names the primary subject (title / alias / newsroom identity), not a co-mentioned company. Co-mentions use “mentions watched X”. Primary watch match ranks above mention match; a company primary watch ranks above a geography primary watch so Mexico/Canada headlines do not bury company-specific intelligence.

Pending drafts are triaged on `/pending` (and still summarized on `/brief#pending-triage`) into Review now / Review soon / Adjacent / Likely ignore / Older backlog using stored `relevance_tier`, berry-direct, reading priority, source `monitoring_priority`, primary subject, watch-match quality, recency, and duplicate-title warnings. This is not a second AI score. Untrusted entity suggestions come from `app/services/draft_attribution.py` (canonical name, aliases, legal names, newsroom `linked_competitor_ids`) and are not written onto trusted `entity_ids`. Dismiss uses `analyst_queue_state.json` `pending` and keeps the draft file; Reject/Promote/Save still POST the existing `/review/{id}` routes. Bulk dismiss requires explicit selection and never publishes or auto-rejects.

Related pending/current items may be presented as a **developing story / story thread** (`app/services/story_threads.py`, `/threads/{id}`). That is organizational grouping only — not a Fact, Assessment, Position, Signal, or trusted conclusion. There is no “trust thread” action. Membership is conservative (canonical URL, exact normalized title, stored `evidence_links` that are actually same-event, or same primary company/variety plus strong title/date evidence). Same-company mention is not enough. Generic patent-monitor “assignee already linked” corroboration does not merge unrelated filings with trade articles. Patents participate as generic Evidence when deterministic edges exist; do not add patent-specific UI. Dismiss redundant coverage uses the existing pending-dismiss path and preserves the file. Review Soon strip counts remain raw item counts; the bucket heading also shows distinct stories after compression.

| Surface | Count meaning | Resolution |
|---|---|---|
| Morning Brief | new-since-last + important unresolved this cycle | Reader → Mark read / Keep / Dismiss / Promote |
| Reading Queue | unread + saved items still to consume | Mark read / Keep / Dismiss / Promote. Show completed. Bulk mark visible top-priority unread/saved. Buckets: top / saved / adjacent / backlog. |
| Publication review | Review now pending drafts (action). Remaining pending is inventory. | existing Approve / Save / Reject; triage Dismiss keeps history |
| Claim testing | tagged evidence still `needs_testing` | Pass / Fail / Defer (Reopen from completed). This is claim verification, not `scripts/qualify_extraction_model.py`. |
| Watches | **active** monitors only (inventory, not a queue) | Pause (1d) / Remove watch / Resume. Stopped stays auditable. Recent intelligence opens ReaderOffcanvas. |
| Alerts "N new" | proposed signals not yet confirmed/dismissed (action). Other alert groups deep-link; they are not a second inbox. | Confirm / Dismiss on proposed Signals. Candidates → Signal Review. Activity → Reader. Failing sources → Source Health. Catalog at `/signals` is inventory. |
| Commercial positions | tagged evidence (inventory) | not a queue; no Clear. Company grouping is a view, not a Position object. Position proposals (Accept / Edit / Reject) are separate. |

Workflow state lives in `inbox/analyst_queue_state.json` (runtime, gitignored). Do not mutate trusted `data/evidence/*.json` `priority.*` fields to dequeue. Dismiss/Stop/Pass/Reject never delete source history. Reading state is independent of trust state.

Brief ranking is deterministic: direct > adjacent (stored `relevance_tier`), high reading priority, **primary** watch match (company ≫ variety ≫ geography; co-mentions are weaker and labeled “mention”), recency relative to last brief and calendar date, current `web_article` drafts, company/variety linkage, source `monitoring_priority`, unread vs already read. Do not invent an opaque AI score. Consume Claude’s article sources and source-health states; do not add ingestion adapters.

A larger object-model rewrite is still open: first-class Position objects do not exist; three monitoring concepts still coexist (evidence `priority.monitoring`, Source `monitoring_priority`, Signal `status`); model qualification stays in scripts. Do not flatten those into one object type without an explicit IA migration.

### Mutable runtime integrity

`inbox/` is runtime state; never solve cross-worktree visibility by committing
untrusted drafts. Local acquisition inboxes are isolated from production
`demo-runtime/inbox`; code deploy must not replace either. Operator draft
delivery is `scripts/deliver_drafts.py` (`app/services/draft_delivery.py`):
explicit source/destination identities, dry-run by default, additive and
hash-aware. Never overwrite an existing destination draft or analyst
`review_state`. Never copy a draft that is already trusted. Never treat
local-inbox presence as production review-ready. See
`docs/v2/ACQUISITION-PRODUCTION-DRAFT-DELIVERY-V1.md`. Production `data/`
and `inbox/` must remain on persistent mounts and be backed up before
deployment. New acquisition pipelines must
register enabled/scheduled cadence and health state in
`data/configuration/collection_pipelines.json`, use the shared runtime lock,
and version idempotency state when acquisition meaning changes. Cross-pipeline
dedup may use deterministic identity only; never fuzzy-merge claims or trust.

Untrusted Signal candidates live in `inbox/signal_candidates/` (`app/services/signal_candidates.py`). Presentation is `app/services/signal_review.py`, Morning Brief **Emerging signals**, `/signals/review`, and `/signals/candidates/{id}`. Candidate identity is opaque: persist and route by the actual `id`, never by `(entity_id, pattern_type)`. Multiple live candidates may share a company and pattern after id fingerprinting. Consume `load_candidates`, `independence_report`, and `apply_review_decision`; do not regenerate candidates or rewrite confidence to improve triage metrics. `persist_candidates()` never overwrites a reviewed file — persist a human decision with `persist_reviewed_candidate`, which refuses to create a missing id. After generation, `archive_candidates_absent_from()` moves ids that are no longer in the generated set to `inbox/signal_candidate_audit/` so live Morning Brief / company / watch counts reflect the current set; prior decisions stay on the original id and are never copied onto a regenerated id. Stale `/signals/candidates/{id}` links return 410 when an audit record exists, otherwise 404. Evidence quality is a generic overlay. Prefer Claude’s calibrated candidate stamp: an explicit `evidence_quality` field when present, otherwise the spoken-media `does_not_prove` caveat from `signal_candidates.py`. Fall back to supporting Evidence `transcript.status` / spoken `media_format` only when the candidate was not stamped. Full vs Limited source evidence is not a podcast-only branch. Confirming a candidate does **not** write `data/signals/`. Static `build_static.py` must pass `include_signal_candidates=False`. Do not lock ranking copy to a frozen candidate count. Developing stories (`/threads/{id}`) are organizational only; they may support a Signal but are not Signals. There is no “trust thread” action. A confirmed Signal Candidate is not a trusted Signal and does not create an Assessment.
