# Runtime dependencies for the interactive FastAPI service.
# Omits faster-whisper, yt-dlp, and playwright: those are local collection
# tools. The remote demo serves already-exported transcripts and does not
# acquire or transcribe media.
fastapi==0.116.1
uvicorn[standard]==0.35.0
jinja2==3.1.6
pydantic==2.11.7
jsonschema==4.25.0
python-multipart==0.0.20
feedparser==6.0.11
httpx==0.28.1
itsdangerous==2.2.0
openpyxl==3.1.5
requests==2.32.3
# trafilatura -- readable-article-text extraction for
# app/services/article_acquisition.py. Continuous Intelligence Refresh
# (2026-08-18): the recurring collector (scripts/run_collection.py --all
# --skip-transcription) runs inside this same deployed container, and
# article body acquisition is cheap/pure-Python (no system deps, unlike
# faster-whisper/yt-dlp/playwright, which stay local-only). Pinned to the
# same version as requirements.txt.
trafilatura==2.2.0
# reportlab -- server-side PDF rendering for AI-Assisted Report Builder V1
# (app/services/report_builder/pdf_export.py). Pure Python + a small C
# extension with prebuilt wheels, no browser binary or system dependency
# to install (unlike playwright, which stays local-only), so it works
# identically in both the full and remote-interactive deployments.
reportlab==4.2.5
