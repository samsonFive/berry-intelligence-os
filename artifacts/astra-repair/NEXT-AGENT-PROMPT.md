# Continue Berry OS repairs

Work in `C:/Users/Johnny/Downloads/sscanar/berry-intelligence-os-astra-repair`, branch `fix/astra-news-reader`. Read `artifacts/astra-repair/REPAIR-CHECKPOINT.md` first (both the "continuation session" sections at the top and the prior-checkpoint sections below them) for the latest commit, verification, and outstanding issues. Do not restart the investigation from scratch -- the diagnostic work below is settled, not a hypothesis to re-derive.

Johnny authorizes autonomous investigation, implementation, focused tests, browser verification, and coherent commits on this isolated branch. Do not merge, deploy, change production data, rewrite shared Git history, expose credentials, or send messages to others. Continue without non-blocking questions. Preserve a reviewable checkpoint and a refreshed continuation prompt before running out of usage. Do not consume usage-reset credits unless explicitly requested. No subagents unless separately authorized.

The product acceptance path is still: **search Cal Giant → canonical company → last 90 days by publication date → readable sources/excerpts with honest quality and review status → complete company report and PDF**. Respect publication review, source-fidelity review, and atomic-claim review as separate human gates. Never fabricate article bodies, trust, analysis, relationships, or screenshot data. Pending items stay private. Avoid new parallel discovery engines: reuse Industry Pulse, media discovery/acquisition, Source Health, and the existing reader.

## Settled facts from the last session -- do not re-investigate these

1. **Full suite is clean**: 2639 passed, 0 real failures. (One `tests/exports/test_intelligence_package.py` failure during the full run was a confirmed self-inflicted race from a concurrent file edit, not a code defect -- re-ran isolated, 6/6 passed.)
2. **Fixed**: a real bug where a new draft's top-level `published_date` (the field every date view reads) came only from the discovery feed's signal and never checked against the real date extracted from the actual article page. Now reconciled in `app/services/article_refresh.py::_reconcile_published_date()`, with honest provenance (`published_date_basis`, `discovery_provenance.discovery_published_date`). This was necessary, not cosmetic -- confirmed against a real calgiant.com sitemap that stamps a bulk 2026-07-14 republish date onto ~a dozen unrelated 2018-2023 articles.
3. **calgiant.com direct acquisition is blocked by a client TLS/HTTP fingerprint** (Cloudflare bot management), confirmed via A/B test: identical URL + identical User-Agent succeeds via `curl`, fails via this project's `httpx` client, on the sitemap, the newsroom index, and individual article pages alike -- while `robots.txt` explicitly permits crawling. This is documented in both Cal Giant entries in `data/configuration/sources.json`. **Do not try to "fix" this by adopting a browser-fingerprint-spoofing HTTP client (e.g. curl_cffi) without first deciding, deliberately, whether that crosses from honest diagnosis into bot-detection evasion** -- the instructions governing this repair explicitly caution against exactly that, and the codebase's whole ethos is "report a wall honestly, never work around it."
4. **76 of 201 configured sources are collection-eligible but have never been run once in this local snapshot** (`last_checked_at: null` on all 76), including three already-verified-live syndicators independently known to carry real historical Cal Giant content: `source-freshplaza-global`, `source-20260819-blue-book-services`, `source-20260824-perishable-news-produce`. No scheduled workflow triggers collection anywhere in this repo -- it is purely operator-invoked.
5. **The pipeline itself works, proven live this session** (not just in tests): running `scripts/ingest_articles.py --source <id>` against those three sources (with `PERPLEXITY_API_KEY` unset, so no LLM calls) produced 3 real, honestly-acquired review-ready drafts from FreshPlaza (zero fabrication), correctly all-irrelevant screening from Perishable News, and correctly-honest `blocked` reporting for all 5 Blue Book Services items that reached acquisition. None of the three runs' current RSS windows happened to contain a fresh Cal Giant item today -- RSS is forward-only and does not backfill the missing Jun 14 - Sep 11, 2026 window.

## Next bounded milestone

Pick up from "Remaining issues" in the checkpoint. In order of priority:

1. **Decide and act on forward collection at scale.** The 76 never-run eligible sources (not just Cal Giant's two blocked reference entries) are sitting idle. Either run `scripts/run_collection.py` across them for real (bounded, review-gated, watch for surprises -- this session only proved 3 as a narrow proof-of-concept via the thinner `ingest_articles.py` CLI, not the full recurring orchestrator with its lock/retry machinery) or make a deliberate, documented call not to, with reasons.
2. **Design a compliant historical-backfill path for Cal Giant** if the user still wants the Jun-Aug 2026 gap closed without waiting for forward collection to catch up naturally: e.g., a syndicator's own site-search/archive page for "California Giant" (through the *existing* discover → screen → acquire → review pipeline, never a new parallel engine), not calgiant.com direct (blocked) and not a fabricated summary.
3. Only after (1)/(2) materially move the needle, revisit the wider build guide (`docs/v2/INTELLIGENCE-EXPANSION-BUILD-GUIDE.md`) for anything beyond this path.

## Runtime / housekeeping (unchanged from prior prompt)

Use `../berry-intelligence-os/.venv/Scripts/python.exe` (the parent repo's Python 3.13 environment); system Python lacks dependencies. Git needs per-command `-c safe.directory=C:/Users/Johnny/Downloads/sscanar/berry-intelligence-os-astra-repair`; do not globally disable safe.directory. Default autocrlf applies; do not force false and stage whole-file line-ending churn. The original sibling checkout contains pre-existing work; leave it alone.

Validation: focused pytest first, then full pytest / `scripts/validate_records.py` / `scripts/build_static.py`. Full suite takes ~15 minutes now (grew from ~10 -- more tests, or just machine load; not itself a concern). **If editing `data/configuration/sources.json` or other files a full-suite export/round-trip test might read, avoid doing so while a full-suite run is in flight in the background** -- this exact collision caused this session's one (non-reproducing) failure. Use browser tools for local UI checks and save actual screenshots; nothing changed in the UI this session so no new screenshots were taken (prior checkpoint's screenshots in `screenshots/` still stand for what they cover). Local preview may still run at `http://127.0.0.1:8018`, PID 25144, confirmed alive at end of session -- verify before restarting only that owned server. `PERPLEXITY_API_KEY` is set in this environment; explicitly unset it (`PERPLEXITY_API_KEY= <cmd>`) for any diagnostic ingestion run where deterministic, no-LLM-cost behavior is wanted, as this session did.

Keep context economical: read targeted files, run small focused tests during iteration, run the full suite once near the checkpoint, and record reproducible facts rather than repeating broad repository audits. Finish with commit IDs, real screenshots (if the UI changed), test outcomes, material limitations, and the next concrete acceptance test.
