# Astra repair — paused for user review

Date: 2026-09-11. Branch: `fix/astra-news-reader`.
Base: `c0de14c88960c22fee97a51fef0c71ffe386bcf9` (`origin/v2/intelligence-os`).
Worktree: `C:/Users/Johnny/Downloads/sscanar/berry-intelligence-os-astra-repair`.

This is an unfinished review checkpoint, not a release candidate. Development paused at the user's request. No merge, production deployment, production data changes, or remote branch push was performed.

## Implemented

- Default root entry redirects to the shared news page. Latest, Today, seven-day, archive, and undated editions use publication dates, with explicit timezone handling and 24 stories per page. Capture dates cannot promote old reporting into Today.
- Berry, geography, company and text filters persist in pagination links. Unknown geography stays unknown. Company filtering reuses conservative name/alias recall and labels named mentions separately from structured links.
- Existing native reader now preserves edition URL state, opens from shareable story links, supports history handling, and avoids duplicate title/summary presentation. Full reader and evidence detail use the shared stakeholder shell. Company and variety pages have section links.
- Read-time guards suppress stored consent/bot-screen text and its unsupported interpretation without rewriting source history. Acquisition and enrichment guards reject known failed-page content before AI enrichment. Source completeness reflects these failures.
- Verified California Giant Berry Farms canonical identity and aliases added, with official identity-source URLs. No factual claims, ownership relationships, genetics links or trust promotions were generated by the identity addition.
- Future article acquisition saves explicit publisher image metadata and its origin, plus extracted publication-date basis. Missing historical imagery is not fabricated.
- Explicit fictional seed stories/companies are excluded from news. Latest edits reuse existing Industry Pulse relevance rules to exclude unrelated stories; these edits await validation.

## Findings

- Production at `https://intel.johnnyaceii.com` was observed serving the old root newsfeed with 1,288 items rendered together. Actual deployed Git SHA was not established.
- Read-only checked-in corpus audit: 1,269 records, 71 flagged for consent/bot text, 20 mentioning California Giant, and no canonical California Giant company before repair. These are local corpus counts, not production counts.
- Historical source bodies are largely absent from the checked-in corpus. The repair does not recover missing prose merely by improving the reader. Existing private Source Fidelity Review/reacquisition workflows remain the appropriate path; no historical bulk crawl or production inbox synchronization was performed.
- The checked-in corpus has no recent reporting for the current date. Empty current editions are shown honestly, with an archive link.

## Verification completed

- Earlier focused run: **103 passed**, one dependency warning (`focused-tests.txt`). Subsequent edits are not covered by that result.
- Full run: **2,624 passed, 10 failed**, 602.71 seconds (`full-tests.txt`). This run started before the final relevance/layout edits and does not validate the final checkpoint in full.
- Record validation passed before the final image-schema additions (`record-validation.txt`).
- Browser: archive and reader rendered, deep-linked reader reopened on reload, duplicate title/summary removed, Escape closed the reader. California Giant appeared in the company options. Screenshots retained.
- `reader-after.png` shows the revised reader. `archive-after.png` is an earlier intermediate layout, not the final checkpoint. Final compact layout and relevance changes await updated screenshots.
- Git whitespace check passed. Static build has not yet been run for this checkpoint.

## Outstanding before acceptance

1. Reconcile the ten full-suite failures: homepage/analyst-console integration and navigation expectations, legacy feed assertions, plus an exact entity-count assertion. Preserve required analyst functionality rather than simply removing its tests.
2. Retest final relevance, image metadata, timezone persistence, malformed image handling, alias-filter behavior and seed exclusions. Check for lost useful regulatory/market coverage and poor-content false positives.
3. Finish browser Back/Forward, next/previous story, return focus/scroll, filter persistence, mobile viewport, full reader and company/variety navigation checks. Save current screenshots and comparable timings.
4. Run record validation and static build, including private-data leak checks. Review static navigation and shared styles.
5. Add a reproducible audit command and repair acceptance report. Document limitations around missing source bodies, date provenance, quality classification and production/runtime differences.
6. Continue the broader repository/build-guide work after user review. The expansion guide and backlog were inspected; the broader implementation phase is not complete. Priorities include source fidelity, source/competitor coverage, shared UI consistency and benchmark-backed recall. Existing trust gates remain mandatory.

## Assumptions

- Repair brief is supporting context; the user's explicit constraints govern execution.
- Current publication dates remain as stored. No guessed dates or new event dates.
- Canonical aliases support discovery; textual mentions do not establish business relationships.
- Existing JSON/repository services, reader, qualification and source-fidelity mechanisms are reused.
- Local preview uses this isolated worktree and its private local runtime; it is not production.
