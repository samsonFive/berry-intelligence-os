{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "strategic-question.schema.json",
  "title": "Strategic Question Record",
  "type": "object",
  "required": ["id", "record_type", "title", "status"],
  "properties": {
    "id": {"type": "string", "pattern": "^sq-[a-z0-9-]+$"},
    "record_type": {"const": "strategic_question"},
    "title": {"type": "string", "minLength": 1},
    "description": {"type": "string"},
    "status": {
      "enum": ["active", "answered", "retired"],
      "$comment": "V2 Phase 1 (BL-015, docs/v2/10-BACKLOG.md): replaces [active, resolved, archived] per docs/v2/03-DOMAIN-MODEL.md and the blueberry import package's proposed-schema-enhancements.md P-6. No compatibility bridge needed: all 9 live strategic-question records use status=active only, so no existing record used the old resolved/archived values -- verified directly before this change, not assumed."
    },
    "berry_ids": {"type": "array", "items": {"type": "string"}},
    "evidence_ids": {"type": "array", "items": {"type": "string"}}
  }
}
