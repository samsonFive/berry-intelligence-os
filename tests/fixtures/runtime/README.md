# Synthetic runtime fixture

Every record below is explicitly fictional and hand-authored for persistence,
status, and collection-runner tests. It is not copied from an operator inbox,
production backup, transcript, or untrusted collection run. The fixture may be
committed because it contains no real runtime content or secrets.
